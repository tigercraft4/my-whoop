---
phase: 05-ios-app-server-port
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/gen_synthetic_fixtures.py
  - scripts/gen_golden.py
  - Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/frames_5.json
  - Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/golden_5.json
  - Packages/WhoopProtocol/Tests/WhoopProtocolTests/Parity5Tests.swift
  - Packages/WhoopProtocol/Tests/WhoopProtocolTests/SchemaSyncTests.swift
autonomous: true
requirements: [SWIFT-05]
must_haves:
  truths:
    - "Existem golden fixtures 5.0 Maverick-wrapped para XCTest"
    - "Os testes Swift 5.0 passam (paridade byte-a-byte com o decoder Python)"
    - "SchemaSyncTests compara contra whoop_protocol_5.json"
  artifacts:
    - path: "Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/frames_5.json"
      provides: "fixtures de frames 5.0 Maverick-wrapped"
    - path: "Packages/WhoopProtocol/Tests/WhoopProtocolTests/Parity5Tests.swift"
      provides: "teste de paridade Python-Swift para 5.0"
      contains: "frames_5"
  key_links:
    - from: "Parity5Tests.swift"
      to: "frames_5.json"
      via: "Bundle.module.url(forResource:)"
      pattern: "frames_5"
    - from: "gen_synthetic_fixtures.py build_maverick_frame()"
      to: "frames_5.json"
      via: "json.dump"
      pattern: "build_maverick_frame"
---

<objective>
Gerar golden fixtures 5.0 Maverick-wrapped e adicionar testes Swift de paridade que provam que o decoder Swift produz output byte-a-byte idêntico ao decoder Python sobre os mesmos bytes (SWIFT-05). Inclui a correcção dos SchemaSyncTests para o schema 5.0 (corolário de D-01).

Purpose: Sem paridade validada, não há garantia de que o strip Maverick + offsets body-absolute portados em 05-01 estão correctos. Este plano é o gate de qualidade do decoder.
Output: `frames_5.json` + `golden_5.json`, `Parity5Tests.swift`, e SchemaSyncTests verdes; `swift test` passa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ios-app-server-port/05-CONTEXT.md
@.planning/phases/05-ios-app-server-port/05-PATTERNS.md
@.planning/phases/05-ios-app-server-port/05-01-SUMMARY.md
@re/survey_5/validate_frames_5.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_maverick_frame() + geracao de frames_5.json/golden_5.json (D-03)</name>
  <files>scripts/gen_synthetic_fixtures.py, scripts/gen_golden.py, Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/frames_5.json, Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/golden_5.json</files>
  <read_first>
    - scripts/gen_synthetic_fixtures.py (build_frame() padrao linhas 78-84; _OUT_DIR; padrao de output array de objectos com campo hex + json.dump)
    - scripts/gen_golden.py (wrapper fino sobre gen_synthetic_fixtures.main())
    - re/survey_5/validate_frames_5.py (strip_maverick() e o layout Maverick — alvo inverso de build_maverick_frame)
    - re/survey_5/frames_5_golden.json (corpus de 123 fixtures 5.0 curados — formato de referencia)
    - server/packages/whoop-protocol/whoop_protocol/schema.py (load_schema_5() criado em 05-01 — usado para decodificar e gerar golden_5)
  </read_first>
  <action>
    Em gen_synthetic_fixtures.py adicionar `build_maverick_frame(body, role=0x01)` que embrulha um body plano no envelope Maverick: `bytes([0xAA, 0x01]) + struct.pack("<H", len(body)) + body + bytes(4)` (trailer de 4 zeros sinteticos; o conteudo do trailer e HYPOTHESIS mas irrelevante porque parseFrame so usa o campo length). O body deve ter layout body-absolute: body[0]=role (0x00 cmd-in / 0x01 notify), body[1:4]=token (zeros sinteticos), body[4]=ptype, body[5]=seq, body[6]=cmd, body[7:]=payload.

    Adicionar uma funcao de geracao 5.0 (no mesmo ficheiro, ou variante invocada por gen_golden.py) que: (1) constroi frames 5.0 sinteticos para cada packet type presente no schema 5.0 usando build_maverick_frame(); (2) decodifica cada frame com o decoder Python usando load_schema_5() (de schema.py, criado em 05-01) para produzir os valores esperados; (3) escreve frames_5.json no formato array de objectos com campo hex (maverick-wrapped) em Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/; (4) escreve golden_5.json com a mesma estrutura que golden.json (4.0) em Resources/. Usar o path verificado Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/ — NAO o path Packages/WhoopProtocolTests/Resources/ que nao existe no repo.

    NAO remover nem regenerar os ficheiros 4.0 (frames.json, golden.json) — os testes 4.0 continuam a exercitar Framing/CRC/Reassembler. Adicionar os ficheiros 5.0 ao lado.
  </action>
  <verify>
    <automated>python3 -m venv /tmp/whoop_venv5 && /tmp/whoop_venv5/bin/pip install -q -e server/packages/whoop-protocol && /tmp/whoop_venv5/bin/python scripts/gen_golden.py && test -f Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/frames_5.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def build_maverick_frame' scripts/gen_synthetic_fixtures.py`
    - Ficheiro frames_5.json existe e e JSON valido (array de objectos com chave "hex")
    - Ficheiro golden_5.json existe e e JSON valido
    - Cada entrada de frames_5.json comeca por "aa01" (0xAA 0x01 — assinatura Maverick) quando descodificada de hex
    - frames.json e golden.json (4.0) permanecem inalterados (mesmos SHA que antes do plano)
  </acceptance_criteria>
  <done>build_maverick_frame() existe; frames_5.json e golden_5.json gerados nos Resources do test target; fixtures 4.0 intactas</done>
</task>

<task type="auto">
  <name>Task 2: Parity5Tests.swift + correccao de SchemaSyncTests (SWIFT-05, Pitfall 2/3)</name>
  <files>Packages/WhoopProtocol/Tests/WhoopProtocolTests/Parity5Tests.swift, Packages/WhoopProtocol/Tests/WhoopProtocolTests/SchemaSyncTests.swift</files>
  <read_first>
    - Packages/WhoopProtocol/Tests/WhoopProtocolTests/ParityTests.swift (padrao de teste de paridade 4.0; helper resourceURL via Bundle.module.url; loop sobre frames + comparacao com golden)
    - Packages/WhoopProtocol/Tests/WhoopProtocolTests/SchemaSyncTests.swift (testBundledSchemaMatchesCanonical linhas 14-25; testBundleModuleSchemaAlsoMatchesCanonical linhas 27-42; paths protocol/whoop_protocol.json e Resources/whoop_protocol.json)
    - Packages/WhoopProtocol/Sources/WhoopProtocol/Interpreter.swift (parseFrame apos strip Maverick — o que os fixtures 5.0 exercitam)
    - Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/frames_5.json (input gerado na Task 1)
  </read_first>
  <action>
    Criar Parity5Tests.swift seguindo o padrao de ParityTests.swift: carregar frames_5.json e golden_5.json via Bundle.module.url(forResource:"frames_5"/"golden_5", withExtension:"json"), iterar sobre cada frame, chamar parseFrame() (que faz strip Maverick interno), e XCTAssertEqual o resultado contra o golden 5.0 correspondente. Usar o helper resourceURL com XCTUnwrap e mensagem "missing test resource — run scripts/gen_golden.py". NAO duplicar logica do decoder no teste — apenas chamar a API publica.

    Em SchemaSyncTests.swift: em testBundledSchemaMatchesCanonical() alterar o canonical path de protocol/whoop_protocol.json para protocol/whoop_protocol_5.json e o bundled path de Resources/whoop_protocol.json para Resources/whoop_protocol_5.json. Em testBundleModuleSchemaAlsoMatchesCanonical() alterar o canonical path para protocol/whoop_protocol_5.json (o moduleURL ja vem de WhoopProtocolInfo.schemaResourceURL() corrigido em 05-01). Manter os XCTAssertEqual inalterados.

    IMPORTANTE: antes de correr os testes, re-sincronizar o schema bundled correndo scripts/sync-schema-5.sh, porque o bundled Resources/whoop_protocol_5.json pode estar desactualizado face a protocol/whoop_protocol_5.json (tamanhos diferentes observados no planeamento).
  </action>
  <verify>
    <automated>bash scripts/sync-schema-5.sh && cd Packages/WhoopProtocol && swift test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'frames_5' Packages/WhoopProtocol/Tests/WhoopProtocolTests/Parity5Tests.swift`
    - `grep -q 'whoop_protocol_5' Packages/WhoopProtocol/Tests/WhoopProtocolTests/SchemaSyncTests.swift`
    - `grep -q 'whoop_protocol.json' Packages/WhoopProtocol/Tests/WhoopProtocolTests/SchemaSyncTests.swift` nao encontra referencia 4.0 nos paths canonical/bundled (so 5.0)
    - `cd Packages/WhoopProtocol && swift test` sai com codigo 0 (todos os testes, 4.0 e 5.0, passam)
    - O teste de paridade 5.0 cobre pelo menos um frame de cada packet type presente em frames_5.json
  </acceptance_criteria>
  <done>Parity5Tests prova paridade Python-Swift sobre frames 5.0; SchemaSyncTests valida o schema 5.0; swift test passa inteiro</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures sinteticas → test runner | dados de teste sao gerados, nao capturados; sem PII real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | frames_5.json / golden_5.json | accept | Fixtures provably synthetic (build_maverick_frame com tokens/trailer a zeros); nenhuma captura real entra no repo publico (politica gen_golden.py ja estabelecida) |
| T-05-02-02 | Tampering | gate de paridade | mitigate | swift test e o gate; paridade byte-a-byte Python-Swift falha o build se o strip Maverick ou os offsets estiverem errados |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | pip install -e do package local ja existente no repo; zero packages externos novos (RESEARCH confirma) |
</threat_model>

<verification>
- `bash scripts/sync-schema-5.sh` sincroniza o schema canonico para o bundle
- `cd Packages/WhoopProtocol && swift test` passa (4.0 + 5.0)
- frames_5.json/golden_5.json existem e sao sinteticos
</verification>

<success_criteria>
- SWIFT-05: testes Swift passam com golden fixtures 5.0 e provam paridade byte-a-byte com o decoder Python
- SchemaSyncTests valida o schema 5.0 (nao o 4.0)
</success_criteria>

<output>
Create `.planning/phases/05-ios-app-server-port/05-02-SUMMARY.md` when done
</output>
