---
phase: 05-ios-app-server-port
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - server/db/init.sql
  - server/ingest/app/main.py
autonomous: true
requirements: [SRV-01, SRV-02, SRV-03, SRV-04, SRV-05]
must_haves:
  truths:
    - "init.sql adiciona device_generation a cada hypertable idempotentemente"
    - "POST /v1/ingest-decoded aceita device_generation opcional (default '5.0')"
    - "compute_day() corre apos cada ingest 5.0 (comportamento existente verificado)"
    - "GET /v1/daily, /v1/sleep, /v1/workouts retornam dados 5.0 ingeridos"
    - "docker compose up -d --build arranca o servidor com o init.sql actualizado"
  artifacts:
    - path: "server/db/init.sql"
      provides: "coluna device_generation nas hypertables"
      contains: "device_generation"
    - path: "server/ingest/app/main.py"
      provides: "campo device_generation no DecodedBatch"
      contains: "device_generation"
  key_links:
    - from: "POST /v1/ingest-decoded (DecodedBatch)"
      to: "store.upsert_streams + compute_day"
      via: "ingest_decoded handler"
      pattern: "device_generation"
    - from: "docker compose up -d --build"
      to: "init.sql"
      via: "postgres init / idempotent ALTER TABLE"
      pattern: "ADD COLUMN IF NOT EXISTS device_generation"
---

<objective>
Portar o servidor FastAPI + TimescaleDB para WHOOP 5.0: adicionar `device_generation` a cada hypertable via ALTER TABLE idempotente no init.sql (D-09, SRV-04), aceitar `device_generation` como campo opcional no DecodedBatch Pydantic (D-10, SRV-01), e verificar que o pipeline existente (compute_day apos ingest — SRV-02; read endpoints /v1/daily, /v1/sleep, /v1/workouts — SRV-03; docker compose — SRV-05) funciona com dados 5.0.

Purpose: O servidor opcional aceita os streams decodificados da app iOS 5.0 e classifica-os correctamente por geracao. As mudancas sao aditivas e backward-compatible (clientes sem o campo continuam a funcionar).
Output: init.sql + main.py actualizados; docker compose arranca; ingest-decoded aceita 5.0; read endpoints servem 5.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ios-app-server-port/05-CONTEXT.md
@.planning/phases/05-ios-app-server-port/05-PATTERNS.md
@server/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: device_generation no init.sql + DecodedBatch (D-09, D-10)</name>
  <files>server/db/init.sql, server/ingest/app/main.py</files>
  <read_first>
    - server/db/init.sql (padrao ALTER TABLE ADD COLUMN IF NOT EXISTS — linha 66 battery charging; linhas 145-152 exercise_sessions; linhas 193-197 daily_metrics; nomes de hypertable: hr_samples, rr_intervals, events, battery, spo2_samples, skin_temp_samples, resp_samples, gravity_samples)
    - server/ingest/app/main.py (DecodedBatch BaseModel linhas 107-109; padrao de campo opcional com default linha 83 decode_streams: bool = True; ingest_decoded handler linhas 143-171 que ja chama store.upsert_streams + compute_day)
  </read_first>
  <action>
    Em init.sql: adicionar, na seccao de migracoes idempotentes (apos os CREATE TABLE + create_hypertable, junto da linha 66/193 onde o padrao ja existe), oito declaracoes `ALTER TABLE <t> ADD COLUMN IF NOT EXISTS device_generation TEXT DEFAULT '5.0';` para os hypertables: hr_samples, rr_intervals, events, battery, spo2_samples, skin_temp_samples, resp_samples, gravity_samples. O DEFAULT '5.0' classifica linhas existentes como 5.0 (deployment 5.0-only). IF NOT EXISTS garante idempotencia em re-runs no startup.

    Em main.py: no DecodedBatch (linhas 107-109) adicionar o campo `device_generation: str | None = "5.0"`, seguindo o padrao de campo opcional com default (decode_streams: bool = True, linha 83). NAO alterar o handler ingest_decoded nem a logica de compute_day — payload["device_generation"] fica disponivel automaticamente via model_dump() se o store quiser persisti-lo. Backward compat: clientes sem o campo recebem o default '5.0'.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('server/ingest/app/main.py').read()); print('main.py OK')" && grep -c "ADD COLUMN IF NOT EXISTS device_generation" server/db/init.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADD COLUMN IF NOT EXISTS device_generation' server/db/init.sql` retorna 8 (uma por hypertable)
    - init.sql contem device_generation para cada um de: hr_samples, rr_intervals, events, battery, spo2_samples, skin_temp_samples, resp_samples, gravity_samples
    - Cada ALTER usa `TEXT DEFAULT '5.0'`
    - `grep -q "device_generation: str | None" server/ingest/app/main.py` (campo opcional no DecodedBatch)
    - `python3 -c "import ast; ast.parse(open('server/ingest/app/main.py').read())"` nao lanca SyntaxError
  </acceptance_criteria>
  <done>init.sql adiciona device_generation idempotente a 8 hypertables; DecodedBatch aceita device_generation opcional default '5.0'; ingest_decoded inalterado</done>
</task>

<task type="auto">
  <name>Task 2: arranque end-to-end + verificacao de ingest/compute/read 5.0 (SRV-01/02/03/05)</name>
  <files>server/db/init.sql, server/ingest/app/main.py</files>
  <read_first>
    - server/docker-compose.yml (servicos db + ingest; comando docker compose up -d --build)
    - server/ingest/app/main.py (ingest_decoded ja chama daily.compute_day apos upsert — SRV-02 ja satisfeito; read endpoints /v1/daily linha 233, /v1/sleep linha 243, /v1/workouts linha 297)
    - server/ingest/app/store.py (upsert_streams; persistencia dos streams)
  </read_first>
  <action>
    Arrancar o servidor com `docker compose up -d --build` a partir de server/ (SRV-05). Aguardar o healthcheck. Confirmar que o init.sql actualizado correu (a coluna device_generation existe nas 8 hypertables) consultando o information_schema via docker exec psql.

    Executar um POST /v1/ingest-decoded com um batch 5.0 minimo (incluindo device_generation: "5.0" e pelo menos um hr sample) usando o Bearer token de Secrets/env, e confirmar resposta 200 com upserted counts (SRV-01). Confirmar que compute_day correu (sem erro nos logs do ingest; SRV-02 — comportamento existente).

    Confirmar que GET /v1/daily, GET /v1/sleep e GET /v1/workouts retornam 200 e servem os dados 5.0 ingeridos (SRV-03). NOTA: os nomes reais dos endpoints sao /v1/daily, /v1/sleep, /v1/workouts (o ROADMAP refere /v1/daily-metrics e /v1/sleep-sessions de forma aspiracional; documentar esta correspondencia no SUMMARY — os endpoints existentes satisfazem SRV-03 sem renomear). NAO renomear endpoints (quebraria o cliente iOS Uploader.swift).

    Registar no SUMMARY a evidencia: output do psql confirmando a coluna, a resposta 200 do ingest, e as respostas dos tres read endpoints. Parar o stack com docker compose down no fim se apropriado.
  </action>
  <verify>
    <automated>cd server && docker compose up -d --build 2>&1 | tail -5 && docker compose ps 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose up -d --build` arranca db + ingest sem erro (SRV-05)
    - Query a information_schema.columns confirma device_generation em hr_samples (e nas restantes 7 hypertables)
    - POST /v1/ingest-decoded com device_generation '5.0' retorna HTTP 200 com chave "upserted" (SRV-01)
    - Logs do ingest mostram compute_day a correr apos o ingest (ou ausencia de erro de compute) (SRV-02)
    - GET /v1/daily, GET /v1/sleep, GET /v1/workouts retornam HTTP 200 (SRV-03)
    - Os nomes dos endpoints permanecem /v1/daily, /v1/sleep, /v1/workouts (nao renomeados)
  </acceptance_criteria>
  <done>Servidor arranca via docker compose; ingest-decoded 5.0 retorna 200; compute_day corre; read endpoints servem dados 5.0; correspondencia de nomes documentada no SUMMARY</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente iOS → POST /v1/ingest-decoded | input de rede nao confiavel; validado por Pydantic + Bearer auth |
| servico ingest → TimescaleDB | queries parametrizadas via psycopg (sem string interpolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | POST /v1/ingest-decoded | mitigate | require_auth (Bearer token) ja aplicado via Depends — endpoint nao aceita writes sem o token correcto |
| T-05-04-02 | Tampering | device_generation no payload | mitigate | Pydantic valida o tipo (str | None); o campo nao alimenta SQL dinamico — e persistido como valor parametrizado; sem injeccao possivel |
| T-05-04-03 | Tampering / SQL injection | init.sql ALTER TABLE | accept | DDL estatico sem input do utilizador; nomes de tabela hardcoded no ficheiro de schema; sem superficie de injeccao |
| T-05-04-04 | Information Disclosure | read endpoints /v1/daily etc | mitigate | Todos os read endpoints ja exigem require_auth (Bearer); sem mudanca a esta postura |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero packages novos; FastAPI/psycopg/Pydantic ja presentes (RESEARCH confirma) |
</threat_model>

<verification>
- `grep -c 'ADD COLUMN IF NOT EXISTS device_generation' server/db/init.sql` retorna 8
- DecodedBatch tem device_generation opcional
- docker compose up -d --build arranca; ingest 200; read endpoints 200
</verification>

<success_criteria>
- SRV-01: POST /v1/ingest-decoded aceita 5.0 streams com device_generation
- SRV-02: compute_day() corre apos cada ingest 5.0 (verificado)
- SRV-03: /v1/daily, /v1/sleep, /v1/workouts retornam dados 5.0
- SRV-04: device_generation adicionado as hypertables via init.sql idempotente
- SRV-05: servidor corre via docker compose up -d --build
</success_criteria>

<output>
Create `.planning/phases/05-ios-app-server-port/05-04-SUMMARY.md` when done
</output>
