---
phase: 05-ios-app-server-port
plan: 05
type: execute
wave: 2
depends_on: ["05-01", "05-03"]
files_modified:
  - ios/OpenWhoop/BLE/BLEManager.swift
  - ios/OpenWhoop/BLE/Commands.swift
autonomous: true
requirements: [IOS-01, IOS-07, IOS-08]
must_haves:
  truths:
    - "BLEManager usa os UUIDs FD4B0001-CCE1-... (5.0) em vez dos 61080001-... (4.0)"
    - "O enum WhoopCommand contem os 10 comandos VERIFIED e exclui HYPOTHESIS e destrutivos"
    - "State restoration (CBCentralManagerOptionRestoreIdentifierKey/willRestoreState) preservado"
    - "Offline mode (AppConfig.uploaderConfig() retorna nil em placeholders) preservado"
  artifacts:
    - path: "ios/OpenWhoop/BLE/BLEManager.swift"
      provides: "UUIDs 5.0 wired"
      contains: "FD4B0001"
    - path: "ios/OpenWhoop/BLE/Commands.swift"
      provides: "enum revisto contra VERIFIED 5.0"
  key_links:
    - from: "BLEManager scan-by-service"
      to: "WHOOP 5.0 strap"
      via: "CBUUID customService FD4B0001"
      pattern: "FD4B0001"
    - from: "BLEManager.restoreID"
      to: "CBCentralManagerOptionRestoreIdentifierKey"
      via: "centralManager init options (inalterado)"
      pattern: "CBCentralManagerOptionRestoreIdentifierKey"
---

<objective>
Ligar a camada BLE iOS ao WHOOP 5.0: substituir em-place os 5 UUIDs `61080001-...` por `FD4B0001-CCE1-4033-93CE-002D5875F58A` no BLEManager (D-04, IOS-01), rever o enum WhoopCommand contra os 10 comandos VERIFIED da Fase 4 excluindo HYPOTHESIS e destrutivos (D-05), e validar que o offline mode (IOS-07) e a state preservation (IOS-08) continuam intactos apos a substituicao.

Purpose: A camada CoreBluetooth e a unica que conhece os UUIDs fisicos. Sem a substituicao, a app procura um servico inexistente neste device (a legacy 61080001 esta AUSENTE — FINDINGS_5.md §2) e nunca liga.
Output: BLEManager + Commands wired para 5.0; build iOS passa; state restoration e offline preservados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ios-app-server-port/05-CONTEXT.md
@.planning/phases/05-ios-app-server-port/05-PATTERNS.md
@.planning/phases/05-ios-app-server-port/05-01-SUMMARY.md
@FINDINGS_5.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: substituir UUIDs 5.0 e "WHOOP 4.0" no BLEManager (D-04, IOS-01)</name>
  <files>ios/OpenWhoop/BLE/BLEManager.swift</files>
  <read_first>
    - ios/OpenWhoop/BLE/BLEManager.swift (bloco de UUIDs linhas 13-17; comentario header linha 12 "from FINDINGS.md"; doc-comment linha 6 "WHOOP 4.0"; restoreID linha 23 — NAO codifica UUIDs; upsertDevice name "WHOOP 4.0" linha 131; log "61080002" linha 695; willRestoreState linha 632; options CBCentralManagerOptionRestoreIdentifierKey linhas 119/159)
    - FINDINGS_5.md (§1 UUIDs VERIFIED: FD4B0001..FD4B0005-CCE1-4033-93CE-002D5875F58A)
  </read_first>
  <action>
    Substituir em-place as 5 strings de UUID nas linhas 13-17:
    - customService: 61080001-8d6d-82b8-614a-1c8cb0f8dcc6 -> FD4B0001-CCE1-4033-93CE-002D5875F58A
    - cmdWriteChar: 61080002-... -> FD4B0002-CCE1-4033-93CE-002D5875F58A
    - cmdNotifyChar: 61080003-... -> FD4B0003-CCE1-4033-93CE-002D5875F58A
    - eventNotifyChar: 61080004-... -> FD4B0004-CCE1-4033-93CE-002D5875F58A
    - dataNotifyChar: 61080005-... -> FD4B0005-CCE1-4033-93CE-002D5875F58A
    As characteristics standard (heartRateService/heartRateChar/batteryService/batteryChar) ficam INALTERADAS (UUIDs standard Bluetooth).

    Actualizar o comentario header (linha 12) de "from FINDINGS.md" para "from FINDINGS_5.md §1 — WHOOP 5.0". Actualizar a string "WHOOP 4.0" para "WHOOP 5.0" em todas as ocorrencias: doc-comment linha 6, upsertDevice(name:) linha 131, e o log da linha 695 ("...write GET_BATTERY_LEVEL to 61080002" -> "...to FD4B0002"). Usar grep para localizar todas as ocorrencias de "WHOOP 4.0" e "61080002" antes de editar.

    NAO alterar restoreID (linha 23) — "com.openwhoop.ble.central" nao codifica UUIDs e e necessario para state restoration. NAO alterar o mecanismo de bonding (confirmed-write trick funciona em iOS — FINDINGS_5.md §3). NAO adicionar branch dual-UUID (legacy ausente).
  </action>
  <verify>
    <automated>grep -c "FD4B000[1-5]" ios/OpenWhoop/BLE/BLEManager.swift; grep -c "61080001\|WHOOP 4.0" ios/OpenWhoop/BLE/BLEManager.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'FD4B000[1-5]-CCE1-4033-93CE-002D5875F58A' ios/OpenWhoop/BLE/BLEManager.swift` retorna 5
    - `grep -c '61080001' ios/OpenWhoop/BLE/BLEManager.swift` retorna 0 (customService 4.0 removido)
    - `grep -c 'WHOOP 4.0' ios/OpenWhoop/BLE/BLEManager.swift` retorna 0
    - `grep -q 'WHOOP 5.0' ios/OpenWhoop/BLE/BLEManager.swift`
    - `grep -q 'restoreID = "com.openwhoop.ble.central"' ios/OpenWhoop/BLE/BLEManager.swift` (restoreID intacto — IOS-08)
    - `grep -q 'CBCentralManagerOptionRestoreIdentifierKey' ios/OpenWhoop/BLE/BLEManager.swift` (state preservation intacto)
    - heartRateService/batteryService permanecem com os UUIDs standard (inalterados)
  </acceptance_criteria>
  <done>5 UUIDs custom substituidos por FD4B; strings "WHOOP 4.0" e log 61080002 actualizados; restoreID e state preservation intactos</done>
</task>

<task type="auto">
  <name>Task 2: rever enum WhoopCommand contra VERIFIED 5.0 (D-05)</name>
  <files>ios/OpenWhoop/BLE/Commands.swift</files>
  <read_first>
    - ios/OpenWhoop/BLE/Commands.swift (enum WhoopCommand cases linhas 11-58; commandType=35 linha 107; func frame() linhas 116-128 — formato 4.0 CRC8+CRC32)
    - FINDINGS_5.md (§8 Decode — 10 comandos VERIFIED: TOGGLE_REALTIME_HR=3, SET_CLOCK=10, GET_CLOCK=11, SEND_HISTORICAL_DATA=22, HISTORICAL_DATA_RESULT=23, GET_BATTERY_LEVEL=26, GET_DATA_RANGE=34, GET_HELLO_HARVARD=35, EXIT_HIGH_FREQ_SYNC=97, GET_EXTENDED_BATTERY_INFO=98)
    - .planning/phases/05-ios-app-server-port/05-PATTERNS.md (tabela dos 10 VERIFIED e a lista de HYPOTHESIS a excluir: reportVersionInfo=7, startRawData=81, stopRawData=82)
  </read_first>
  <action>
    Rever o enum WhoopCommand: confirmar que os 10 comandos VERIFIED da Fase 4 estao presentes (toggleRealtimeHR=3, setClock=10, getClock=11, sendHistoricalData=22, historicalDataResult=23, getBatteryLevel=26, getDataRange=34, getHelloHarvard=35, exitHighFreqSync=97, getExtendedBatteryInfo=98). Remover do enum os cases HYPOTHESIS sem confirmacao 5.0: reportVersionInfo=7, startRawData=81, stopRawData=82. Remover comandos destrutivos/nao-verificados que nao foram observados nas captures: getAdvertisingNameHarvard=76, enterHighFreqSync=96, toggleIMUMode=106, enableOpticalData=107, runHapticsPattern=79, stopHaptics=122, sendR10R11Realtime=63, setAlarmTime=66, getAlarmTime=67, runAlarm=68, disableAlarm=69. Criterio de inclusao (D-05): apenas comandos observados nas captures VERIFIED. Antes de remover qualquer case, verificar com grep que nao e referenciado noutro ficheiro iOS; se for referenciado, manter o case mas comentar o estado HYPOTHESIS — NAO quebrar compilacao por remocao de um case usado.

    Actualizar o comentario do char UUID na docstring de frame() (linha 112) se referir o UUID 4.0. NAO alterar func frame() em si — mantem formato 4.0 (CRC8+CRC32); o teste no iPhone (plano 05-06) decide se e necessario Maverick-wrapping nos writes (Open Question #1 / Pitfall 4). Documentar no SUMMARY que frame() permanece 4.0-format pendente de validacao no dispositivo.
  </action>
  <verify>
    <automated>cd ios && grep -c "case .* = " OpenWhoop/BLE/Commands.swift</automated>
  </verify>
  <acceptance_criteria>
    - Os 10 cases VERIFIED estao presentes em Commands.swift (toggleRealtimeHR, setClock, getClock, sendHistoricalData, historicalDataResult, getBatteryLevel, getDataRange, getHelloHarvard, exitHighFreqSync, getExtendedBatteryInfo)
    - `grep -q 'startRawData' ios/OpenWhoop/BLE/Commands.swift` falha (HYPOTHESIS removido) OU o case foi conservado com comentario HYPOTHESIS porque e referenciado noutro lado (decisao documentada no SUMMARY)
    - Nenhum comando destrutivo nao-verificado permanece sem anotacao HYPOTHESIS/justificacao
    - func frame() permanece em formato 4.0 (CRC8+CRC32) — nao alterado
    - O projecto iOS compila apos a revisao do enum (verificado no plano 05-06 com xcodebuild; aqui confirmar ausencia de referencias orfas via grep)
  </acceptance_criteria>
  <done>Enum revisto: 10 VERIFIED presentes; HYPOTHESIS/destrutivos removidos ou anotados; frame() 4.0-format preservado com nota de validacao pendente</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strap BLE ↔ BLEManager | pareamento BLE; risco de MITM no bonding |
| WhoopCommand.frame() → strap | comandos escritos no strap; comandos destrutivos sao um risco de safety |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Spoofing / MITM | BLE bonding (confirmed-write trick) | accept | O bonding usa o pairing nativo iOS (encriptado, LTK); o confirmed-write trick e o mesmo mecanismo 4.0 validado; sem novo vector — risco residual de MITM aceite (atacante precisa de proximidade fisica + janela de pairing) |
| T-05-05-02 | Elevation/Damage | comandos destrutivos no enum | mitigate | D-05 exclui DFU/REBOOT/POWER_CYCLE/FORCE_TRIM e comandos nao-verificados do enum; o enum so expoe comandos VERIFIED nao-destrutivos, reduzindo a superficie de envio acidental ao strap |
| T-05-05-03 | Tampering | UUIDs hardcoded | accept | UUIDs sao constantes do protocolo (VERIFIED via nRF Connect + 5028 frames); sem input externo |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero packages novos; CoreBluetooth e sistema (RESEARCH confirma) |
</threat_model>

<verification>
- `grep -c 'FD4B000[1-5]' ios/OpenWhoop/BLE/BLEManager.swift` retorna 5
- restoreID e CBCentralManagerOptionRestoreIdentifierKey intactos (IOS-08)
- AppConfig.uploaderConfig() inalterado (IOS-07 — offline preservado)
- 10 comandos VERIFIED presentes; destrutivos excluidos
</verification>

<success_criteria>
- IOS-01: BLEManager usa UUIDs 5.0 (bonding validado fisicamente em 05-06)
- IOS-07: offline mode preservado (uploaderConfig retorna nil em placeholders — nao tocado)
- IOS-08: state preservation preservada (restoreID + willRestoreState intactos; reconnect validado em 05-06)
</success_criteria>

<output>
Create `.planning/phases/05-ios-app-server-port/05-05-SUMMARY.md` when done
</output>
