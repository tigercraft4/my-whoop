---
phase: 01-capture-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - re/capture/wireshark.md
  - re/capture/jadx.md
autonomous: true
requirements: [TOOL-03, TOOL-04]
must_haves:
  truths:
    - "A developer can follow re/capture/wireshark.md to open a .pklg or .btsnoop, filter to the ATT/GATT layer, and locate the WHOOP custom service UUID"
    - "A developer can follow re/capture/jadx.md to adb-pull their own installed WHOOP APK and navigate to the packet-type/command enum definitions in JADX-GUI"
    - "jadx.md states the locked legal recording rule: only enum names + numeric values are recorded; decompiled source is never committed"
  artifacts:
    - path: "re/capture/wireshark.md"
      provides: "TOOL-04 runbook: open pklg/btsnoop, filter ATT/GATT, find WHOOP service, redact for evidence"
      contains: "btatt"
    - path: "re/capture/jadx.md"
      provides: "TOOL-03 runbook: adb pull APK (split-aware), JADX-GUI enum navigation, r52 cross-ref, legal recording rule"
      contains: "pm path"
  key_links:
    - from: "re/capture/wireshark.md"
      to: "re/capture/samples/"
      via: "tshark -r reads raw captures from the gitignored samples dir"
      pattern: "samples/"
    - from: "re/capture/jadx.md"
      to: "DISCLAIMER.md §2"
      via: "legal recording rule — enum names+values only, no committed source"
      pattern: "enum|never commit|DISCLAIMER"
---

<objective>
Author the two host-side analysis runbooks: `re/capture/wireshark.md` (TOOL-04 — open `.pklg`/
`.btsnoop`, filter to ATT/GATT, find the WHOOP custom service, and redact frames for committed
evidence) and `re/capture/jadx.md` (TOOL-03 — `adb pull` the user's own WHOOP APK, navigate to the
packet-type/command enums in JADX-GUI, cross-reference whoop-vault r52, under the locked legal
recording rule). These two tools run entirely on the Mac host and their commands come directly from
RESEARCH §Code Examples, so the runbooks can be written autonomously without device-side checkpoints.

Purpose: Give later phases reproducible analysis procedures for the captures produced in Plan 03,
and satisfy the "developer can read the official enums" success criterion (TOOL-03) and the
"developer can filter captures to ATT/GATT" criterion (TOOL-04) — both with the legal guardrails of
D-04 / DISCLAIMER §2.

Output: `re/capture/wireshark.md`, `re/capture/jadx.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-foundation/01-CONTEXT.md
@.planning/phases/01-capture-foundation/01-RESEARCH.md
@.planning/phases/01-capture-foundation/01-PATTERNS.md
@FINDINGS.md
@DISCLAIMER.md
@re/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write re/capture/wireshark.md (TOOL-04 runbook)</name>
  <files>re/capture/wireshark.md</files>
  <read_first>
    - .planning/phases/01-capture-foundation/01-RESEARCH.md (Code Examples lines 295-316: `tshark -r ... -Y btatt` filter/count snippets, the `btl2cap.cid == 0x0004` ATT-channel filter, the fd4b0001/61080001/8d6d UUID grep, and the SHA256 + redacted hex-excerpt commands; Pitfall 3 warning signs for empty traces; Assumption A6 that filter syntax must be confirmed against installed 4.6.6)
    - .planning/phases/01-capture-foundation/01-PATTERNS.md (runbook doc convention: dated header, prerequisites, numbered steps, "how to verify it worked" non-empty-trace check; capture metadata sidecar shape)
    - .planning/phases/01-capture-foundation/01-CONTEXT.md (D-02 evidence policy: redacted hex + SHA256 + metadata committed, raw stays in gitignored samples/; D-01 file location)
    - FINDINGS.md (4.0 framing `[0xAA][len u16 LE][crc8][type][seq][cmd][payload][crc32 LE]` — what "WHOOP custom service traffic" looks like in the ATT payload, so the runbook can tell the reader what to look for)
    - DISCLAIMER.md §2 (the redaction step must scrub BD_ADDR / SMP key material before any git add)
  </read_first>
  <action>
    Create `re/capture/wireshark.md` as an operational runbook following the repo doc convention:
    dated/versioned header, a "Prerequisites" section (Wireshark/tshark installed via Brewfile +
    `check-tools.sh` green; a raw capture under `re/capture/samples/`), numbered reproducible steps,
    and a "How to verify it worked" non-empty-trace check.

    Steps must cover, using the exact commands from RESEARCH §Code Examples: (1) open a `.pklg` or
    `.btsnoop` in the Wireshark GUI (File > Open); (2) apply the ATT/GATT display filter `btatt` and the
    ATT fixed-channel filter `btl2cap.cid == 0x0004`; (3) headless verification with
    `tshark -r samples/<session>.pklg -Y btatt -T fields -e btatt.handle -e btatt.value | head` to
    PROVE non-empty WHOOP traffic (not just "tool launches"); (4) locate the WHOOP custom service UUID
    by grepping a verbose dissect for `fd4b0001` (new 5.0 prefix) or legacy `61080001` or the `8d6d`
    fragment — note this is what Phase 2 will confirm, but the capture should make the UUID visible;
    (5) the evidence step (D-02): `sha256sum` the raw capture into `re/capture/evidence/<session>.sha256`,
    export a small redacted hex slice with `tshark ... -Y btatt -x | sed -n '1,40p'` into
    `re/capture/evidence/<session>.hex`, and MANUALLY scrub any BD_ADDR / SMP key material before
    `git add` (DISCLAIMER §2). State explicitly that raw `.pklg`/`.btsnoop` stay in the gitignored
    `samples/` dir and only redacted hex + SHA256 + the metadata sidecar are committed under `evidence/`.
    Tag the `btatt` / `btl2cap.cid==0x0004` filter syntax as "confirm against installed Wireshark 4.6.6"
    (Assumption A6).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop && test -f re/capture/wireshark.md && grep -q 'btatt' re/capture/wireshark.md && grep -q 'btl2cap.cid' re/capture/wireshark.md && grep -Eq 'fd4b0001|61080001' re/capture/wireshark.md && grep -q 'sha256' re/capture/wireshark.md && grep -qi 'redact' re/capture/wireshark.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `re/capture/wireshark.md` contains the display filter `btatt` and `btl2cap.cid == 0x0004`
    - File contains the headless `tshark -r ... -Y btatt` non-empty-trace verification command
    - File references the WHOOP custom service UUID lookup (`fd4b0001` and/or `61080001`)
    - File contains a `sha256` evidence-checksum step writing to `re/capture/evidence/`
    - File contains a redaction step that scrubs BD_ADDR / SMP material before commit (DISCLAIMER §2)
    - File states raw captures stay gitignored under `samples/`; only redacted hex + SHA256 + metadata are committed
    - Behavior: a developer following the steps with a sample capture sees non-zero `btatt` packet output
  </acceptance_criteria>
  <done>wireshark.md is a complete TOOL-04 runbook: open, filter ATT/GATT, prove non-empty WHOOP traffic, and produce redacted committed evidence under the D-02 policy.</done>
</task>

<task type="auto">
  <name>Task 2: Write re/capture/jadx.md (TOOL-03 runbook)</name>
  <files>re/capture/jadx.md</files>
  <read_first>
    - .planning/phases/01-capture-foundation/01-RESEARCH.md (Code Examples lines 318-326: `adb shell pm list packages | grep whoop`, `adb shell pm path <pkg>` multi-split pull loop, `jadx-gui samples/apk/base.apk`; Pitfall 4 split-APK handling; Pitfall 5 JADX needs a JRE / openjdk keg-only; Assumption A4 package id `com.whoop.android` must be confirmed live)
    - .planning/phases/01-capture-foundation/01-CONTEXT.md (D-04 locked: adb pull from user's own copy, APKMirror fallback only if blocked; legal recording rule — enum names + numeric values only, never commit decompiled source; decompiled APK + JADX project output gitignored)
    - .planning/phases/01-capture-foundation/01-PATTERNS.md (runbook doc convention; "raw under samples/ gitignored" note)
    - DISCLAIMER.md §2 (no decompiled/disassembled WHOOP source, no APKs committed — governs the recording rule and the gitignore reliance)
    - .gitignore (confirm re/capture/samples/apk/ is covered by the Plan 01 ignore rules so pulled APKs stay local)
  </read_first>
  <action>
    Create `re/capture/jadx.md` as an operational runbook with the repo doc convention (dated header,
    Prerequisites, numbered steps, verify-it-worked). Prerequisites: JADX + adb installed via Brewfile,
    `check-tools.sh` green, openjdk linked (note the keg-only `JAVA_HOME`/`PATH` caveat brew prints).

    Steps using the exact RESEARCH commands: (1) confirm the WHOOP package id live with
    `adb shell pm list packages | grep -i whoop` (do NOT hardcode `com.whoop.android` — Assumption A4,
    confirm on device); (2) list all split paths with `adb shell pm path <pkg>` (Pitfall 4: modern Play
    installs are split by ABI/density/language — may return multiple paths); (3) pull ALL returned paths
    into `re/capture/samples/apk/` with the loop from RESEARCH; (4) open `base.apk` in JADX-GUI
    (`jadx-gui re/capture/samples/apk/base.apk`) and navigate to the Maverick / packet-type / command
    enum definitions, cross-referencing the external whoop-vault r52 map (cite/link only — never vendor);
    (5) record findings: the LOCKED legal recording rule — record ONLY enum names + numeric values in
    your notes, NEVER commit decompiled source or any proprietary material; the pulled APK and JADX
    project output are gitignored under `samples/apk/` (DISCLAIMER §2 / D-04). Document the APKMirror
    fallback as available ONLY if `adb pull` is blocked, under the same recording rule.

    Add a JRE troubleshooting note (Pitfall 5): if `jadx-gui` bounces on launch with a Java error, the
    openjdk keg-only symlink / `JAVA_HOME` is not set — point to the caveat brew printed and
    `check-tools.sh`'s java check.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop && test -f re/capture/jadx.md && grep -q 'pm path' re/capture/jadx.md && grep -qi 'pm list packages' re/capture/jadx.md && grep -qi 'split' re/capture/jadx.md && grep -qi 'r52' re/capture/jadx.md && grep -Eqi 'never commit|enum names' re/capture/jadx.md && grep -qi 'apkmirror' re/capture/jadx.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `re/capture/jadx.md` contains `adb shell pm list packages | grep` to confirm the package id live (not hardcoded)
    - File contains `adb shell pm path` and a multi-split pull step (Pitfall 4)
    - File pulls APKs into `re/capture/samples/apk/` (covered by Plan 01 gitignore)
    - File contains the locked legal recording rule: enum names + numeric values only, never commit decompiled source (DISCLAIMER §2 / D-04)
    - File cross-references the whoop-vault r52 map (cite/link only)
    - File documents APKMirror as a fallback only if adb pull is blocked
    - File contains a JRE/openjdk troubleshooting note (Pitfall 5)
    - Behavior: a developer following the steps can open base.apk in JADX-GUI and reach the packet-type enum definitions
  </acceptance_criteria>
  <done>jadx.md is a complete TOOL-03 runbook: confirm package, pull all splits to gitignored samples/apk/, navigate enums in JADX-GUI, cross-ref r52, under the locked legal recording rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw capture / pulled APK → git repo | Operators following the runbook could accidentally commit proprietary or identifying material |
| WHOOP APK (proprietary) → repo notes | Only uncopyrightable enum facts may cross into committed notes; source must not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | Redacted hex evidence committed from wireshark.md | mitigate | Runbook mandates scrubbing BD_ADDR / SMP key material before `git add` (DISCLAIMER §2); only a small redacted hex slice + SHA256 are committed |
| T-01-06 | Repudiation / Legal | Decompiled WHOOP source via jadx.md | mitigate | Locked recording rule documented in the runbook: enum names + numeric values only; pulled APK + JADX output stay in gitignored `samples/apk/` (DISCLAIMER §2 / D-04) |
| T-01-07 | Information Disclosure | UUID / device identifiers in committed evidence | accept | Custom service UUID is uncopyrightable protocol fact (intended to be visible); device-specific identifiers are scrubbed per the redaction step |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No language-package installs in this plan; tools provided by Plan 01 Brewfile (all brew-verified in RESEARCH audit) |
</threat_model>

<verification>
1. Open a real `.pklg`/`.btsnoop` per wireshark.md and confirm non-zero `btatt` packet count via the headless `tshark` command.
2. Follow jadx.md to confirm the package id and reach the packet-type enum definitions in JADX-GUI.
3. Confirm pulled APKs land under `re/capture/samples/apk/` and `git status` shows them ignored.
4. Confirm only redacted hex + SHA256 (no raw binaries, no decompiled source) are stage-able for commit.
</verification>

<success_criteria>
- wireshark.md lets a developer open both capture formats, filter ATT/GATT, find the WHOOP service, and produce redacted evidence (TOOL-04)
- jadx.md lets a developer adb-pull their own APK and navigate the packet-type enums under the locked legal recording rule (TOOL-03)
- Both runbooks enforce D-02 / D-04 / DISCLAIMER §2 (raw + proprietary stay local; only redacted facts committed)
</success_criteria>

<output>
Create `.planning/phases/01-capture-foundation/01-02-SUMMARY.md` when done.
</output>
