---
phase: 01-capture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Brewfile
  - scripts/check-tools.sh
  - .gitignore
  - re/capture/samples/.gitkeep
  - re/capture/samples/README.md
  - re/capture/evidence/.gitkeep
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04]
must_haves:
  truths:
    - "Running `brew bundle --file=Brewfile` installs the passive-capture toolchain (Wireshark CLI+GUI, JADX, adb, libimobiledevice, a JRE)"
    - "Running `bash scripts/check-tools.sh` exits 0 when all brew tools meet version floors and exits non-zero when any is missing/too old"
    - "check-tools.sh WARNs (does not FAIL) when PacketLogger.app / mobileconfig are absent — they are the irreducible manual steps"
    - "Raw captures and decompiled APK output placed under re/capture/samples/ are gitignored; the directory itself stays tracked via .gitkeep"
  artifacts:
    - path: "Brewfile"
      provides: "Declarative version-floored toolchain manifest"
      contains: "wireshark-app"
    - path: "scripts/check-tools.sh"
      provides: "Version-asserting toolchain verifier"
      contains: "set -euo pipefail"
    - path: ".gitignore"
      provides: "Raw-capture + decompiled-APK ignore rules"
      contains: "re/capture/samples/"
    - path: "re/capture/samples/.gitkeep"
      provides: "Tracked-but-empty samples dir for gitignored raw captures"
  key_links:
    - from: "Brewfile"
      to: "scripts/check-tools.sh"
      via: "brew bundle installs tools, check-tools.sh asserts their versions"
      pattern: "tshark|jadx|adb"
    - from: ".gitignore"
      to: "re/capture/samples/"
      via: "ignore rule with !gitkeep negation"
      pattern: "re/capture/samples"
---

<objective>
Build the version-pinned passive-capture toolchain foundation: a `Brewfile` that installs every
brew-installable RE tool, a `scripts/check-tools.sh` that asserts version floors and presence,
and the `.gitignore` + directory scaffolding that enforces the "raw stays local, redacted evidence
committed" policy (D-02). This is the autonomous infrastructure layer the runbook + capture plans
(02, 03) depend on.

Purpose: Make the toolchain reproducible and verifiable (D-03 maximal automation) so any developer
can `brew bundle` + `check-tools.sh` and know their machine is capture-ready, and so raw captures
can never be accidentally committed (D-02).

Output: `Brewfile`, `scripts/check-tools.sh`, extended `.gitignore`, and the
`re/capture/samples/` + `re/capture/evidence/` directory scaffolding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-foundation/01-CONTEXT.md
@.planning/phases/01-capture-foundation/01-RESEARCH.md
@.planning/phases/01-capture-foundation/01-PATTERNS.md
@scripts/sync-schema.sh
@.gitignore
@re/device_local.example.py
@DISCLAIMER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Brewfile, gitignore rules, and capture directory scaffolding</name>
  <files>Brewfile, .gitignore, re/capture/samples/.gitkeep, re/capture/samples/README.md, re/capture/evidence/.gitkeep</files>
  <read_first>
    - .planning/phases/01-capture-foundation/01-RESEARCH.md (Standard Stack table + Brewfile sketch lines 109-124; the `wireshark-app` cask-rename gotcha; openjdk keg-only note)
    - .planning/phases/01-capture-foundation/01-PATTERNS.md (Brewfile + .gitignore + local-artifact pattern sections)
    - .gitignore (existing ignore rules + annotation style to extend in place — note `apk/` at line 26, `re/device_local.py` at line 57, `!`-negation idiom at lines 13/33)
    - re/device_local.example.py (committed-template-beside-gitignored-real convention to mirror in samples/README.md)
    - DISCLAIMER.md §2 (no proprietary binaries/APKs/decompiled source committed — governs the APK ignore rule)
  </read_first>
  <action>
    Create `Brewfile` at repo root listing exactly these entries, each with a trailing `#` purpose
    comment (mirror the repo's self-documenting config style): `cask "wireshark-app"` (GUI — NOT
    `cask "wireshark"`, which is the wrong post-rename token), `brew "wireshark"` (CLI tshark/editcap),
    `brew "jadx"` (provides jadx + jadx-gui), `cask "android-platform-tools"` (adb/fastboot),
    `brew "libimobiledevice"` (ideviceinfo/idevice_id for firmware/UDID metadata), `brew "openjdk"`
    (JRE that JADX requires — note keg-only in comment), and `brew "blueutil"` (optional Mac BT CLI).
    Per D-03 implement these as the brew-installable set; per RESEARCH, the GUI token MUST be
    `wireshark-app`.

    Extend the EXISTING `.gitignore` in place (do not create a new ignore file). Add a commented
    block per the repo's "ignore raw, keep redacted, say why" idiom (D-02): ignore `re/capture/samples/*`
    with a `!re/capture/samples/.gitkeep` negation, and a comment stating raw `.pklg`/`.btsnoop` and
    decompiled APK output stay LOCAL (may contain device IDs / SMP bonding material; DISCLAIMER §2) while
    redacted hex + SHA256 + metadata under `re/capture/evidence/` ARE committed. The decompiled APK
    lands under `re/capture/samples/apk/` and is already covered by the `samples/*` rule — add an explicit
    `re/capture/samples/apk/` line with a DISCLAIMER §2 / D-04 comment for clarity.

    Create `re/capture/samples/.gitkeep` (empty) and `re/capture/evidence/.gitkeep` (empty) so both
    dirs stay tracked while contents are managed by the ignore rules. Create
    `re/capture/samples/README.md` carrying a header comment mirroring `re/device_local.example.py`:
    state that raw captures + decompiled APK live here, are gitignored, must never be committed, and
    point to `re/capture/README.md` for the redaction workflow.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop && grep -q 'cask "wireshark-app"' Brewfile && grep -q 'brew "wireshark"' Brewfile && grep -q 'brew "jadx"' Brewfile && grep -q 'android-platform-tools' Brewfile && grep -q 'libimobiledevice' Brewfile && grep -q 'openjdk' Brewfile && grep -q 're/capture/samples/' .gitignore && grep -q '!re/capture/samples/.gitkeep' .gitignore && test -f re/capture/samples/.gitkeep && test -f re/capture/evidence/.gitkeep && test -f re/capture/samples/README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `Brewfile` contains `cask "wireshark-app"` (NOT `cask "wireshark"`)
    - `Brewfile` contains `brew "wireshark"`, `brew "jadx"`, `cask "android-platform-tools"`, `brew "libimobiledevice"`, `brew "openjdk"`
    - Every `Brewfile` line has a trailing `#` purpose comment
    - `.gitignore` contains `re/capture/samples/*` and `!re/capture/samples/.gitkeep`
    - `.gitignore` contains an explicit `re/capture/samples/apk/` rule with a DISCLAIMER §2 / D-04 comment
    - `git check-ignore re/capture/samples/test.pklg` returns the path (proves a placed raw capture would be ignored)
    - `git check-ignore re/capture/samples/.gitkeep` returns nothing (proves the gitkeep is tracked)
    - `re/capture/samples/README.md` header states raw captures are gitignored and never committed
  </acceptance_criteria>
  <done>Brewfile, .gitignore additions, and capture dir scaffolding exist; a stray `.pklg` under samples/ is git-ignored while `.gitkeep` is tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/check-tools.sh version-asserting verifier</name>
  <files>scripts/check-tools.sh</files>
  <read_first>
    - scripts/sync-schema.sh (canonical bash idiom to copy verbatim: `#!/usr/bin/env bash` shebang, one-line `#` purpose comment, `set -euo pipefail`, `ROOT="$(cd "$(dirname "$0")/.." && pwd)"`, and the present→ok / absent→skip graceful-degradation shape at lines 12-17)
    - .planning/phases/01-capture-foundation/01-RESEARCH.md (Pattern 1 version-floor assertion lines 202-225; openjdk keg-only / java-runtime gap note; PacketLogger WARN-not-FAIL rule)
    - .planning/phases/01-capture-foundation/01-PATTERNS.md (check-tools.sh pattern assignment — exit-code contract, set -euo pipefail guard for `command -v` so a missing tool does not abort before later checks)
  </read_first>
  <action>
    Create `scripts/check-tools.sh` opening with the exact repo idiom from `sync-schema.sh`:
    `#!/usr/bin/env bash`, a one-line `#` purpose comment ("Verify the passive-capture toolchain. Run
    from anywhere."), `set -euo pipefail`, and `ROOT="$(cd "$(dirname "$0")/.." && pwd)"`.

    Implement an `assert_min name actual min` function using the `sort -V` semver-floor compare from
    RESEARCH Pattern 1: prints `ok` when actual >= min, prints `FAIL` and sets a `fail=1` accumulator
    otherwise. Per D-03 use version FLOORS (not exact equals) because brew delivers Wireshark 4.6.6 /
    JADX 1.5.5 today while D-03 named 4.4.x / 1.5.1 — assert `wireshark >= 4.4.0`, `jadx >= 1.5.1`,
    `adb >= 35.0.0`. Extract versions exactly as in RESEARCH: `tshark --version` first line,
    `jadx --version`, `adb --version`. Add a top-of-script comment recording the floor-vs-exact-pin
    rationale (4.6.x dissects ATT/GATT identically to 4.4.x — Assumption A7).

    Presence-check `libimobiledevice` via `command -v ideviceinfo` and the Java runtime via
    `java -version` (RESEARCH: the local `java` is a stub that reports "Unable to locate a Java
    Runtime" — this MUST FAIL until openjdk is installed/linked). Guard every `command -v` with the
    `&& ok || { FAIL; fail=1; }` shape carefully so `set -euo pipefail` does not abort the script before
    later checks run.

    Presence-check the two irreducible manual tools as WARN ONLY (must NOT touch `fail`):
    `/Applications/PacketLogger.app` OR `$HOME/Applications/PacketLogger.app` (absent → WARN with the
    "Additional Tools for Xcode DMG" hint). End with `exit $fail` so the script is non-zero on any hard
    mismatch per D-03.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop && bash -n scripts/check-tools.sh && head -4 scripts/check-tools.sh | grep -q 'set -euo pipefail' && grep -q 'sort -V' scripts/check-tools.sh && grep -q 'exit $fail' scripts/check-tools.sh && grep -Eq 'PacketLogger' scripts/check-tools.sh && grep -v '^#' scripts/check-tools.sh | grep -q 'WARN' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/check-tools.sh` exits 0 (valid syntax)
    - Line containing `set -euo pipefail` is present in the first 4 lines
    - Script contains `ROOT="$(cd "$(dirname "$0")/.." && pwd)"`
    - Script uses `sort -V` for the version-floor compare (asserts wireshark >= 4.4.0, jadx >= 1.5.1)
    - Script ends with `exit $fail`
    - PacketLogger check prints `WARN` (not `FAIL`) and does not set `fail` (a non-comment line containing `WARN` exists)
    - Java runtime check is a hard FAIL when `java -version` fails
    - Behavior: `bash scripts/check-tools.sh` exits non-zero on a machine missing a required tool, exits 0 once `brew bundle` + openjdk link are done with PacketLogger only WARNed
  </acceptance_criteria>
  <done>check-tools.sh asserts version floors for wireshark/jadx/adb, hard-fails on missing libimobiledevice/Java, WARNs on missing PacketLogger, and exits non-zero on any hard mismatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Homebrew formula/cask download → local machine | Tool binaries fetched over the network and executed locally |
| Raw BLE capture artifacts → git repo | Captures may carry device IDs / SMP bonding material; must never cross into committed history |
| Decompiled WHOOP APK → git repo | Proprietary material that DISCLAIMER §2 forbids committing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Raw `.pklg`/`.btsnoop` under re/capture/samples/ | mitigate | `.gitignore` rule `re/capture/samples/*` with `!.gitkeep`; acceptance criterion runs `git check-ignore` to prove a placed capture is ignored |
| T-01-02 | Information Disclosure | Decompiled WHOOP APK / JADX output | mitigate | Explicit `re/capture/samples/apk/` ignore rule + DISCLAIMER §2 comment; raw APK never tracked |
| T-01-03 | Tampering | Homebrew package supply chain | accept | All tools are homebrew-core / homebrew-cask / Apple-official, verified live in the brew index (RESEARCH Package Legitimacy Audit). No npm/PyPI/crates package added by this phase; slopcheck not applicable to brew formulae |
| T-01-04 | Elevation of Privilege | check-tools.sh execution | accept | Script is read-only verification (version queries + presence checks); performs no installs, no sudo, no network writes |
| T-01-SC | Tampering | brew installs | mitigate | No language-package installs in this plan; brew tools enumerated and verified in RESEARCH Package Legitimacy Audit (all Approved [VERIFIED: brew]). No [ASSUMED]/[SUS] language packages → no blocking checkpoint needed here |
</threat_model>

<verification>
1. `cd <repo> && brew bundle --file=Brewfile` completes (installs missing tools).
2. After `brew bundle` + linking openjdk: `bash scripts/check-tools.sh` exits 0 with PacketLogger only WARNed.
3. `git check-ignore re/capture/samples/foo.pklg` returns the path; `git check-ignore re/capture/samples/.gitkeep` returns nothing.
4. `git status` shows no untracked raw capture binaries (only the tracked .gitkeep / README placeholders).
</verification>

<success_criteria>
- Brewfile installs all brew-installable capture tools with correct `wireshark-app` GUI token
- check-tools.sh asserts version floors and exits non-zero on any hard mismatch, WARNs on manual tools
- .gitignore guarantees raw captures and decompiled APK output can never be committed
- re/capture/samples/ and re/capture/evidence/ exist and are tracked via .gitkeep
</success_criteria>

<output>
Create `.planning/phases/01-capture-foundation/01-01-SUMMARY.md` when done.
</output>
