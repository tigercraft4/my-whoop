---
phase: 13-backend-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - server/db/init.sql
  - server/ingest/app/read.py
  - server/ingest/app/store.py
autonomous: true
requirements:
  - ALG-10
  - ALG-11
  - ALG-12
  - ALG-13

must_haves:
  truths:
    - "REQUIREMENTS.md contém as definições ALG-10, ALG-11, ALG-12 e ALG-13"
    - "daily_metrics no PostgreSQL tem colunas sleep_performance, training_state, sleep_needed_min, total_calories_kcal"
    - "_DAILY_COLS em read.py inclui as 4 novas chaves"
    - "upsert_daily_metrics em store.py escreve e lê as 4 novas colunas"
  artifacts:
    - path: "server/db/init.sql"
      provides: "4 × ALTER TABLE daily_metrics ADD COLUMN IF NOT EXISTS"
      contains: "sleep_performance"
    - path: "server/ingest/app/read.py"
      provides: "_DAILY_COLS com 4 novos campos"
      contains: "sleep_performance"
    - path: "server/ingest/app/store.py"
      provides: "upsert_daily_metrics com INSERT + DO UPDATE para 4 novos campos"
      contains: "sleep_performance"
  key_links:
    - from: "server/db/init.sql"
      to: "daily_metrics"
      via: "ALTER TABLE daily_metrics ADD COLUMN IF NOT EXISTS"
      pattern: "ADD COLUMN IF NOT EXISTS sleep_performance"
    - from: "server/ingest/app/store.py"
      to: "daily_metrics"
      via: "INSERT INTO daily_metrics ... DO UPDATE SET"
      pattern: "sleep_performance.*EXCLUDED.sleep_performance"
---

<objective>
Infraestrutura de dados para a Fase 13: adicionar as definições de requisitos ALG-10 a ALG-13 ao REQUIREMENTS.md e propagar as 4 novas colunas pelo pipeline de dados PostgreSQL → Python, sem implementar qualquer lógica de algoritmo.

Purpose: Desbloquear os planos 13-02, 13-03 e 13-04. Todos os algoritmos dependem destas colunas existirem no DB e nas camadas de leitura/escrita Python.

Output: REQUIREMENTS.md actualizado, init.sql com 4 ALTER TABLE idempotentes, _DAILY_COLS em read.py com 4 novos campos, upsert_daily_metrics em store.py com INSERT + DO UPDATE actualizados.
</objective>

<execution_context>
@/Users/francisco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/francisco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-backend-parity/13-RESEARCH.md
@server/db/init.sql
@server/ingest/app/read.py
@server/ingest/app/store.py
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar ALG-10 a ALG-13 ao REQUIREMENTS.md</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md — ler o ficheiro inteiro para perceber o formato existente e onde inserir os novos requisitos
    - .planning/phases/13-backend-parity/13-RESEARCH.md secção "Requisitos ALG-10 a ALG-13" para as definições exactas
  </read_first>
  <action>
Adicionar uma nova secção "Backend Parity Algorithms" ao REQUIREMENTS.md logo após a secção "Algoritmos" existente (que termina em ALG-04). A secção deve seguir exactamente o mesmo estilo Markdown da secção "Algoritmos" existente (checkbox `- [ ]`, ID em negrito, dois-pontos, descrição).

Os 4 requisitos a adicionar (per planning_context):
- ALG-10: `sleep.py`: Sleep Performance = score ponderado 0–100 com fórmula composta (duração × W_dur=0.45 + eficiência × W_eff=0.25 + staging_ratio/0.40 × W_stg=0.20 + consistência × W_con=0.10); persiste em coluna separada `sleep_performance REAL` no `daily_metrics`; exposto pelo `/v1/today`
- ALG-11: `daily.py`: Training State calculado server-side a partir de (recovery × 100, strain) via `recovery_to_strain.json`; retorna "RESTORATIVE" | "OPTIMAL" | "OVERREACHING" | null; persiste na coluna `training_state TEXT` do `daily_metrics`; exposto pelo `/v1/today`; iOS prefere o valor do servidor quando não nulo, fallback ao cálculo client-side
- ALG-12: `daily.py`: Sleep Needed = baseline rolling 7d (média `total_sleep_min`) + strain_debt (f(strain_yesterday), máx 60 min) + sleep_debt (50% do défice acumulado, máx 120 min); clamp [300, 660] min; persiste na coluna `sleep_needed_min REAL` do `daily_metrics`; exposto pelo `/v1/today`; retorna None se menos de 3 noites de histórico
- ALG-13: `daily.py`/`calories.py`: Calorias totais diárias = RMR via Mifflin–St Jeor (nova função `rmr_kcal_per_day`) + calorias de exercício já computadas por sessão; persiste na coluna `total_calories_kcal REAL` do `daily_metrics`; exposto pelo `/v1/today`; None se sem perfil; iOS Today view mostra o valor num MetricCard "CALORIES"

Adicionar também ao bloco de Traceability no fim do ficheiro as 4 linhas:
| ALG-10 | Phase 13 | Pending |
| ALG-11 | Phase 13 | Pending |
| ALG-12 | Phase 13 | Pending |
| ALG-13 | Phase 13 | Pending |
  </action>
  <verify>
    <automated>grep -c "ALG-10\|ALG-11\|ALG-12\|ALG-13" /Users/francisco/Documents/my-whoop/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md contém ALG-10, ALG-11, ALG-12 e ALG-13 com definições e entradas na tabela de traceability</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar 4 colunas ao DB schema (init.sql)</name>
  <files>
    server/db/init.sql
  </files>
  <read_first>
    - server/db/init.sql — ler na íntegra para ver os ALTER TABLE existentes e onde inserir os novos (depois da linha `ALTER TABLE daily_metrics ADD COLUMN IF NOT EXISTS resp_rate_bpm`)
  </read_first>
  <action>
No final do bloco de migrações idempotentes de `daily_metrics` (após a linha `ALTER TABLE daily_metrics ADD COLUMN IF NOT EXISTS resp_rate_bpm   REAL;`), adicionar 4 linhas ALTER TABLE:

```
ALTER TABLE daily_metrics ADD COLUMN IF NOT EXISTS sleep_performance   REAL;
ALTER TABLE daily_metrics ADD COLUMN IF NOT EXISTS training_state      TEXT;
ALTER TABLE daily_metrics ADD COLUMN IF NOT EXISTS sleep_needed_min    REAL;
ALTER TABLE daily_metrics ADD COLUMN IF NOT EXISTS total_calories_kcal REAL;
```

Cada linha deve ser precedida de um comentário de bloco que identifique a que algoritmo pertence, seguindo o estilo dos comentários existentes no ficheiro (ex: `-- Phase 13 ALG-10: Sleep Performance weighted score 0–100`).

Não alterar o CREATE TABLE de daily_metrics (que usa IF NOT EXISTS e já está em produção). As ALTER TABLE idempotentes são o mecanismo correcto para bases de dados já inicializadas.
  </action>
  <verify>
    <automated>grep -c "sleep_performance\|training_state\|sleep_needed_min\|total_calories_kcal" /Users/francisco/Documents/my-whoop/server/db/init.sql</automated>
  </verify>
  <done>init.sql tem 4 ALTER TABLE idempotentes para daily_metrics; cada coluna tem o tipo correcto (REAL / TEXT)</done>
</task>

<task type="auto">
  <name>Task 3: Actualizar _DAILY_COLS em read.py e upsert_daily_metrics em store.py</name>
  <files>
    server/ingest/app/read.py
    server/ingest/app/store.py
  </files>
  <read_first>
    - server/ingest/app/read.py — focar em `_DAILY_COLS` (linha 227) e `query_today` para ver o padrão de lista de colunas
    - server/ingest/app/store.py — focar em `upsert_daily_metrics` (linha 105) para ver o INSERT e DO UPDATE SET completos, incluindo o binding de parâmetros posicionais
  </read_first>
  <action>
Em read.py, estender a lista `_DAILY_COLS` (linha 227) para incluir os 4 novos campos no fim da lista existente:

Antes: `"spo2_pct", "skin_temp_dev_c", "resp_rate_bpm", "computed_at"]`
Depois: `"spo2_pct", "skin_temp_dev_c", "resp_rate_bpm", "sleep_performance", "training_state", "sleep_needed_min", "total_calories_kcal", "computed_at"]`

Em store.py, actualizar `upsert_daily_metrics` em 3 sítios:
1. O `INSERT INTO daily_metrics (...)` — adicionar as 4 colunas à lista de colunas após `resp_rate_bpm`
2. A cláusula `VALUES (...)` — adicionar 4 marcadores `%s` posicionais correspondentes
3. O `DO UPDATE SET` — adicionar 4 linhas `sleep_performance = EXCLUDED.sleep_performance`, `training_state = EXCLUDED.training_state`, `sleep_needed_min = EXCLUDED.sleep_needed_min`, `total_calories_kcal = EXCLUDED.total_calories_kcal`
4. A chamada `conn.execute(...)` com os parâmetros — adicionar ao fim da tupla (antes do parêntese de fecho): `metrics.get("sleep_performance"), metrics.get("training_state"), metrics.get("sleep_needed_min"), metrics.get("total_calories_kcal")`

O count de `%s` no VALUES deve ser idêntico ao count de colunas na lista de colunas — verificar antes de guardar.
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
src = open('/Users/francisco/Documents/my-whoop/server/ingest/app/store.py').read()
assert 'sleep_performance' in src, 'sleep_performance missing from store.py'
assert 'training_state' in src, 'training_state missing from store.py'
assert 'sleep_needed_min' in src, 'sleep_needed_min missing from store.py'
assert 'total_calories_kcal' in src, 'total_calories_kcal missing from store.py'
src2 = open('/Users/francisco/Documents/my-whoop/server/ingest/app/read.py').read()
assert 'sleep_performance' in src2, 'sleep_performance missing from read.py'
print('OK')
"</automated>
  </verify>
  <done>_DAILY_COLS em read.py inclui os 4 novos campos; upsert_daily_metrics em store.py tem INSERT, DO UPDATE SET e binding de parâmetros actualizados para os 4 campos; Python não levanta SyntaxError</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| REQUIREMENTS.md write | Ficheiro de planeamento — sem superfície de ataque |
| init.sql → PostgreSQL | SQL executado pelo bootstrap_schema no arranque do servidor; só `ALTER TABLE IF NOT EXISTS` idempotentes |
| read.py / store.py → PostgreSQL | Queries parametrizadas — sem interpolação de strings de utilizador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | init.sql ALTER TABLE | accept | Só executado no arranque do servidor sob controlo do operador; as colunas são nullable e a migração é idempotente |
| T-13-01-02 | Information Disclosure | _DAILY_COLS em read.py | accept | As novas colunas (sleep_performance, training_state, sleep_needed_min, total_calories_kcal) são métricas derivadas de dados já expostos — sem PII adicional |
| T-13-01-03 | Tampering | store.py upsert_daily_metrics | mitigate | Manter queries parametrizadas com `%s` — NUNCA interpolação de strings; verificar que o count de parâmetros corresponde ao count de colunas |
| T-13-01-SC | Tampering | npm/pip/cargo installs | accept | Este plano não instala dependências externas |
</threat_model>

<verification>
1. `grep -v "^--" server/db/init.sql | grep -c "ADD COLUMN IF NOT EXISTS"` deve retornar pelo menos 9 (5 existentes + 4 novas)
2. `python3 -c "import server.ingest.app.read"` não deve levantar erro (Python sem erros de sintaxe)
3. `python3 -c "import server.ingest.app.store"` não deve levantar erro
4. `grep "total_calories_kcal" .planning/REQUIREMENTS.md` deve retornar uma linha com a definição ALG-13
</verification>

<success_criteria>
- REQUIREMENTS.md tem ALG-10, ALG-11, ALG-12, ALG-13 com definições completas e traceability
- init.sql tem 4 ALTER TABLE idempotentes para daily_metrics (sleep_performance REAL, training_state TEXT, sleep_needed_min REAL, total_calories_kcal REAL)
- _DAILY_COLS em read.py inclui os 4 novos campos (total agora 23 colunas)
- upsert_daily_metrics em store.py escreve e faz DO UPDATE para os 4 novos campos
- Nenhum algoritmo implementado neste plano (apenas infraestrutura)
</success_criteria>

<output>
Criar `.planning/phases/13-backend-parity/13-01-SUMMARY.md` quando concluído.
</output>
