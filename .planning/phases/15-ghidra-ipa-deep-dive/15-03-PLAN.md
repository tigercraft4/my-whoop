---
wave: 3
plan_id: "15-03"
title: "Remaining Screens (Trends/Coaching/Health/Profile) + BUGFIX-04 + Final Commit"
depends_on: ["15-02"]
files_modified:
  - "docs/specs/v4-ui-map.md"
  - "FINDINGS_5.md"
  - ".planning/notes/bugfix-04-findings.md"
autonomous: true
requirements_addressed: ["GHIDRA-01", "BUGFIX-04"]
---

# Plan 15-03: Remaining Screens (Trends/Coaching/Health/Profile) + BUGFIX-04 + Final Commit

## Objective

Completar o screen map com os 4 ecrãs restantes (Trends, Coaching, Health, Profile+Settings), documentar BUGFIX-04 passivamente, e fazer o commit final da Fase 15 com todos os artefactos.

## Background

- D-06: os primeiros 4 ecrãs (Home→Sleep→Strain→Trends) são o gate para Fase 17 arrancar.
- Coaching, Health, Profile não existem no OpenWhoop — mapeados apenas como referência.
- BUGFIX-04: scope passivo — anotar discrepâncias encontradas durante o mapeamento (não é auditoria separada).
- Após esta fase, `docs/specs/v4-ui-map.md` + `FINDINGS_5.md` alimentam directamente a Fase 17 UI-01.

## Tasks

### Task 1: Mapear ecrã Trends em v4-ui-map.md

<read_first>
- `docs/specs/v4-ui-map.md` — estado actual (Home + Sleep + Strain já mapeados)
- `docs/whoop-ui-reference.md` — secção Trends para evitar duplicação
</read_first>

<action>
1. Usar `mcp__ghidra-mcp__search_strings` com filtros: `"trends"`, `"Trends"`, `"TrendsView"`, `"historical"`, `"WhoopHistoricalTrends"`, `"chart"`, `"week"`, `"month"`.
2. Procurar color constants para trend lines, chart backgrounds, period selectors.
3. Procurar layout: chart height, period selector padding, data point size.
4. Acrescentar `## Screen: Trends` a `v4-ui-map.md` com as 4 subsecções.
5. **Nota**: Trends é o 4º ecrã do gate da Fase 17 — completar antes de avançar para Coaching/Health/Profile.
6. Commit parcial após esta secção.
</action>

<acceptance_criteria>
- `## Screen: Trends` adicionado com 4 subsecções.
- Chart/timeline colors incluídos (hex ou [not found]).
- Commit efectuado após esta secção.
</acceptance_criteria>

---

### Task 2: Mapear ecrã Coaching em v4-ui-map.md

<read_first>
- `docs/specs/v4-ui-map.md` — estado actual
- `docs/whoop-ui-reference.md` — secção Coaching (Tab 2: Coaching) para labels existentes
</read_first>

<action>
1. Usar `mcp__ghidra-mcp__search_strings` com filtros: `"coach"`, `"Coach"`, `"CoachViewController"`, `"recommendation"`, `"insights"`, `"goal"`.
2. Usar `mcp__ghidra-mcp__search_strings` com `filter: "coaching"` para encontrar strings específicas.
3. Acrescentar `## Screen: Coaching` a `v4-ui-map.md`.
4. **Nota implementação**: "Não existe no OpenWhoop — mapeado como referência para Fase 17 backlog."
5. Commit parcial após esta secção.
</action>

<acceptance_criteria>
- `## Screen: Coaching` adicionado com nota "não implementado no OpenWhoop".
- Component Hierarchy descreve o que o Ghidra revela sobre a estrutura — mesmo que parcial.
- Commit efectuado.
</acceptance_criteria>

---

### Task 3: Mapear ecrã Health em v4-ui-map.md

<read_first>
- `docs/specs/v4-ui-map.md` — estado actual
- `docs/whoop-ui-reference.md` — secção Health (Tab 3: Health) para labels existentes
</read_first>

<action>
1. Usar `mcp__ghidra-mcp__search_strings` com filtros: `"health"`, `"Health"`, `"HealthView"`, `"spo2"`, `"SpO2"`, `"bloodOxygen"`, `"skinTemp"`, `"respiration"`.
2. Estes dados são HYPOTHESIS no OpenWhoop (SpO₂/skin temp/respiration não confirmados via hardware) — notar isso.
3. Acrescentar `## Screen: Health` a `v4-ui-map.md` com nota de status de cada campo.
4. Commit parcial após esta secção.
</action>

<acceptance_criteria>
- `## Screen: Health` adicionado.
- Nota explícita sobre campos HYPOTHESIS vs VERIFIED.
- Commit efectuado.
</acceptance_criteria>

---

### Task 4: Mapear Profile + Settings em v4-ui-map.md

<read_first>
- `docs/specs/v4-ui-map.md` — estado actual
</read_first>

<action>
1. Usar `mcp__ghidra-mcp__search_strings` com filtros: `"profile"`, `"Profile"`, `"settings"`, `"Settings"`, `"account"`, `"preferences"`, `"notification"`.
2. Acrescentar `## Screen: Profile + Settings` com nota "não existe no OpenWhoop".
3. Focar em layout patterns que possam ser reutilizados (listas, toggles, cards de configuração).
4. Commit parcial após esta secção.
</action>

<acceptance_criteria>
- `## Screen: Profile + Settings` adicionado com nota "não existe no OpenWhoop".
- Commit efectuado.
</acceptance_criteria>

---

### Task 5: Documentar BUGFIX-04 passivamente

<read_first>
- `FINDINGS_5.md` — para referências cruzadas de bugs encontrados durante mapeamento
- `ios/OpenWhoop/Metrics/LocalMetricsComputer.swift` — implementação actual para comparação
- `docs/whoop-ui-reference.md` — labels esperadas vs o que está implementado
</read_first>

<action>
1. Criar `.planning/notes/bugfix-04-findings.md` com estrutura:
```markdown
# BUGFIX-04 Findings — Ghidra IPA Analysis (Phase 15)

**Date:** 2026-06-01
**Source:** Passive findings during Phase 15 screen mapping and coefficient decode

## Bugs Found

### BUG-04-NN: [Description]
- **Expected behavior (WHOOP 5.37.0 app):** [what Ghidra reveals the app should do]
- **Actual behavior (OpenWhoop):** [what our implementation does]
- **Reproduction:** [steps or conditions]
- **Source:** [Ghidra address or string key that reveals the discrepancy]
- **Priority:** [High/Medium/Low]
- **Phase for fix:** [17 or backlog]
```

2. Para cada discrepância identificada durante Plans 01-03 desta fase, adicionar uma entrada.
3. Se nenhuma discrepância encontrada durante o mapeamento, criar o ficheiro com `## No bugs found during Phase 15 mapping` — não forçar findings.
4. Actualizar `FINDINGS_5.md` com referência cruzada: `## BUGFIX-04 (see .planning/notes/bugfix-04-findings.md)`.
</action>

<acceptance_criteria>
- `.planning/notes/bugfix-04-findings.md` criado com pelo menos o template vazio mas estruturado.
- `FINDINGS_5.md` referencia `bugfix-04-findings.md`.
- Cada bug tem: description, expected (WHOOP app), actual (OpenWhoop), reproduction, source.
</acceptance_criteria>

---

### Task 6: Verificação final e commit de fase

<read_first>
- `docs/specs/v4-ui-map.md` — verificar 7 ecrãs completos
- `FINDINGS_5.md` — verificar GHIDRA-HB-01, GHIDRA-02, BUGFIX-04 ref
- `.planning/notes/bugfix-04-findings.md` — verificar existência
</read_first>

<action>
1. Verificar que `docs/specs/v4-ui-map.md` tem exactamente 7 secções `## Screen:`.
2. Verificar que `FINDINGS_5.md` foi actualizado com GHIDRA-HB-01 e GHIDRA-02.
3. Verificar que nenhum ficheiro `.swift` foi modificado: `git diff --name-only | grep "\.swift$"` deve retornar vazio.
4. Fazer commit final com todos os ficheiros pendentes.
5. Actualizar `FINDINGS_5.md` com uma secção `## Phase 15 Summary` no topo ou final:
   - Screens mapeados: 7
   - Coeficientes validados: Harris-Benedict (CONFIRMED) + Keytel (CONFIRMED ou CORRECTED)
   - BUGFIX-04 items: N
   - Ficheiros produzidos: `docs/specs/v4-ui-map.md`, `.planning/notes/bugfix-04-findings.md`
</action>

<acceptance_criteria>
- `docs/specs/v4-ui-map.md` tem 7 secções `## Screen:` (Home/Overview, Sleep, Strain, Trends, Coaching, Health, Profile+Settings).
- `FINDINGS_5.md` actualizado com GHIDRA-HB-01, GHIDRA-02, Phase 15 Summary.
- `.planning/notes/bugfix-04-findings.md` existe.
- `git diff --name-only HEAD | grep "\.swift$"` retorna vazio.
- Commit final efectuado com mensagem descritiva.
</acceptance_criteria>

---

## Verification

```bash
# Verificar 7 ecrãs mapeados
grep -c "## Screen:" docs/specs/v4-ui-map.md

# Verificar cada ecrã obrigatório
for screen in "Home" "Sleep" "Strain" "Trends" "Coaching" "Health" "Profile"; do
  grep -q "## Screen:.*${screen}" docs/specs/v4-ui-map.md && echo "✓ ${screen}" || echo "✗ MISSING: ${screen}"
done

# Verificar cada ecrã tem 4 subsecções (aproximado)
for heading in "Component Hierarchy" "Colors" "Spacing" "Labels"; do
  grep -c "### ${heading}" docs/specs/v4-ui-map.md
done

# Verificar BUGFIX-04 file
test -f .planning/notes/bugfix-04-findings.md && echo "bugfix-04-findings.md exists" || echo "MISSING"

# Verificar sem ficheiros Swift
git diff --name-only HEAD | grep "\.swift$" && echo "WARNING: Swift modified" || echo "Clean — no Swift"

# Verificar FINDINGS_5.md
grep "GHIDRA-HB-01\|GHIDRA-02\|BUGFIX-04" FINDINGS_5.md
```

## must_haves

```yaml
goal: "Phase 15 complete — all 7 screens mapped, GHIDRA-02 done, BUGFIX-04 documented"
truths:
  - "docs/specs/v4-ui-map.md has 7 ## Screen: sections covering all WHOOP tabs"
  - "Each screen section has Component Hierarchy, Colors, Spacing & Radii, Labels subsections"
  - "FINDINGS_5.md has GHIDRA-HB-01 (confirmed), GHIDRA-02 (confirmed or corrected), BUGFIX-04 reference"
  - ".planning/notes/bugfix-04-findings.md exists with structured template"
  - "No Swift files were modified throughout Phase 15"
  - "Commits done incrementally — at least one per screen section"
  - "All values are clean-room: structural/data only, no pseudocode or proprietary assets"
```
