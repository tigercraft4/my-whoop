---
wave: 2
plan_id: "15-02"
title: "Harris-Benedict Confirmation + UI Strings — Home/Sleep/Strain Screen Map"
depends_on: ["15-01"]
files_modified:
  - "FINDINGS_5.md"
  - "docs/specs/v4-ui-map.md"
autonomous: true
requirements_addressed: ["GHIDRA-01"]
---

# Plan 15-02: Harris-Benedict Confirmation + UI Strings — Home/Sleep/Strain Screen Map

## Objective

Commit a finding GHIDRA-HB-01 (Harris-Benedict confirmado), criar `docs/specs/v4-ui-map.md` com a estrutura base, e mapear os primeiros 3 ecrãs (Home/Overview, Sleep, Strain) com Component Hierarchy, Colors, Spacing e Labels via Ghidra MCP.

## Background

- `docs/whoop-ui-reference.md` já tem string keys e field labels da Fase 8 — não duplicar.
- `v4-ui-map.md` deve focar em **valores visuais** (cores hex, espaçamentos, raios) ausentes do `whoop-ui-reference.md`.
- `mcp__ghidra-mcp__search_strings` é a ferramenta principal para localizar UI constants.
- Padrão de commit: uma secção por screen, committed antes de avançar.

## Tasks

### Task 1: Commit GHIDRA-HB-01 (Harris-Benedict confirmed)

<read_first>
- `FINDINGS_5.md` — estado actual para inserir HB-01 no lugar correcto
- `.planning/phases/15-ghidra-ipa-deep-dive/15-CONTEXT.md` — secção `<specifics>` com os valores confirmados
</read_first>

<action>
Acrescentar secção `## GHIDRA-HB-01: Harris-Benedict Resting Coefficients — CONFIRMED` a `FINDINGS_5.md` com:
- Endereço: `0x1058a5a80`
- Valores: `[13.397, 479.9, -5.677, 88.362, 9.247, 309.8, -4.330, 447.593]`
- Comparação com `calories.py` `_HB_COEFFS` ou equivalente — tabela: coef | Ghidra | calories.py | Match
- Status: `CONFIRMED — all 8 values match calories.py exactly`
- Nota: estes são coeficientes RESTING (Harris-Benedict), não Keytel workout (endereço separado — ver GHIDRA-02)
</action>

<acceptance_criteria>
- `FINDINGS_5.md` contém `## GHIDRA-HB-01` com tabela de 8 doubles e status CONFIRMED.
- Secção clarifica que `0x1058a5a80` é Harris-Benedict RESTING, não Keytel.
</acceptance_criteria>

---

### Task 2: Criar docs/specs/v4-ui-map.md com estrutura base e ecrã Home/Overview

<read_first>
- `docs/whoop-ui-reference.md` — tab structure e string labels existentes (secções Tab 1 Home, Field Labels, Screen Hierarchy)
- `docs/specs/` — verificar se v4-ui-map.md já existe
</read_first>

<action>
1. Criar `docs/specs/v4-ui-map.md` com frontmatter:
   - Título: `# WHOOP 5.37.0 — v4 UI Screen Map`
   - Data, fonte (Ghidra IPA analysis, clean-room), aviso legal
   - Nota: "Complementa `docs/whoop-ui-reference.md` — este ficheiro foca valores visuais (cores, espaçamentos, raios)"

2. Para **Home/Overview**, usar `mcp__ghidra-mcp__search_strings` com filtros relacionados:
   - `"Recovery"`, `"#"` (hex colors), `"cornerRadius"`, `"padding"`, `"gauge"`, `"ring"`
   - Procurar padrões como `"#1A1A1A"`, `"0xFF..."`, `"cornerRadius = "`, `"spacing = "`

3. Usar `mcp__ghidra-mcp__search_strings` com `filter: "color"`, `filter: "Color"`, `filter: "background"` para extrair hex codes.

4. Estrutura da secção Home/Overview em `v4-ui-map.md`:
```markdown
## Screen: Home / Overview

### Component Hierarchy
[stack order: ring gauges → metric row → impact tiles]

### Colors
| Role | Hex | Usage |
|------|-----|-------|
| Background | #... | Main screen background |
| Recovery ring | #... | Recovery gauge fill |
| ... | | |

### Spacing & Radii
| Element | Value | Type |
|---------|-------|------|
| Card corner radius | ...pt | cornerRadius |
| Ring gauge diameter | ...pt | frame |
| ... | | |

### Labels
[Cross-ref: ver docs/whoop-ui-reference.md Tab 1 para string keys completos]
[Só adicionar strings novas não documentadas lá]
```

5. Se Ghidra não retornar valores concretos para um campo: marcar como `[not found in binary]` — não inventar valores.
</action>

<acceptance_criteria>
- `docs/specs/v4-ui-map.md` criado com header e secção `## Screen: Home / Overview`.
- Secção tem 4 subsecções: Component Hierarchy, Colors, Spacing & Radii, Labels.
- Colors inclui pelo menos 2 valores hex concretos extraídos do Ghidra (ou `[not found]` com nota explicativa).
- Nenhum valor inventado — cada valor tem source (endereço Ghidra ou string pattern).
</acceptance_criteria>

---

### Task 3: Mapear ecrã Sleep em v4-ui-map.md

<read_first>
- `docs/whoop-ui-reference.md` — secção Sleep (Tab 4: Sleep) para evitar duplicação
- `docs/specs/v4-ui-map.md` — estado actual após Task 2
</read_first>

<action>
1. Usar `mcp__ghidra-mcp__search_strings` com filtros: `"sleep"`, `"Sleep"`, `"stage"`, `"SleepCard"`, `"SleepView"`, `"awake"`, `"rem"`, `"light"`, `"deep"`.
2. Procurar color constants associadas a sleep stages — tipicamente: Awake (amarelo), REM (azul), Light (verde claro), Deep (verde escuro/teal).
3. Procurar strings de espaçamento e layout: `"sleepStageHeight"`, `"barHeight"`, `"stagePadding"`.
4. Acrescentar `## Screen: Sleep` a `v4-ui-map.md` com:
   - Component Hierarchy: SleepCard → stage breakdown bars → duration labels → efficiency score
   - Colors: sleep stage colors por nome/hex
   - Spacing: bar heights, card padding
   - Labels: apenas strings novas não documentadas em `whoop-ui-reference.md`
5. Commit parcial após esta secção.
</action>

<acceptance_criteria>
- `## Screen: Sleep` adicionado a `v4-ui-map.md` com 4 subsecções.
- Sleep stage colors incluídos (Awake, REM, Light, Deep) — valores hex ou `[not found]`.
- Commit efectuado antes de avançar para Strain.
</acceptance_criteria>

---

### Task 4: Mapear ecrã Strain em v4-ui-map.md

<read_first>
- `docs/whoop-ui-reference.md` — secção Strain para evitar duplicação
- `docs/specs/v4-ui-map.md` — estado actual após Tasks 2-3
</read_first>

<action>
1. Usar `mcp__ghidra-mcp__search_strings` com filtros: `"strain"`, `"Strain"`, `"workout"`, `"StrainCard"`, `"StrainView"`, `"zone"`, `"activity"`, `"restorative"`, `"optimal"`, `"overreaching"`.
2. Procurar strain zone colors — tipicamente gradient do azul ao vermelho por zona.
3. Procurar layout: strain bar height, workout card radius, activity tile dimensions.
4. Acrescentar `## Screen: Strain` a `v4-ui-map.md` com as 4 subsecções.
5. Commit parcial após esta secção.
</action>

<acceptance_criteria>
- `## Screen: Strain` adicionado a `v4-ui-map.md` com 4 subsecções.
- Strain zone colors (Restorative/Optimal/Overreaching) incluídos.
- Commit efectuado após esta secção.
</acceptance_criteria>

---

## Verification

```bash
# Verificar que v4-ui-map.md foi criado com as 3 secções obrigatórias
test -f docs/specs/v4-ui-map.md && echo "v4-ui-map.md exists" || echo "MISSING"
grep -c "## Screen:" docs/specs/v4-ui-map.md
grep "## Screen: Home" docs/specs/v4-ui-map.md
grep "## Screen: Sleep" docs/specs/v4-ui-map.md
grep "## Screen: Strain" docs/specs/v4-ui-map.md

# Verificar GHIDRA-HB-01 em FINDINGS_5.md
grep "GHIDRA-HB-01" FINDINGS_5.md

# Verificar que nenhum ficheiro Swift foi tocado
git diff --name-only HEAD | grep "\.swift$" && echo "WARNING: Swift files modified" || echo "No Swift files touched (correct)"
```

## must_haves

```yaml
goal: "Gate screens Home/Sleep/Strain mapped — Fase 17 UI work can begin"
truths:
  - "docs/specs/v4-ui-map.md exists with ## Screen: Home/Overview, ## Screen: Sleep, ## Screen: Strain"
  - "Each screen has 4 subsections: Component Hierarchy, Colors, Spacing & Radii, Labels"
  - "Colors section uses hex values from Ghidra or explicit [not found] markers — no invented values"
  - "FINDINGS_5.md has GHIDRA-HB-01 section confirming Harris-Benedict at 0x1058a5a80"
  - "No Swift files were modified"
  - "Commits done per screen (at least 3 commits: Home, Sleep, Strain)"
```
