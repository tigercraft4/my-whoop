---
plan: "18.1-01"
phase: "18.1"
title: "LAYOUT-01 — RootTabView: Nova Tab Bar (4 tabs) + Botão W Overlay"
status: complete
completed: "2026-06-01"
---

## Summary

Substituída a tab bar de 5 tabs (Today/Sleep/Strain/Trends/Device) por 4 tabs WHOOP-style (Início/Saúde/Comunidade/Mais) em `RootTabView.swift`, e movido o LiveView para um botão overlay circular "W" no canto inferior direito.

## What was built

- **RootTabView.swift** reescrito com `@SceneStorage("selectedTab")` default "home", 4 tabs (`Label("Início", "house")`, `Label("Saúde", "heart.text.clipboard")`, `Label("Comunidade", "person.3")`, `Label("Mais", "line.3.horizontal")`), envolvido em `ZStack(alignment: .bottomTrailing)` com botão W circular `WH.Color.sleepPurple` + sheet `showLive`.
- **HomeView.swift** criado como stub de compilação (preenchido pelo plano 18.1-02).
- **HealthView.swift** criado como stub de compilação.
- **CommunityView.swift** criado como stub "Em breve".
- **MoreView.swift** criado como stub "Mais".

## Key files

key-files.created:
  - ios/OpenWhoop/Tabs/HomeView.swift
  - ios/OpenWhoop/Tabs/HealthView.swift
  - ios/OpenWhoop/Tabs/CommunityView.swift
  - ios/OpenWhoop/Tabs/MoreView.swift

key-files.modified:
  - ios/OpenWhoop/App/RootTabView.swift

## Deviations

Nenhum desvio. Build gate BUILD SUCCEEDED após a criação dos stubs e nova tab bar.

## Self-Check: PASSED

- `grep -c 'tabItem' ios/OpenWhoop/App/RootTabView.swift` → 4 ✓
- `grep 'sleepPurple\|showLive\|sheet' ios/OpenWhoop/App/RootTabView.swift` → ≥3 matches ✓
- Build gate BUILD SUCCEEDED ✓
- `grep 'TodayView\|\.tag("today")\|\.tag("device")' ios/OpenWhoop/App/RootTabView.swift` → vazio ✓
