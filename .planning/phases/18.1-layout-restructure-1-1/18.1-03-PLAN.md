---
id: "18.1-03"
wave: 2
depends_on: ["18.1-01", "18.1-02"]
phase: "18.1"
title: "LAYOUT-03 — HealthView completo + CommunityView + MoreView finais"
objective: "Preencher HealthView com métricas de saúde locais (HRV, RHR, SpO₂, sleepPerformance — gráficos 7 dias com MetricChart), e finalizar CommunityView e MoreView com conteúdo real de placeholder (deviceId, app version, settings básicos)."
files_modified:
  - ios/OpenWhoop/Tabs/HealthView.swift
  - ios/OpenWhoop/Tabs/CommunityView.swift
  - ios/OpenWhoop/Tabs/MoreView.swift
requirements_addressed: []
autonomous: true
---

# Plan 18.1-03 — LAYOUT-03: HealthView + CommunityView + MoreView Finais

## Objective

Substituir os stubs de Wave 1 com implementações completas: `HealthView` com gráficos de tendência HRV/RHR/SpO₂/sono 7 dias; `CommunityView` com mensagem "Em breve" definitiva; `MoreView` com deviceId, app version, e navegação para settings existentes.

## Context

`MetricChart` aceita `series: [TrendPoint]` e `kind: MetricKind`. Para HealthView, os dados de 7 dias vêm de `metrics.daily(fromDay: sevenDaysAgo, toDay: today)` mapeados para TrendPoint. `CommunityView` e `MoreView` são views stub funcionais (sem dados cloud). `MoreView` acede a `AppConfig.deviceId` e `Bundle.main.infoDictionary["CFBundleShortVersionString"]`.

## Tasks

### Task 3.1 — HealthView com gráficos de tendência 7 dias

<read_first>
- `ios/OpenWhoop/Tabs/HealthView.swift` (stub actual a substituir)
- `ios/OpenWhoop/Charts/MetricChart.swift` (interface completa: series, kind, showAxes, showSelection, yDomain, selected)
- `ios/OpenWhoop/Metrics/MetricsRepository.swift` (func daily(fromDay:toDay:) -> [DailyMetric], today.avgHrv, today.restingHr, today.spo2, today.sleepPerformance)
- `ios/OpenWhoop/Design/DesignTokens.swift` (WH.Color.recoveryGreen, WH.Color.strainBlue, WH.Color.sleepPurple, WH.Font.*)
- `ios/OpenWhoop/Charts/MetricDetailView.swift` (ver como usa MetricChart e MetricKind para padrão)
</read_first>

<action>
Substituir `ios/OpenWhoop/Tabs/HealthView.swift` com implementação completa:

Estrutura:
```swift
struct HealthView: View {
    @EnvironmentObject private var metrics: MetricsRepository
    @State private var weeklyMetrics: [DailyMetric] = []
    @State private var selectedHRV: TrendPoint? = nil
    @State private var selectedRHR: TrendPoint? = nil
    @State private var selectedSleep: TrendPoint? = nil
    @State private var isLoading = false

    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(alignment: .leading, spacing: WH.Spacing.lg) {
                    // HRV Section
                    healthSection(
                        title: "VARIABILIDADE DA FREQUÊNCIA CARDÍACA",
                        subtitle: "HRV",
                        currentValue: metrics.today?.avgHrv.map { "\(Int($0.rounded())) ms" } ?? "—",
                        accentColor: WH.Color.recoveryGreen,
                        series: hrvSeries,
                        kind: .hrv,
                        selected: $selectedHRV
                    )

                    // RHR Section
                    healthSection(
                        title: "FREQUÊNCIA CARDÍACA EM REPOUSO",
                        subtitle: "RHR",
                        currentValue: metrics.today?.restingHr.map { "\($0) bpm" } ?? "—",
                        accentColor: WH.Color.recoveryRed,
                        series: rhrSeries,
                        kind: .rhr,
                        selected: $selectedRHR
                    )

                    // SpO2 Section (último valor, sem gráfico de tendência)
                    spo2Section

                    // Sleep Performance Section
                    healthSection(
                        title: "QUALIDADE DO SONO",
                        subtitle: "SONO",
                        currentValue: metrics.today?.sleepPerformance.map { "\(Int($0.rounded()))%" } ?? "—",
                        accentColor: WH.Color.sleepPurple,
                        series: sleepSeries,
                        kind: .sleepPerformance,
                        selected: $selectedSleep
                    )

                    Spacer(minLength: WH.Spacing.xl)
                }
                .padding(WH.Spacing.md)
            }
            .background(WH.Color.background)
            .navigationTitle("SAÚDE")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .principal) {
                    Text("SAÚDE")
                        .font(.system(size: 15, weight: .semibold))
                        .foregroundStyle(WH.Color.textPrimary)
                        .tracking(1.5)
                }
            }
        }
        .preferredColorScheme(.dark)
        .task { await loadWeeklyMetrics() }
    }
```

`healthSection(title:subtitle:currentValue:accentColor:series:kind:selected:)` function:
```swift
@ViewBuilder
private func healthSection(title: String, subtitle: String, currentValue: String,
                            accentColor: Color, series: [TrendPoint], kind: MetricKind,
                            selected: Binding<TrendPoint?>) -> some View {
    VStack(alignment: .leading, spacing: WH.Spacing.sm) {
        Text(title)
            .font(WH.Font.cardTitle)
            .foregroundStyle(WH.Color.textSecondary)
            .tracking(0.8)
        HStack(alignment: .lastTextBaseline, spacing: WH.Spacing.xs) {
            Text(currentValue)
                .font(WH.Font.metricLarge())
                .foregroundStyle(accentColor)
                .monospacedDigit()
            Text("7 dias")
                .font(WH.Font.caption)
                .foregroundStyle(WH.Color.textSecondary)
        }
        MetricChart(series: series, kind: kind, showAxes: false, showSelection: false, selected: selected)
            .frame(height: 80)
            .clipped()
    }
    .padding(WH.Spacing.md)
    .background(WH.Color.surface, in: RoundedRectangle(cornerRadius: WH.Radius.card, style: .continuous))
}
```

`spo2Section` var:
```swift
private var spo2Section: some View {
    VStack(alignment: .leading, spacing: WH.Spacing.sm) {
        Text("SATURAÇÃO DE OXIGÉNIO").font(WH.Font.cardTitle).foregroundStyle(WH.Color.textSecondary).tracking(0.8)
        Text(metrics.today?.spo2.map { String(format: "%.1f%%", $0) } ?? "—")
            .font(WH.Font.metricLarge())
            .foregroundStyle(WH.Color.teal)
            .monospacedDigit()
        Text("Último valor registado")
            .font(WH.Font.caption)
            .foregroundStyle(WH.Color.textSecondary)
    }
    .padding(WH.Spacing.md)
    .background(WH.Color.surface, in: RoundedRectangle(cornerRadius: WH.Radius.card, style: .continuous))
}
```

Série helpers (mapear `weeklyMetrics` para `[TrendPoint]`):
- `private var hrvSeries: [TrendPoint]` — `weeklyMetrics.compactMap { m in m.avgHrv.map { TrendPoint(date: ..., value: $0) } }`
- `private var rhrSeries: [TrendPoint]` — `.restingHr.map { Double($0) }`
- `private var sleepSeries: [TrendPoint]` — `.sleepPerformance`

`loadWeeklyMetrics()`:
```swift
private func loadWeeklyMetrics() async {
    isLoading = true
    let cal = Calendar.current
    let today = cal.startOfDay(for: Date())
    let sevenDaysAgo = cal.date(byAdding: .day, value: -6, to: today) ?? today
    let fmt = DateFormatter()
    fmt.dateFormat = "yyyy-MM-dd"
    weeklyMetrics = await metrics.daily(fromDay: fmt.string(from: sevenDaysAgo),
                                        toDay: fmt.string(from: today))
    isLoading = false
}
```

`TrendPoint` — verificar interface em `MetricDetailView.swift`; se estrutura é `TrendPoint(date: Date, value: Double)`, usar esse init.

Nota: se `MetricsRepository.today` não tiver propriedade `spo2`, usar `metrics.lastNight?.spo2 ?? nil` — confirmar nome correcto lendo o modelo em `MetricsRepository.swift`.
</action>

<acceptance_criteria>
- `grep 'MetricChart' ios/OpenWhoop/Tabs/HealthView.swift | wc -l` retorna 3 (HRV, RHR, Sono)
- Ficheiro contém `VARIABILIDADE DA FREQUÊNCIA CARDÍACA` e `FREQUÊNCIA CARDÍACA EM REPOUSO` e `QUALIDADE DO SONO`
- Ficheiro contém `SATURAÇÃO DE OXIGÉNIO` (secção SpO₂ sem gráfico de tendência)
- Ficheiro contém `loadWeeklyMetrics` async function com `metrics.daily(`
- Ficheiro usa `WH.Color.recoveryGreen`, `WH.Color.sleepPurple`, `WH.Color.teal` — sem hardcoded hex
- `grep 'navigationTitle\|SAÚDE' ios/OpenWhoop/Tabs/HealthView.swift | wc -l` retorna ≥ 2
</acceptance_criteria>

---

### Task 3.2 — CommunityView definitiva

<read_first>
- `ios/OpenWhoop/Tabs/CommunityView.swift` (stub actual)
- `ios/OpenWhoop/Design/DesignTokens.swift` (WH.Font.metricLarge(), WH.Color.textSecondary, WH.Color.background)
</read_first>

<action>
Substituir `ios/OpenWhoop/Tabs/CommunityView.swift` com implementação definitiva:

```swift
import SwiftUI

struct CommunityView: View {
    var body: some View {
        NavigationStack {
            VStack(spacing: WH.Spacing.md) {
                Spacer()
                Image(systemName: "person.3")
                    .font(.system(size: 48, weight: .light))
                    .foregroundStyle(WH.Color.textSecondary.opacity(0.5))
                Text("Em breve")
                    .font(.system(size: 24, weight: .semibold, design: .rounded))
                    .foregroundStyle(WH.Color.textPrimary)
                Text("A funcionalidade de Comunidade estará disponível numa versão futura.")
                    .font(WH.Font.caption)
                    .foregroundStyle(WH.Color.textSecondary)
                    .multilineTextAlignment(.center)
                    .padding(.horizontal, WH.Spacing.xl)
                Spacer()
            }
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(WH.Color.background)
            .toolbar {
                ToolbarItem(placement: .principal) {
                    Text("COMUNIDADE")
                        .font(.system(size: 15, weight: .semibold))
                        .foregroundStyle(WH.Color.textPrimary)
                        .tracking(1.5)
                }
            }
        }
        .preferredColorScheme(.dark)
    }
}
```
</action>

<acceptance_criteria>
- `grep 'Em breve' ios/OpenWhoop/Tabs/CommunityView.swift` retorna match
- Ficheiro contém `Image(systemName: "person.3")`
- Ficheiro contém `COMUNIDADE` no toolbar
- Ficheiro usa apenas tokens WH.* — sem hardcoded colors
</acceptance_criteria>

---

### Task 3.3 — MoreView com deviceId + app version + settings básicos

<read_first>
- `ios/OpenWhoop/Tabs/MoreView.swift` (stub actual)
- `ios/OpenWhoop/Settings/SettingsView.swift` (verificar se existe para reutilizar ou linkar)
- `ios/OpenWhoop/App/OpenWhoopApp.swift` (confirmar AppConfig.deviceId existe e é acessível)
- `ios/OpenWhoop/Design/DesignTokens.swift` (WH.Color.surface, WH.Font.*)
</read_first>

<action>
Substituir `ios/OpenWhoop/Tabs/MoreView.swift` com implementação definitiva:

```swift
import SwiftUI

struct MoreView: View {
    private var appVersion: String {
        Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "—"
    }
    private var buildNumber: String {
        Bundle.main.infoDictionary?["CFBundleVersion"] as? String ?? "—"
    }

    var body: some View {
        NavigationStack {
            List {
                Section("DISPOSITIVO") {
                    moreRow(label: "Device ID", value: AppConfig.deviceId)
                }

                Section("APP") {
                    moreRow(label: "Versão", value: "\(appVersion) (\(buildNumber))")
                    NavigationLink("Definições") {
                        SettingsView()
                    }
                }

                Section("INFORMAÇÃO") {
                    moreRow(label: "Protocolo", value: "WHOOP 5.0 BLE")
                    moreRow(label: "Desenvolvido por", value: "OpenWhoop")
                }
            }
            .listStyle(.insetGrouped)
            .scrollContentBackground(.hidden)
            .background(WH.Color.background)
            .toolbar {
                ToolbarItem(placement: .principal) {
                    Text("MAIS")
                        .font(.system(size: 15, weight: .semibold))
                        .foregroundStyle(WH.Color.textPrimary)
                        .tracking(1.5)
                }
            }
        }
        .preferredColorScheme(.dark)
    }

    private func moreRow(label: String, value: String) -> some View {
        HStack {
            Text(label)
                .foregroundStyle(WH.Color.textPrimary)
            Spacer()
            Text(value)
                .foregroundStyle(WH.Color.textSecondary)
                .lineLimit(1)
                .truncationMode(.middle)
        }
    }
}
```

Se `SettingsView` não existir: substituir `NavigationLink("Definições") { SettingsView() }` por `moreRow(label: "Definições", value: "—")` para não bloquear a build.
</action>

<acceptance_criteria>
- `grep 'AppConfig.deviceId\|Device ID' ios/OpenWhoop/Tabs/MoreView.swift` retorna match
- Ficheiro contém `CFBundleShortVersionString` (app version)
- Ficheiro contém `WHOOP 5.0 BLE`
- Ficheiro contém `Text("MAIS")` no toolbar
- Ficheiro usa `WH.Color.background` e `WH.Color.textSecondary` — sem hardcoded colors
</acceptance_criteria>

---

### Task 3.4 — Build gate Wave 2

<read_first>
- `ios/OpenWhoop/Tabs/HealthView.swift` (implementação completa)
- `ios/OpenWhoop/Tabs/CommunityView.swift` (implementação completa)
- `ios/OpenWhoop/Tabs/MoreView.swift` (implementação completa)
</read_first>

<action>
Executar build gate final para Wave 2:

```bash
cd ios && xcodegen generate && xcodebuild build \
  -scheme OpenWhoop \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED"
```

Se erros em `HealthView`:
- `TrendPoint` init incorrecto: ler `MetricDetailView.swift` para estrutura exacta de TrendPoint
- `MetricKind` casos não existem: usar `.hrv`, `.rhr`, `.sleepPerformance` (confirmar em MetricDetailView.swift)
- `metrics.daily(fromDay:toDay:)` não existe: verificar assinatura exacta em MetricsRepository.swift

Se erros em `MoreView`:
- `SettingsView` não encontrado: remover NavigationLink e usar `moreRow(label: "Definições", value: "—")`
- `AppConfig.deviceId` não é String: usar `String(AppConfig.deviceId)`
</action>

<acceptance_criteria>
- `cd ios && xcodegen generate && xcodebuild build -scheme OpenWhoop -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | tail -3` mostra "BUILD SUCCEEDED"
- Zero erros nos 3 ficheiros desta wave
- `grep 'error:' /tmp/build_18_1_03.log 2>/dev/null | wc -l` retorna 0 (ou log não usado — verificar saída directa)
</acceptance_criteria>

---

## must_haves

```yaml
truths:
  - HealthView mostra HRV, RHR, SpO₂, e sleepPerformance com MetricChart (7 dias) sem WHOOP Age nem Healthspan
  - Dados de 7 dias carregados via metrics.daily(fromDay:toDay:) como [DailyMetric] → [TrendPoint]
  - SpO₂ apresentado como último valor (sem gráfico 7 dias — dados insuficientes)
  - CommunityView tem "Em breve" centrado com ícone person.3 e explicação em pt-PT
  - MoreView tem deviceId (AppConfig.deviceId), app version (Bundle), e link para SettingsView
  - Todos os valores nil mostram "—" — nenhuma crash com optional unwrap forçado
  - Zero hardcoded hex — apenas tokens WH.*
  - Build gate BUILD SUCCEEDED no final desta wave

risks:
  - TrendPoint pode ter init diferente do esperado — ler MetricDetailView.swift antes de assumir
  - metrics.daily() pode retornar [DailyMetric] vazio no simulador sem dados — charts mostram "Not enough data" (comportamento built-in do MetricChart)
  - spo2 pode não existir como campo em DailyMetric/today — verificar schema; se não existe omitir secção SpO₂
  - SettingsView pode não existir no projecto após cleanup da Fase 16 — usar fallback gracioso
```

## Verification

- [ ] Build gate: `cd ios && xcodegen generate && xcodebuild build -scheme OpenWhoop -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` exit 0
- [ ] `grep -c 'MetricChart' ios/OpenWhoop/Tabs/HealthView.swift` retorna 3
- [ ] `grep 'Em breve' ios/OpenWhoop/Tabs/CommunityView.swift` retorna match
- [ ] `grep 'AppConfig.deviceId\|CFBundleShortVersionString' ios/OpenWhoop/Tabs/MoreView.swift | wc -l` retorna 2
- [ ] `grep 'Color(hex:\|UIColor\|#[0-9A-Fa-f]' ios/OpenWhoop/Tabs/HealthView.swift ios/OpenWhoop/Tabs/CommunityView.swift ios/OpenWhoop/Tabs/MoreView.swift` retorna vazio
</content>
</invoke>