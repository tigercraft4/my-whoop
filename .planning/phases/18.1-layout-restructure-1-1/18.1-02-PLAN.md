---
id: "18.1-02"
wave: 1
depends_on: []
phase: "18.1"
title: "LAYOUT-02 — HomeView: Header + 3 Rings + Secções WHOOP"
objective: "Reestruturar completamente TodayView em HomeView com header customizado (perfil + strain + navegação de data + recovery%), 3 rings em linha (Sono/Recuperação/Esforço) com tap para detalhe, e todas as secções abaixo dos rings: Dados em falta, Monitor de Saúde/Stress, O meu dia, Atividades de Hoje, Sono desta noite."
files_modified:
  - ios/OpenWhoop/Tabs/HomeView.swift
requirements_addressed: []
autonomous: true
---

# Plan 18.1-02 — LAYOUT-02: HomeView Completo (Header + Rings + Secções)

## Objective

Preencher `HomeView.swift` (stub criado pelo plano 18.1-01) com toda a implementação: header customizado com navegação de data, 3 rings `ZoneRingView` em linha, e 5 secções de scroll abaixo. Replicar o layout do WHOOP 5.37.0 ecrã Início.

## Context

`TodayView.swift` mantém-se no projecto (mantém funcionalidade de detalhe), mas a tab Início passa a usar `HomeView.swift`. Os rings reutilizam `ZoneRingView(value:maxValue:color:lineWidth:size:)` com `size: 90`. Os dados vêm de `@EnvironmentObject var metrics: MetricsRepository`. A navegação de data usa `@State private var selectedDate: Date = .now` com `metrics.daily(fromDay:toDay:)` para carregar dias anteriores.

## Tasks

### Task 2.1 — Header customizado (perfil + strain + data nav + recovery%)

<read_first>
- `ios/OpenWhoop/Tabs/HomeView.swift` (stub actual a substituir)
- `ios/OpenWhoop/Design/DesignTokens.swift` (tokens WH.Color.*, WH.Font.*, WH.Spacing.*)
- `ios/OpenWhoop/Design/ScreenHeader.swift` (interface actual — para NÃO reutilizar, criar header custom)
- `ios/OpenWhoop/Metrics/MetricsRepository.swift` (propriedades: today.strain, today.recovery)
</read_first>

<action>
Substituir o stub `HomeView.swift` com a implementação completa. Começar pelo header:

Estrutura `HomeView`:
```swift
@EnvironmentObject private var metrics: MetricsRepository
@EnvironmentObject private var live: LiveViewModel
@EnvironmentObject private var hkExporter: HealthKitExporterViewModel
@State private var selectedDate: Date = .now
@State private var selectedDayMetric: DailyMetric? = nil
@AppStorage("hk.authDeniedShown") private var authDeniedShown = false
```

Header customizado (`homeHeader` private var):
- `HStack(alignment: .center, spacing: 0)` com padding horizontal `WH.Spacing.md`
- **Left side**: `HStack(spacing: WH.Spacing.xs)`:
  - `Image(systemName: "person.circle")` font(.system(size: 22)) foregroundStyle(WH.Color.textSecondary)
  - `Image(systemName: "flame.fill")` font(.system(size: 13)) foregroundStyle(WH.Color.strainAccent)
  - `Text(strainLabel)` font(WH.Font.caption) foregroundStyle(WH.Color.textPrimary) — onde `strainLabel = metrics.today?.strain.map { String(format: "%.1f", $0) } ?? "—"`
- **Center**: `HStack(spacing: WH.Spacing.sm)`:
  - `Button { navigateDate(-1) } label: { Image(systemName: "chevron.left").font(.system(size: 13, weight: .semibold)) }` foregroundStyle(WH.Color.textSecondary)
  - `Text(dateLabelText)` font(.system(size: 15, weight: .semibold)).foregroundStyle(WH.Color.textPrimary) — "HOJE" ou "SEG 31 MAI"
  - `Button { navigateDate(+1) } label: { Image(systemName: "chevron.right").font(.system(size: 13, weight: .semibold)) }` foregroundStyle(selectedDate.isToday ? WH.Color.textSecondary.opacity(0.3) : WH.Color.textSecondary) `.disabled(selectedDate.isToday)`
- **Right side**: `HStack(spacing: WH.Spacing.xs)`:
  - `Text(recoveryHeaderLabel)` font(WH.Font.caption) foregroundStyle(recoveryHeaderColor) — onde `recoveryHeaderLabel = effectiveMetric?.recovery.map { "\(Int($0.rounded()))%" } ?? "--"` e `recoveryHeaderColor` baseia-se em `WH.Color.recoveryColor(forPercent:)`
  - `Image(systemName: live.state.connected ? "wave.3.right" : "wave.3.right.slash")` font(.system(size: 13)) foregroundStyle(live.state.connected ? WH.Color.recoveryGreen : WH.Color.textSecondary)
- `.frame(maxWidth: .infinity)` + `.padding(.top, WH.Spacing.sm)`

Helpers:
- `private var dateLabelText: String` — se `Calendar.current.isDateInToday(selectedDate)` retorna "HOJE"; caso contrário formatar com `DateFormatter` "EEE d MMM" em maiúsculas (locale "pt_PT")
- `private func navigateDate(_ days: Int)` — `selectedDate = Calendar.current.date(byAdding: .day, value: days, to: selectedDate) ?? selectedDate` + trigger de load de dados
- `private var effectiveMetric: DailyMetric?` — `selectedDate.isToday ? metrics.today : selectedDayMetric`
- Extensão local: `extension Date { var isToday: Bool { Calendar.current.isDateInToday(self) } }`
</action>

<acceptance_criteria>
- `ios/OpenWhoop/Tabs/HomeView.swift` contém `@State private var selectedDate: Date = .now`
- Ficheiro contém `Text("HOJE")` ou `dateLabelText` que produz "HOJE" para data actual
- Ficheiro contém `navigateDate` function com `byAdding: .day`
- Ficheiro contém `chevron.left` e `chevron.right` system images
- Ficheiro contém `recoveryHeaderLabel` ou equivalente com `?? "--"`
- Header usa apenas tokens WH.* — `grep 'Color(hex:\|UIColor\|#[0-9A-F]' ios/OpenWhoop/Tabs/HomeView.swift` retorna vazio
</acceptance_criteria>

---

### Task 2.2 — 3 Rings em linha (Sono / Recuperação / Esforço)

<read_first>
- `ios/OpenWhoop/Design/Components/ZoneRingView.swift` (interface completa — parâmetros: value, maxValue, color, lineWidth, size, centerLabel)
- `ios/OpenWhoop/Design/DesignTokens.swift` (WH.Color.sleepPurple, WH.Color.strainBlue, WH.Color.recoveryColor(forPercent:))
- `ios/OpenWhoop/Tabs/SleepView.swift` (destino de tap no ring SONO)
- `ios/OpenWhoop/Tabs/StrainView.swift` (destino de tap no ring ESFORÇO)
- `ios/OpenWhoop/Design/Components/RecoveryCard.swift` (confirmar que existe como detail view)
</read_first>

<action>
Adicionar à `HomeView` a secção `ringsSection` private var:

```
HStack(spacing: WH.Spacing.lg) {
  ringSono
  ringRecuperacao
  ringEsforco
}
.frame(maxWidth: .infinity)
.padding(.horizontal, WH.Spacing.md)
.padding(.vertical, WH.Spacing.sm)
```

Cada ring é uma `NavigationLink` + `VStack`:

**Ring SONO** (`ringSono`):
- `NavigationLink(destination: SleepView())`
- `VStack(spacing: WH.Spacing.xs)`:
  - `ZoneRingView(value: sleepPerf, maxValue: 100, color: WH.Color.sleepPurple, lineWidth: 8, size: 90, centerLabel: sleepPerfLabel)`
  - `Text("SONO").font(WH.Font.cardTitle).foregroundStyle(WH.Color.textSecondary).tracking(0.8)`
  - `Image(systemName: "chevron.right").font(.system(size: 10)).foregroundStyle(WH.Color.textSecondary)`
- Onde `sleepPerf = effectiveMetric?.sleepPerformance ?? 0` e `sleepPerfLabel = effectiveMetric?.sleepPerformance.map { "\(Int($0.rounded()))%" } ?? "—"`

**Ring RECUPERAÇÃO** (`ringRecuperacao`):
- `NavigationLink(destination: MetricDetailView(kind: .recovery))`
- `VStack(spacing: WH.Spacing.xs)`:
  - `ZoneRingView(value: recoveryVal, maxValue: 100, color: recoveryRingColor, lineWidth: 8, size: 90, centerLabel: recoveryLabel)`
  - `Text("RECUPERAÇÃO").font(WH.Font.cardTitle).foregroundStyle(WH.Color.textSecondary).tracking(0.8)`
  - `Image(systemName: "chevron.right").font(.system(size: 10)).foregroundStyle(WH.Color.textSecondary)`
- Onde `recoveryVal = effectiveMetric?.recovery ?? 0`, `recoveryRingColor = WH.Color.recoveryColor(forPercent: effectiveMetric?.recovery ?? 0)`, `recoveryLabel = effectiveMetric?.recovery.map { "\(Int($0.rounded()))%" } ?? "—"`

**Ring ESFORÇO** (`ringEsforco`):
- `NavigationLink(destination: StrainView())`
- `VStack(spacing: WH.Spacing.xs)`:
  - `ZoneRingView(value: strainVal, maxValue: 21, color: WH.Color.strainBlue, lineWidth: 8, size: 90, centerLabel: strainLabel)`
  - `Text("ESFORÇO").font(WH.Font.cardTitle).foregroundStyle(WH.Color.textSecondary).tracking(0.8)`
  - `Image(systemName: "chevron.right").font(.system(size: 10)).foregroundStyle(WH.Color.textSecondary)`
- Onde `strainVal = effectiveMetric?.strain ?? 0`, `strainLabel = effectiveMetric?.strain.map { String(format: "%.1f", $0) } ?? "—"`

Todos os NavigationLink com `.buttonStyle(.plain)`.
</action>

<acceptance_criteria>
- `grep -c 'ZoneRingView' ios/OpenWhoop/Tabs/HomeView.swift` retorna 3
- Ficheiro contém `size: 90` nos ZoneRingView (ou valor próximo — não mais de 100pt)
- Ficheiro contém `Text("SONO")` e `Text("RECUPERAÇÃO")` e `Text("ESFORÇO")`
- Ficheiro contém `NavigationLink(destination: SleepView())` e `NavigationLink(destination: StrainView())`
- Todos os rings usam `WH.Color.sleepPurple`, `WH.Color.recoveryColor(`, `WH.Color.strainBlue` — sem hardcoded hex
- `grep 'lineWidth: 8\|size: 9' ios/OpenWhoop/Tabs/HomeView.swift | wc -l` retorna ≥ 3
</acceptance_criteria>

---

### Task 2.3 — Secções de scroll abaixo dos rings

<read_first>
- `ios/OpenWhoop/Tabs/HomeView.swift` (estado actual com header + rings)
- `ios/OpenWhoop/Alarm/AlarmView.swift` (interface para integrar no card "Sono desta noite")
- `ios/OpenWhoop/Design/DesignTokens.swift` (WH.Color.surface, WH.Radius.card, WH.Spacing.*)
- `ios/OpenWhoop/Metrics/MetricsRepository.swift` (lastRefreshedAt, isRefreshing para card "Dados em falta")
</read_first>

<action>
Adicionar `ScrollView` com `VStack(alignment: .leading, spacing: WH.Spacing.md)` contendo:

**Secção 1 — "Dados em falta" card** (`dadosEmFaltaCard`):
- Mostrar apenas quando: `effectiveMetric == nil && !metrics.isRefreshing && selectedDate.isToday`
- Card: `VStack(alignment: .leading, spacing: WH.Spacing.sm)` com padding `WH.Spacing.md`, background `WH.Color.surface2` (dark grey), cornerRadius `WH.Radius.card`
- `Text("DADOS EM FALTA").font(WH.Font.cardTitle).foregroundStyle(WH.Color.textSecondary).tracking(0.8)`
- `Text("O WHOOP não recebeu dados nas últimas \(hoursSinceRefresh)h. Verifica a ligação Bluetooth.").font(WH.Font.caption).foregroundStyle(WH.Color.textSecondary)`
- `hoursSinceRefresh`: calculado de `metrics.lastRefreshedAt` — se nil usar 24

**Secção 2 — Monitor de Saúde + Monitor de Stress** (`healthMonitorsRow`):
- `HStack(spacing: WH.Spacing.sm)`:
  - Card esquerdo "MONITOR DE SAÚDE": `NavigationLink(destination: HealthView()) { monitorCard(title: "MONITOR\nDE SAÚDE", value: hrv7dayAvgLabel, icon: "waveform.path.ecg") }`
  - Card direito "MONITOR DE STRESS": `NavigationLink(destination: HealthView()) { monitorCard(title: "MONITOR\nDE STRESS", value: rhr7dayAvgLabel, icon: "brain.head.profile") }`
- `monitorCard(title: String, value: String, icon: String) -> some View`: `VStack(alignment: .leading, spacing: WH.Spacing.xs)` com Icon + título + valor, background `WH.Color.surface`, cornerRadius `WH.Radius.card`, `.frame(maxWidth: .infinity)`
- `hrv7dayAvgLabel = metrics.today?.avgHrv.map { "\(Int($0.rounded())) ms" } ?? "--"`
- `rhr7dayAvgLabel = metrics.today?.restingHr.map { "\($0) bpm" } ?? "--"`

**Secção 3 — "O meu dia"** (`myDaySection`):
- `HStack { Text("O meu dia").font(.system(size: 17, weight: .semibold)).foregroundStyle(WH.Color.textPrimary); Spacer(); Button(action: {}) { Image(systemName: "plus").foregroundStyle(WH.Color.textSecondary) }.disabled(true) }`
- `.padding(.horizontal, WH.Spacing.md).padding(.top, WH.Spacing.sm)`

**Secção 4 — "Atividades de Hoje" card** (`actividadesCard`):
- Card com background `WH.Color.surface`, cornerRadius `WH.Radius.card`, padding `WH.Spacing.md`
- `VStack(spacing: WH.Spacing.sm)`:
  - `Text("ATIVIDADES DE HOJE").font(WH.Font.cardTitle).foregroundStyle(WH.Color.textSecondary).tracking(0.8)`
  - `HStack(spacing: WH.Spacing.sm)`:
    - `Button("ADICIONAR ATIVIDADE") {}` e `Button("INICIAR ATIVIDADE") {}` — ambos `.disabled(true)`, estilo: `Text(...).font(WH.Font.caption).foregroundStyle(WH.Color.textSecondary).padding(.vertical, 8).padding(.horizontal, 12).background(WH.Color.surface2, in: RoundedRectangle(cornerRadius: WH.Radius.chip))`

**Secção 5 — "Sono desta noite" card** (`sonoNoiteCard`):
- Card com background `WH.Color.surface`, cornerRadius `WH.Radius.card`, padding `WH.Spacing.md`
- `VStack(alignment: .leading, spacing: WH.Spacing.sm)`:
  - `Text("SONO DESTA NOITE").font(WH.Font.cardTitle).foregroundStyle(WH.Color.textSecondary).tracking(0.8)`
  - Se `effectiveMetric?.sleepNeededMin` != nil: `Text("Recomendado: \(formatSleepMinutes(needed))").font(WH.Font.body).foregroundStyle(WH.Color.textPrimary)`
  - Caso contrário: `Text("—").font(WH.Font.body).foregroundStyle(WH.Color.textSecondary)`
  - `NavigationLink(destination: AlarmView()) { Text("CONFIGURAR ALARME").font(WH.Font.caption).foregroundStyle(WH.Color.sleepPurple) }` — AlarmView injectado via EnvironmentObject live
- Reutilizar `formatSleepMinutes` de TodayView (copiar função helper privada)

Estrutura final do body:
```
NavigationStack {
  ZStack(alignment: .top) {
    WH.Color.background.ignoresSafeArea()
    if metrics.isRefreshing && effectiveMetric == nil {
      loadingView
    } else {
      VStack(spacing: 0) {
        homeHeader
        ScrollView {
          VStack(alignment: .leading, spacing: WH.Spacing.md) {
            ringsSection
            dadosEmFaltaCard
            healthMonitorsRow
            myDaySection
            actividadesCard
            sonoNoiteCard
            Spacer(minLength: WH.Spacing.xl)
          }
          .padding(.horizontal, WH.Spacing.md)
        }
        .background(WH.Color.background)
      }
    }
  }
  .toolbar(.hidden, for: .navigationBar)
}
.preferredColorScheme(.dark)
.task { await metrics.refresh() }
.refreshable { await metrics.refresh() }
```
</action>

<acceptance_criteria>
- `grep 'DADOS EM FALTA\|hoursSinceRefresh' ios/OpenWhoop/Tabs/HomeView.swift` retorna match
- `grep 'MONITOR' ios/OpenWhoop/Tabs/HomeView.swift | wc -l` retorna ≥ 2
- Ficheiro contém `Text("O meu dia")` e `Image(systemName: "plus")`
- Ficheiro contém `Text("ATIVIDADES DE HOJE")` e `"ADICIONAR ATIVIDADE"` e `"INICIAR ATIVIDADE"`
- Ficheiro contém `Text("SONO DESTA NOITE")` e `sleepNeededMin`
- Ficheiro contém `AlarmView()` como destination de NavigationLink
- Ficheiro contém `formatSleepMinutes` function
- `grep 'WH.Color.background\|WH.Color.surface' ios/OpenWhoop/Tabs/HomeView.swift | wc -l` retorna ≥ 4
</acceptance_criteria>

---

### Task 2.4 — Build gate + remoção de TodayView como tab principal

<read_first>
- `ios/OpenWhoop/Tabs/HomeView.swift` (implementação completa)
- `ios/OpenWhoop/Tabs/TodayView.swift` (manter — não apagar, é destination válido para MetricDetailView)
- `ios/OpenWhoop/App/RootTabView.swift` (confirmar que usa HomeView, não TodayView)
</read_first>

<action>
1. Verificar que `RootTabView.swift` referencia `HomeView()` e não `TodayView()` como tab Início
2. Verificar que `TodayView.swift` ainda existe (NÃO apagar — é usado como destino de NavigationLink noutras partes)
3. Executar build gate:
   ```bash
   cd ios && xcodegen generate && xcodebuild build \
     -scheme OpenWhoop \
     -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
     2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED"
   ```
4. Se erros de `@EnvironmentObject`: verificar que `OpenWhoopApp.swift` injeta `MetricsRepository`, `LiveViewModel`, `HealthKitExporterViewModel` via `.environmentObject()` — adicionar `HealthView`, `HomeView` ao chain se necessário (normalmente herdado do RootTabView)
5. Se erro "cannot find 'MetricDetailView'": confirmar import ou usar `NavigationLink(destination: { MetricDetailView(kind: .recovery) })`
</action>

<acceptance_criteria>
- `cd ios && xcodegen generate && xcodebuild build -scheme OpenWhoop -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | tail -3` mostra "BUILD SUCCEEDED"
- `ls ios/OpenWhoop/Tabs/TodayView.swift` sem erro (ficheiro mantido)
- `grep 'HomeView()' ios/OpenWhoop/App/RootTabView.swift` retorna match
- Zero erros de compilação relacionados com tipos não encontrados
</acceptance_criteria>

---

## must_haves

```yaml
truths:
  - HomeView tem header customizado com navegação de data (@State selectedDate) mostrando "HOJE" ou "SEG 31 MAI"
  - 3 ZoneRingView em HStack: SONO (sleepPurple, maxValue 100), RECUPERAÇÃO (recoveryColor, maxValue 100), ESFORÇO (strainBlue, maxValue 21)
  - Cada ring tem label abaixo + chevron.right indicando tappável
  - Tap nos rings navega para SleepView, MetricDetailView(.recovery), StrainView respectivamente
  - Card "Dados em falta" mostra apenas quando sem dados e sem refresh
  - 2 cards Monitor de Saúde e Monitor de Stress lado a lado, navegam para HealthView
  - Card "Sono desta noite" mostra sleepNeededMin formatado e link para AlarmView
  - TodayView.swift mantém-se no projecto (não apagado)
  - Todos os valores nil mostram "—" (em dashes, não vazio)
  - Zero hardcoded hex — apenas tokens WH.*

risks:
  - MetricDetailView(kind: .recovery) pode requerer import adicional — verificar namespace
  - AlarmView requer @EnvironmentObject live: LiveViewModel — certificar que é injectado na chain de NavigationStack
  - heart.text.clipboard pode não compilar em iOS 15 — tab Saúde usa esse ícone em RootTabView (verificar disponibilidade mínima no project.yml)
  - Dados de dia anterior (navigateDate) requerem chamada async metrics.daily() — implementar como .task(id: selectedDate)
```

## Verification

- [ ] Build gate: `cd ios && xcodegen generate && xcodebuild build -scheme OpenWhoop -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` exit 0
- [ ] `grep -c 'ZoneRingView' ios/OpenWhoop/Tabs/HomeView.swift` retorna 3
- [ ] `grep '@State private var selectedDate' ios/OpenWhoop/Tabs/HomeView.swift` retorna match
- [ ] `grep 'SONO\|RECUPERAÇÃO\|ESFORÇO' ios/OpenWhoop/Tabs/HomeView.swift | wc -l` retorna ≥ 3
- [ ] `grep 'sleepNeededMin\|SONO DESTA NOITE' ios/OpenWhoop/Tabs/HomeView.swift | wc -l` retorna ≥ 2
- [ ] `ls ios/OpenWhoop/Tabs/TodayView.swift` sem erro
</content>
</invoke>