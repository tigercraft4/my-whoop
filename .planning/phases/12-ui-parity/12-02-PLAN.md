---
phase: 12-ui-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/OpenWhoop/Resources/recovery_to_strain.json
  - ios/OpenWhoop/BLE/TrainingState.swift
  - ios/OpenWhoop/Design/Components/StrainCard.swift
  - ios/OpenWhoop/Tabs/StrainView.swift
  - ios/OpenWhoopTests/TrainingStateTests.swift
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "StrainCard shows a Training State badge reading RESTORATIVE, OPTIMAL, or OVERREACHING when recovery is known"
    - "The badge is computed from recovery_to_strain.json using DailyMetric.recovery and DailyMetric.strain"
    - "The badge is omitted entirely when DailyMetric.recovery is nil"
    - "Badge colour: RESTORATIVE=strainAccent(blue), OPTIMAL=recoveryGreen, OVERREACHING=recoveryRed"
  artifacts:
    - path: "ios/OpenWhoop/Resources/recovery_to_strain.json"
      provides: "Training-state lookup table bundled into the app"
      contains: "lower_rec_strain"
    - path: "ios/OpenWhoop/BLE/TrainingState.swift"
      provides: "trainingState(recovery:strain:) lookup function"
      min_lines: 25
    - path: "ios/OpenWhoop/Design/Components/StrainCard.swift"
      provides: "Training State badge UI"
      contains: "trainingState"
    - path: "ios/OpenWhoopTests/TrainingStateTests.swift"
      provides: "Unit tests for the training-state lookup"
      min_lines: 20
  key_links:
    - from: "ios/OpenWhoop/BLE/TrainingState.swift"
      to: "ios/OpenWhoop/Resources/recovery_to_strain.json"
      via: "Bundle.main.url(forResource:)"
      pattern: "recovery_to_strain"
    - from: "ios/OpenWhoop/Design/Components/StrainCard.swift"
      to: "ios/OpenWhoop/BLE/TrainingState.swift"
      via: "trainingState(recovery:strain:) call"
      pattern: "trainingState\\("
---

<objective>
Add the WHOOP Training State badge to the Strain card (D-05 to D-08). Copy `recovery_to_strain.json`
into the iOS bundle, add a `trainingState(recovery:strain:)` lookup that reads the table and compares
the day's `strain` against the row's `lower_rec_strain`/`upper_rec_strain` for the user's `recovery`,
and render the resulting zone (RESTORATIVE / OPTIMAL / OVERREACHING) as a coloured badge below the
strain gauge — only when `DailyMetric.recovery != nil`.

This plan owns `StrainCard.swift` and `StrainView.swift` exclusively so it runs in parallel with
plans 01 and 03.

Purpose: WHOOP-parity Training State display on the Strain tab (UI-05).
Output: bundled lookup JSON, `TrainingState.swift` lookup + tests, updated `StrainCard.swift` + `StrainView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-ui-parity/12-CONTEXT.md
@ios/OpenWhoop/Design/Components/StrainCard.swift
@ios/OpenWhoop/Tabs/StrainView.swift
@ios/OpenWhoop/BLE/LiveStrainAccumulator.swift
@ios/OpenWhoop/Design/DesignTokens.swift
@ios/OpenWhoopTests/MetricsRepositoryTests.swift
@server/ingest/app/analysis/recovery_to_strain.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bundle the lookup table and add trainingState(recovery:strain:)</name>
  <files>ios/OpenWhoop/Resources/recovery_to_strain.json, ios/OpenWhoop/BLE/TrainingState.swift, ios/OpenWhoopTests/TrainingStateTests.swift</files>
  <behavior>
    - trainingState(recovery: 100, strain: 5) → "RESTORATIVE" (5 < lower_rec_strain 13 for recovery 100)
    - trainingState(recovery: 100, strain: 17) → "OPTIMAL" (rec_strain band: 13 ≤ 17 ≤ 21)
    - trainingState(recovery: 50, strain: 20) → "OVERREACHING" (20 > upper_rec_strain ~14.98 for recovery 50)
    - trainingState(recovery: 0, strain: 5) → "OPTIMAL" (recovery 0: lower 0, upper 10; 5 within band)
    - Rounds recovery to nearest integer 0–100 to index the table; clamps out-of-range recovery.
  </behavior>
  <action>
    Copy `server/ingest/app/analysis/recovery_to_strain.json` to
    `ios/OpenWhoop/Resources/recovery_to_strain.json` (the constraint requires this — it is NOT yet
    in the bundle; only `strain_raw_scale_lookup.json` is). Because `project.yml` declares
    `sources: [OpenWhoop]` with createIntermediateGroups, running `xcodegen generate` auto-includes the
    new JSON as a bundle resource — no manual pbxproj edit.

    Automated test scaffold is MISSING — create `ios/OpenWhoopTests/TrainingStateTests.swift` FIRST
    with the behavior cases above (import XCTest + the app module), then implement until green.

    Create `ios/OpenWhoop/BLE/TrainingState.swift` following the bundle-loading pattern in
    `LiveStrainAccumulator.swift` (a `static let` table loaded once via
    `Bundle.main.url(forResource: "recovery_to_strain", withExtension: "json")` + `JSONDecoder`).
    Decode rows of `{recovery, lower_rec_strain, rec_strain, upper_rec_strain}` into a Codable struct.
    Implement `func trainingState(recovery: Double, strain: Double) -> String?` that (D-05/D-08):
    - clamps `recovery` to 0...100 and rounds to Int to pick the matching row,
    - returns "RESTORATIVE" when `strain < row.lower_rec_strain`,
    - returns "OVERREACHING" when `strain > row.upper_rec_strain`,
    - returns "OPTIMAL" otherwise (the rec_strain band).
    Do NOT emit "IMPOSSIBLE" (D-05). Return nil if the table failed to load so callers can omit the badge.
  </action>
  <verify>
    <automated>cd ios && test -f OpenWhoop/Resources/recovery_to_strain.json && grep -q "lower_rec_strain" OpenWhoop/Resources/recovery_to_strain.json && test -f OpenWhoopTests/TrainingStateTests.swift && xcodegen generate --project . >/dev/null 2>&1 && xcodebuild test -project OpenWhoop.xcodeproj -scheme OpenWhoop -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:OpenWhoopTests/TrainingStateTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5 | grep -q "TEST SUCCEEDED" && echo OK</automated>
  </verify>
  <done>recovery_to_strain.json is in Resources/ and contains the table; TrainingState.swift loads it and trainingState(recovery:strain:) returns the correct zone for all behavior cases; TrainingStateTests created and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Render the Training State badge in StrainCard and StrainView zone label</name>
  <files>ios/OpenWhoop/Design/Components/StrainCard.swift, ios/OpenWhoop/Tabs/StrainView.swift</files>
  <action>
    In `StrainCard.swift` (D-06/D-07/D-08): replace the current hardcoded `zoneLabel` threshold logic
    with the lookup result. Compute the badge text via `trainingState(recovery: r*100, strain: s)`
    using `daily?.recovery` (stored as 0–1 fraction, multiply by 100) and `daily?.strain`. Show the
    badge below the existing ZoneRingView only when BOTH `daily?.recovery != nil` AND the lookup
    returns non-nil (D-07 — omit entirely otherwise; do not show "—"). Colour the badge per D-06:
    RESTORATIVE → `WH.Color.strainAccent`, OPTIMAL → `WH.Color.recoveryGreen`,
    OVERREACHING → `WH.Color.recoveryRed`. Keep the gauge `ringColor` as `strainAccent` (do not
    recolour the ring — D-08 governs only the zone text). Render as a Text chip styled like the
    existing zone label (cardTitle font, kerning 1.5).

    In `StrainView.swift`: if a strain zone label is shown there independently of StrainCard, apply the
    same `trainingState`-driven text + colour so the tab and card agree (D-08). If StrainView simply
    embeds StrainCard, no change is needed beyond confirming the badge appears — record which case
    applies in the SUMMARY.

    Update the StrainCard `#Preview` so the existing sample DailyMetrics (which include recovery) show
    the badge; add a no-recovery sample to confirm omission.
  </action>
  <verify>
    <automated>cd ios && grep -q "trainingState(" OpenWhoop/Design/Components/StrainCard.swift && xcodegen generate --project . >/dev/null 2>&1 && xcodebuild -project OpenWhoop.xcodeproj -scheme OpenWhoop -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && echo OK</automated>
  </verify>
  <done>StrainCard shows the colour-coded Training State badge from the lookup when recovery is present, omits it when recovery is nil, and the app builds; StrainView agrees with the card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle→app | recovery_to_strain.json is a static, version-controlled asset copied from the server repo; read-only at runtime, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | recovery_to_strain.json bundled asset | accept | Static asset committed to repo; integrity covered by git + build; no runtime mutation |
| T-12-03 | Denial of Service | TrainingState.swift table load | mitigate | Loader returns nil on decode failure; StrainCard omits badge gracefully (no crash) |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs; only a local JSON copy and Swift source |
</threat_model>

<verification>
- recovery_to_strain.json present in Resources/ and contains `lower_rec_strain`.
- TrainingStateTests created and pass (lookup correctness across recovery/strain quadrants).
- `xcodebuild` BUILD SUCCEEDED after badge wiring.
- StrainCard.swift references `trainingState(`.
</verification>

<success_criteria>
- Training State badge (RESTORATIVE/OPTIMAL/OVERREACHING) shows on the Strain card, computed from the lookup table, coloured per D-06.
- Badge omitted when recovery is nil (D-07).
- App builds and unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-ui-parity/12-02-SUMMARY.md` when done.
</output>
