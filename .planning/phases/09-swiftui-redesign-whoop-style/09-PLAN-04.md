---
phase: 9
plan: "09-04"
title: "SleepCard — SleepView WHOOP-Style"
wave: 3
depends_on:
  - "09-02"
files_modified:
  - ios/OpenWhoop/Design/Components/SleepCard.swift
  - ios/OpenWhoop/Tabs/SleepView.swift
autonomous: true
requirements:
  - UI-04
---

# Plan 09-04: SleepCard — SleepView WHOOP-Style

## Objective

Criar `SleepCard.swift` que usa `HypnogramView` para a stacked bar de fases de sono (REM/Deep/Light/Awake) e mostra duração e eficiência de `CachedSleepSession` com dados reais. Integrar na `SleepView` como card de destaque no topo.

## Context

`SleepView` actualmente já mostra `HypnogramView` e dados de `CachedSleepSession`. A tarefa é encapsular a apresentação num `SleepCard` WHOOP-style com `.background(Color.black)` (D-12) e garantir que os dados reais de Phase 7 são usados. O `SleepView` já carrega `detail.session: CachedSleepSession` e `detail.daily: DailyMetric?`.

<tasks>

<task id="09-04-T1">
<type>execute</type>
<title>Criar SleepCard.swift</title>

<read_first>
- `ios/OpenWhoop/Tabs/SleepView.swift` — estrutura actual: como `detail` é carregado, layout existente de duração/eficiência/hypnogram
- `ios/OpenWhoop/Tabs/HypnogramView.swift` — assinatura de `HypnogramView(session:)` e `parseStages(_:)`
- `ios/OpenWhoop/Design/DesignTokens.swift` — WH.Color, WH.Font, WH.Spacing, WH.Radius, WH.Color.sleepPurple
- `.planning/phases/09-swiftui-redesign-whoop-style/09-CONTEXT.md` — D-07, D-12
- `docs/whoop-ui-reference.md` — Tab 4 Sleep, field labels: SLEEP PERFORMANCE, HOURS OF SLEEP
</read_first>

<action>
1. Criar `ios/OpenWhoop/Design/Components/SleepCard.swift`.
2. Implementar `SleepCard` como `View` com parâmetros:
   - `session: CachedSleepSession?` — sessão de sono (nil → placeholder)
   - `daily: DailyMetric?` — para efficiency se não disponível via session
3. Layout do card (background preto conforme D-12):
   - `.background(Color.black).cornerRadius(WH.Radius.card)`
   - Padding interno: `WH.Spacing.lg`
4. Conteúdo do card:
   - Header row: `Text("SLEEP").font(WH.Font.cardTitle).foregroundStyle(WH.Color.textSecondary).kerning(1.5)`
   - Stats row (2 colunas):
     - HOURS OF SLEEP: `session?.totalSleepMin.map { String(format: "%.1f hr", $0 / 60) } ?? "—"`
     - SLEEP PERFORMANCE: `daily?.efficiency.map { "\(Int($0 * 100))%" } ?? session.map { ... } ?? "—"`
   - `Divider().background(WH.Color.separator)` 
   - Hypnogram: se `session != nil && parseStages(session?.stagesJSON) != nil`:
     - `HypnogramView(session: session!)` — usando o componente existente
   - Else: `Text("No sleep data").font(WH.Font.caption).foregroundStyle(WH.Color.textSecondary)`
5. Calcular duração: `session?.totalSleepMin` (em minutos, converter para horas com 1 decimal). Se `totalSleepMin == nil`, mostrar `"—"`.
6. Calcular eficiência: preferir `daily?.efficiency` (0-1 fraction → display %). Fallback: se `session?.totalSleepMin != nil && session?.totalTimeInBedMin != nil && session!.totalTimeInBedMin > 0`, calcular `totalSleepMin / totalTimeInBedMin`. Else `"—"`.
7. Adicionar `#Preview` com mock session de exemplo.
</action>

<acceptance_criteria>
- `ios/OpenWhoop/Design/Components/SleepCard.swift` existe
- `SleepCard(session: CachedSleepSession?, daily: DailyMetric?)` aceita ambos como optional
- Quando `session == nil`: placeholder `"—"` em todos os campos, sem `HypnogramView`
- Quando `session != nil` com `stagesJSON` válido: `HypnogramView` renderizado dentro do card
- HOURS OF SLEEP mostra duração em horas (ex: "7.2 hr")
- SLEEP PERFORMANCE mostra percentagem (ex: "84%") ou `"—"`
- `.background(Color.black)` no container do card
- `xcodebuild build` passa sem erros
</acceptance_criteria>
</task>

<task id="09-04-T2">
<type>execute</type>
<title>Integrar SleepCard na SleepView como card de destaque</title>

<read_first>
- `ios/OpenWhoop/Tabs/SleepView.swift` — scrollContent actual, como `detail` é carregado e usado
- `ios/OpenWhoop/Design/Components/SleepCard.swift` — componente recém-criado
- `.planning/phases/09-swiftui-redesign-whoop-style/09-CONTEXT.md` — D-07
</read_first>

<action>
1. Em `SleepView.swift`, localizar o `scrollContent` (o `var scrollContent: some View`).
2. No topo do ScrollView/VStack do `scrollContent`, após o `ScreenHeader("Sleep")`, substituir ou envolver o conteúdo principal existente com `SleepCard(session: detail?.session, daily: detail?.daily)`.
3. Manter os outros elementos da SleepView abaixo do SleepCard:
   - SevenNightChart ou gráfico de 7 noites (se existir)
   - AlarmView button / summary (se existia)
4. O `SleepCard` deve ser o primeiro item de conteúdo visível após o header, em fullWidth no ScrollView.
5. Remover código de duração/eficiência/hypnogram que foi movido para `SleepCard` (para evitar duplicação), mas preservar qualquer lógica única do `SleepView`.
</action>

<acceptance_criteria>
- `SleepView.swift` referencia `SleepCard`
- `SleepCard(session: detail?.session, daily: detail?.daily)` está no topo do scrollContent
- Não há duplicação de duração/eficiência/hypnogram entre SleepView e SleepCard
- `xcodebuild build` passa sem erros
- No simulador: SleepView mostra SleepCard com hypnogram quando há dados reais de Phase 7
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `test -f ios/OpenWhoop/Design/Components/SleepCard.swift` — deve existir
2. `grep -n "SleepCard" ios/OpenWhoop/Tabs/SleepView.swift` — deve aparecer no scrollContent
3. `grep -n "HypnogramView" ios/OpenWhoop/Design/Components/SleepCard.swift` — usado internamente
4. `grep -n "Color.black" ios/OpenWhoop/Design/Components/SleepCard.swift` — background preto
5. `grep -n '"—"' ios/OpenWhoop/Design/Components/SleepCard.swift` — placeholder presente
6. `xcodebuild build -workspace ios/OpenWhoop.xcworkspace -scheme OpenWhoop -destination 'platform=iOS Simulator,name=iPhone 16'` — sem erros
</verification>

<success_criteria>
- [ ] `SleepCard.swift` criado com HypnogramView integrada
- [ ] Mostra HOURS OF SLEEP e SLEEP PERFORMANCE de dados reais
- [ ] Placeholder `"—"` quando session é nil
- [ ] `.background(Color.black)` no card
- [ ] `SleepView` usa `SleepCard` no topo do scroll
- [ ] UI-04 coberto: stacked bar com fases, duração, eficiência de dados reais
</success_criteria>

<must_haves>
<truths>
  - `SleepCard` reutiliza `HypnogramView` existente (não duplica a lógica)
  - Dados de `CachedSleepSession` de Phase 7 são o source of truth
  - Placeholder `"—"` para campos nil
  - UI-04 coberto
</truths>
</must_haves>

<threat_model>
- Componente de UI sem acesso directo a rede ou dados sensíveis
- `CachedSleepSession` vem do store local GRDB (Phase 7) — sem risco de exposição
- Sem novas permissões
</threat_model>
