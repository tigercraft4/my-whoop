---
phase: 11
plan: "11-04"
title: "Tests + Debug Reset + VERIFICATION.md (HK-03 Deferred)"
wave: 3
depends_on:
  - "11-02"
  - "11-03"
files_modified:
  - ios/OpenWhoopTests/HealthKitExporterTests.swift
  - ios/OpenWhoop/Settings/SettingsView.swift
  - .planning/phases/11-healthkit-export/11-VERIFICATION.md
autonomous: true
requirements:
  - HK-01
  - HK-02
  - HK-03
  - HK-04
  - HK-05
---

<objective>
Write unit tests for HealthKitExporter (cursor advancement, sleep stage mapping, HK-03 absence), add HK cursor reset to the existing SettingsView #if DEBUG section, and author the phase VERIFICATION.md explicitly marking HK-03 as `deferred — PROTO-11 HYPOTHESIS`.
</objective>

<threat_model>
- LOW: Tests using a real HKHealthStore will fail in CI (simulator has no HealthKit). Tests must mock via a protocol or skip on `!HKHealthStore.isHealthDataAvailable()`.
</threat_model>

<tasks>

  <task id="11-04-T1">
    <title>Write HealthKitExporterTests covering cursor advancement, stage mapping, and HK-03 absence</title>
    <type>execute</type>
    <read_first>
      - ios/OpenWhoopTests/UploaderTests.swift (understand test structure, StubURLProtocol pattern, XCTest conventions used in this project)
      - ios/OpenWhoop/HealthKit/HealthKitExporter.swift (understand what to test — cursor keys, stage mapping, export methods)
      - Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift (CachedSleepSession fields for test data construction)
    </read_first>
    <action>
      Create ios/OpenWhoopTests/HealthKitExporterTests.swift with the following tests:

      **Test 1 — HR cursor advancement:**
      - Create a mock/stub WhoopStore with 3 hrSamples at ts 100, 200, 300
      - Set `hk.hrHighwater` to 0 in UserDefaults before test
      - Call `exportHR()` (or exercise the cursor logic directly)
      - Assert `UserDefaults.standard.double(forKey: "hk.hrHighwater")` == 300 after export
      - Teardown: remove `hk.hrHighwater` from UserDefaults

      **Test 2 — HR cursor skips already-exported samples:**
      - Set `hk.hrHighwater` to 200 in UserDefaults
      - Create WhoopStore stub with hrSamples at ts 100, 200, 300
      - Verify only ts=300 sample would be queried (since > 200)
      - Use `hrSamples(deviceId:since:limit:)` directly on a test WhoopStore to confirm the GRDB query filter

      **Test 3 — Sleep stage mapping:**
      - Given stagesJSON: `[{"start":0,"end":3600,"stage":"light"},{"start":3600,"end":7200,"stage":"deep"},{"start":7200,"end":9000,"stage":"rem"},{"start":9000,"end":10800,"stage":"awake"}]`
      - Assert mapped HKCategoryValueSleepAnalysis values: `.asleepCore`, `.asleepDeep`, `.asleepREM`, `.awake`
      - Assert unknown stage "unknown_stage" is skipped (produces 0 HK samples for that segment)
      - This test can be a pure unit test of the stage-mapping function extracted from HealthKitExporter

      **Test 4 — HK-03 absent from HealthKitExporter:**
      - Assert that HealthKitExporter.swift does NOT contain any reference to `oxygenSaturation` or `spo2`
      - Implementation: read the HealthKitExporter.swift source file in the test and XCTAssertFalse it contains those strings (or implement as a compile-time check via absence of the symbol)
      - Alternative simpler: add a code comment `// HK-03 OMITTED: spo2/oxygenSaturation not implemented per D-02` and assert the export() method does not call any spo2-related HealthKit type

      **Guard:** All tests that touch HKHealthStore must guard `guard HKHealthStore.isHealthDataAvailable() else { return }` to pass in CI/simulator.
    </action>
    <acceptance_criteria>
      - ios/OpenWhoopTests/HealthKitExporterTests.swift exists with at least 3 test methods
      - Test for cursor advancement (hk.hrHighwater updated to last sample ts) present
      - Test for cursor filtering (since > highwater filters older samples) present
      - Test for sleep stage mapping covering .asleepCore, .asleepDeep, .asleepREM, .awake present
      - All tests pass in simulator (those requiring HealthKit skip via isHealthDataAvailable guard)
      - `xcodebuild test -scheme OpenWhoop -destination "platform=iOS Simulator,name=iPhone 16"` exits 0 (or equivalent)
    </acceptance_criteria>
  </task>

  <task id="11-04-T2">
    <title>Add HK cursor reset to SettingsView #if DEBUG section</title>
    <type>execute</type>
    <read_first>
      - ios/OpenWhoop/Settings/SettingsView.swift (find the existing `#if DEBUG` section and `debugSection` view — understand current buttons and structure)
      - ios/OpenWhoop/HealthKit/HealthKitExporter.swift (confirm exact key strings: "   </read_first>
    <action>
      In ios/OpenWhoop/Settings/SettingsView.swift, inside the existing `#if DEBUG` / `debugSection` computed property, add a new button:

      ```swift
      Button("Reset HealthKit Cursors") {
          UserDefaults.standard.removeObject(forKey: "hk.hrHighwater")
          UserDefaults.standard.removeObject(forKey: "hk.hrvHighwater")
      }
      ```

      Place after existing debug buttons (IMU Mode toggle, etc.) following the same List/Section style already used. Label can be "Reset HealthKit Cursors" or similar — consistent with existing button naming in that section.
    </action>
    <acceptance_criteria>
      - SettingsView.swift contains `removeObject(forKey: "hk.hrHighwater")` inside `#if DEBUG` block
      - SettingsView.swift contains `removeObject(forKey: "hk.hrvHighwater")` inside `#if DEBUG` block
      - The reset buttons are inside the existing debugSection (not a new unconditional section)
      - App builds without errors
      - `grep -A 5 "hk.hrHighwater" ios/OpenWhoop/Settings/SettingsView.swift` → shows the removeObject call
    </acceptance_criteria>
  </task>

  <task id="11-04-T3">
    <title>Author 11-VERIFICATION.md with HK-03 explicitly deferred</title>
    <type>execute</type>
    <read_first>
      - .planning/ROADMAP.md §"Phase 11" (6 success criteria — must address each one)
      - .planning/REQUIREMENTS.md (HK-01 through HK-05 criteria)
      - .planning/phases/11-healthkit-export/11-CONTEXT.md (D-02: HK-03 deferred, PROTO-11 HYPOTHESIS language)
    </read_first>
    <action>
      Create .planning/phases/11-healthkit-export/11-VERIFICATION.md with the following structure:

      ```markdown
      # Phase 11: HealthKit Export — Verification

      **Status:** Pending execution
      **Phase:** 11
      **Date:** (fill at execution time)

      ## Success Criteria Verification

      ### SC-1: HealthKit capability + plist keys
      - [ ] com.apple.developer.healthkit in ios/project.yml capabilities
      - [ ] NSHealthShareUsageDescription in ios/project.yml info.properties
      - [ ] NSHealthUpdateUsageDescription in ios/project.yml info.properties
      - [ ] Regenerated .xcodeproj via xcodegen generate

      ### SC-2: HR visible in Apple Health (HK-01)
      - [ ] HKQuantityType(.heartRate) samples written after export()
      - [ ] Unit: count/min
      - [ ] hk.hrHighwater advances after each export run

      ### SC-3: HRV visible in Apple Health (HK-02)
      - [ ] HKQuantityType(.heartRateVariabilitySDNN) samples written per sleep session
      - [ ] Value source: CachedSleepSession.avgHrv (RMSSD in ms)
      - [ ] One SDNN sample per sleep session

      ### SC-4: Sleep sessions visible in Apple Health (HK-04)
      - [ ] HKCategoryType(.sleepAnalysis) samples written with correct stages
      - [ ] Stage mapping: light/core→.asleepCore, deep→.asleepDeep, rem→.asleepREM, awake→.awake
      - [ ] Delete+reinsert prevents overlapping samples

      ### SC-5: SpO₂ export — HK-03 DEFERRED
      **Status: DEFERRED — PROTO-11 is HYPOTHESIS**

      HK-03 (SpO₂ export via HKQuantityType(.oxygenSaturation)) is intentionally omitted from
      this phase. PROTO-11 (SpO₂ capture from WHOOP 5.0 hardware) has not been VERIFIED against
      a ground-truth oximeter. Implementing HK-03 before PROTO-11 is VERIFIED would export
      unvalidated or zero values to Apple Health.

      SpO₂ export will be implemented as an incremental phase when PROTO-11 status transitions
      from HYPOTHESIS to VERIFIED in a future capture session.

      Verification: `grep -r "oxygenSaturation\|spo2\|SpO2" ios/OpenWhoop/HealthKit/` → 0 matches

      ### SC-6: Lazy authorization + graceful degradation (HK-05)
      - [ ] HealthKit auth requested in TodayView.task, not at app launch
      - [ ] guard metrics.today != nil before auth call
      - [ ] HKHealthStore.isHealthDataAvailable() guard prevents simulator crash
      - [ ] "Health not connected" banner shown once on denial (hk.authDeniedShown)
      - [ ] App functions normally with HealthKit denied

      ## Requirements Coverage

      | Req ID | Status | Notes |
      |--------|--------|-------|
      | HK-01  | Covered | Plan 11-02: HR export with highwater cursor |
      | HK-02  | Covered | Plan 11-02: HRV export via avgHrv per sleep session |
      | HK-03  | DEFERRED | PROTO-11 HYPOTHESIS — no SpO₂ code in this phase |
      | HK-04  | Covered | Plan 11-02: Sleep export with delete+reinsert + stage mapping |
      | HK-05  | Covered | Plan 11-03: Lazy auth in TodayView.task, graceful degradation |

      ## HK-P1 Invariant Check
      - [ ] `grep -r "import HealthKit" ios/` shows only ios/OpenWhoop/HealthKit/HealthKitExporter.swift (and any sub-files in HealthKit/)
      - [ ] Plan 11-01 (project.yml) was completed in Wave 1 before any Swift HK code was written
      ```
    </action>
    <acceptance_criteria>
      - .planning/phases/11-healthkit-export/11-VERIFICATION.md exists
      - File contains "HK-03" and "DEFERRED" and "PROTO-11 HYPOTHESIS" in the SC-5 section
      - File contains requirements coverage table with HK-01 through HK-05
      - HK-03 row in the table shows "DEFERRED"
      - File contains HK-P1 invariant check
    </acceptance_criteria>
  </task>

</tasks>

<verification>
1. `ls ios/OpenWhoopTests/HealthKitExporterTests.swift` → file exists
2. Unit tests pass: `xcodebuild test -scheme OpenWhoop -destination "platform=iOS Simulator,name=iPhone 16"` exits 0
3. `grep "hk.hrHighwater" ios/OpenWhoop/Settings/SettingsView.swift` → match in #if DEBUG section
4. `grep "hk.hrvHighwater" ios/OpenWhoop/Settings/SettingsView.swift` → match in #if DEBUG section
5. `ls .planning/phases/11-healthkit-export/11-VERIFICATION.md` → file exists
6. `grep "DEFERRED" .planning/phases/11-healthkit-export/11-VERIFICATION.md` → match (HK-03 deferred)
7. `grep "PROTO-11 HYPOTHESIS" .planning/phases/11-healthkit-export/11-VERIFICATION.md` → match
8. `grep "oxygenSaturation\|spo2\|SpO2" ios/OpenWhoop/HealthKit/HealthKitExporter.swift` → 0 matches (final confirmation of HK-03 absence)
</verification>

<success_criteria>
- [ ] HealthKitExporterTests with cursor, stage-mapping, and HK-03-absence tests — HK-01/02/04/03
- [ ] Debug cursor reset in SettingsView #if DEBUG — usability for development
- [ ] VERIFICATION.md authored with HK-03 explicitly DEFERRED (PROTO-11 HYPOTHESIS language) — D-02
- [ ] All existing tests still pass (no regressions)
</success_criteria>

<must_haves>
  <truths>
    - HK-03 DEFERRED wording in VERIFICATION.md is non-negotiable: must say "PROTO-11 HYPOTHESIS" exactly as in STATE.md and CONTEXT.md
    - Debug reset buttons use exact key strings "hk.hrHighwater" and "hk.hrvHighwater" — no typos
    - Tests skip gracefully in simulator via HKHealthStore.isHealthDataAvailable() guard (no XCTFail in CI)
    - VERIFICATION.md is a planning artifact, not executable code — it documents what must be true after execution
  </truths>
</must_haves>
