---
phase: 02-gatt-survey-bonding
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml
  - FINDINGS_5.md
  - re/survey_5/README.md
autonomous: false
requirements: [PROTO-01, PROTO-02, PROTO-03]
user_setup: []

must_haves:
  truths:
    - "Phase 2 GATT-survey evidence is committed following the D-02 redacted-hex + SHA256 + meta.yaml pattern, with no device identifiers or key material"
    - "FINDINGS_5.md sections 3 (bonding) and 4 (standard characteristics) are completed with the Wave 3 outcomes"
    - "All four ROADMAP Phase 2 success criteria are documented in FINDINGS_5.md"
  artifacts:
    - path: "re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml"
      provides: "committed evidence sidecar: UUID list, handle map, legacy verdict, bond outcome"
      contains: "custom_service_uuid"
    - path: "FINDINGS_5.md"
      provides: "completed Phase 2 protocol-survey reference"
      contains: "## 3. Bonding"
      min_lines: 60
    - path: "re/survey_5/README.md"
      provides: "index linking the survey_5 scripts to the capture/bonding runbooks"
      contains: "survey_gatt_5.py"
  key_links:
    - from: "FINDINGS_5.md ## 3. Bonding"
      to: "re/survey_5/bond_5.py outcome"
      via: "Wave 3 bond result transcription"
      pattern: "bond"
    - from: "re/survey_5/README.md"
      to: "re/capture/ios-packetlogger.md"
      via: "runbook link"
      pattern: "ios-packetlogger"
---

<objective>
Close out Phase 2: commit the GATT-survey evidence under the established D-02 policy, complete the remaining `FINDINGS_5.md` sections with the Wave 3 bonding and HR/battery outcomes, document all four ROADMAP success criteria, and add a `re/survey_5/README.md` index linking the scripts to the capture and bonding runbooks.

Purpose: The phase is only "done" when the evidence is committed (redacted, no key material), `FINDINGS_5.md` is the canonical Phase 2 reference (consumed by Phase 3 framing work), and a developer can navigate `re/survey_5/` without re-reading the planning docs.

Output: committed evidence sidecar + completed `FINDINGS_5.md` + `re/survey_5/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gatt-survey-bonding/02-RESEARCH.md
@re/capture/evidence/2026-05-30-ios.meta.yaml
@re/README.md
@FINDINGS_5.md
@.planning/phases/02-gatt-survey-bonding/02-01-SUMMARY.md
@.planning/phases/02-gatt-survey-bonding/02-02-SUMMARY.md
@.planning/phases/02-gatt-survey-bonding/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the GATT-survey evidence sidecar (D-02 policy)</name>
  <files>re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml</files>
  <read_first>
    - re/capture/evidence/2026-05-30-ios.meta.yaml (Phase 1 evidence sidecar — mirror its YAML structure: source/tool/captured/handles/notes)
    - .planning/phases/02-gatt-survey-bonding/02-RESEARCH.md (§Evidence Policy table — what commits vs stays local; §Recommended Project Structure evidence block; §Common Pitfalls 5 — scrub BD_ADDR/keys)
    - .planning/phases/02-gatt-survey-bonding/02-01-SUMMARY.md and 02-03-SUMMARY.md (confirmed UUIDs, handle map, bond outcome to record)
  </read_first>
  <action>Create `re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml` mirroring the structure of `re/capture/evidence/2026-05-30-ios.meta.yaml` (RESEARCH §Evidence Policy). Required keys: `source` (e.g. "macOS Bleak + iPhone nRF Connect"), `tool`, `captured` (date), `custom_service_uuid` (the confirmed 5.0 service UUID from 02-01-SUMMARY), a `characteristic_uuids` map (cmd-in/cmd-resp/events/data/diagnostics/HR/battery to UUID), a `handle_uuid_map` resolving 0x099b/0x099d/0x09a3 to UUIDs, `legacy_61080001_verdict` ("present"/"absent"), `bond_outcome` (from 02-03-SUMMARY), `hr_battery_confirmed` (yes/no with sample values), and a `notes` field. If a fallback PacketLogger SMP capture was used in Wave 3, ALSO create the scrubbed `.hex` excerpt and `.sha256` of the raw capture per the evidence table — and scrub BD_ADDR (6-byte MAC) plus LTK/IRK/CSRK from the .hex (Pitfall 5, DISCLAIMER §2). Do NOT commit raw `.pklg` (gitignored under re/capture/samples/) or nRF Connect screenshots (local only). The meta.yaml itself must contain NO real device serial, MAC, or macOS BLE UUID — use `[REDACTED]` for the device identity.</action>
  <verify>
    <automated>test -f re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml && grep -Eq 'custom_service_uuid|characteristic_uuids' re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml && grep -Eq 'present|absent' re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml && ! grep -iE '(long_term_key|identity_resolving_key)' re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml` exists
    - Contains `custom_service_uuid` and a `characteristic_uuids` map
    - Contains an explicit `legacy_61080001_verdict` of `present` or `absent`
    - Contains `bond_outcome` and `hr_battery_confirmed` keys
    - Contains NO real MAC: `grep -iE '[0-9A-F]{2}(:[0-9A-F]{2}){5}' ...meta.yaml` matches only `XX:XX...` placeholders, not a real MAC
    - No `long_term_key`/`identity_resolving_key`/`csrk` strings in any committed evidence file for this date
  </acceptance_criteria>
  <done>The Phase 2 GATT-survey evidence is committed per D-02 (UUID list, handle map, legacy verdict, bond outcome) with all device identifiers and key material scrubbed.</done>
</task>

<task type="auto">
  <name>Task 2: Complete FINDINGS_5.md sections 3-4 and document all four success criteria</name>
  <files>FINDINGS_5.md</files>
  <read_first>
    - FINDINGS_5.md (current state from plan 02-01 — sections 1/2/5 populated, sections 3/4 are "pending Wave 3" placeholders to fill)
    - .planning/phases/02-gatt-survey-bonding/02-03-SUMMARY.md (bond outcome, sample battery %, sample HR BPM)
    - .planning/ROADMAP.md (§Phase 2 — the four success criteria to map)
    - .planning/phases/02-gatt-survey-bonding/02-RESEARCH.md (§FINDINGS_5.md Structure)
  </read_first>
  <action>Update `FINDINGS_5.md`: replace the "pending Wave 3" placeholders in `## 3. Bonding` (record the confirmed-write outcome, or the SMP-capture fallback result, from 02-03-SUMMARY — D-05) and `## 4. Standard Characteristics` (HR subscription yes/no + sample BPM; battery read yes/no + sample %). Update the `## Status at a glance` table so each capability row reflects its final state. Add a `## Phase 2 Success Criteria` section that explicitly maps each of the four ROADMAP Phase 2 criteria to its evidence: (1) GATT services + 7 characteristics enumerated (point to section 1 + evidence meta.yaml), (2) legacy 61080001 verdict (point to section 2), (3) bonding without official app + SMP visible (point to section 3), (4) live HR via Bleak (point to section 4). Keep the `[REDACTED]` device identity in the header — no raw identifiers.</action>
  <verify>
    <automated>grep -q '^## 3. Bonding' FINDINGS_5.md && grep -q '^## 4. Standard Characteristics' FINDINGS_5.md && grep -qi 'success criteria' FINDINGS_5.md && test $(wc -l < FINDINGS_5.md) -ge 60 && ! grep -iE '[0-9A-F]{2}(:[0-9A-F]{2}){5}' FINDINGS_5.md</automated>
  </verify>
  <acceptance_criteria>
    - `FINDINGS_5.md` sections `## 3. Bonding` and `## 4. Standard Characteristics` no longer contain a "pending" placeholder
    - A `## Phase 2 Success Criteria` (case-insensitive) section exists mapping all four ROADMAP criteria to evidence
    - Section 3 records the bond mechanism that worked (confirmed-write or SMP fallback)
    - Section 4 records a sample HR BPM and battery %
    - File is at least 60 lines and contains no raw MAC address
  </acceptance_criteria>
  <done>`FINDINGS_5.md` is the complete Phase 2 reference: GATT map, legacy verdict, bonding outcome, HR/battery confirmation, and an explicit success-criteria map.</done>
</task>

<task type="auto">
  <name>Task 3: Add re/survey_5/README.md index</name>
  <files>re/survey_5/README.md</files>
  <read_first>
    - re/README.md (existing 4.0 RE scripts index — mirror its concise style)
    - re/capture/ios-packetlogger.md (the bonding-fallback runbook to link)
    - .planning/phases/02-gatt-survey-bonding/02-CONTEXT.md (D-04 — survey_5 isolation rationale)
  </read_first>
  <action>Create `re/survey_5/README.md` indexing the Phase 2 5.0 survey workspace. Include: a one-line purpose (isolated WHOOP 5.0 in-discovery scripts per D-04); the venv setup commands (`python3.11 -m venv re/survey_5/.venv`, activate, `pip install -r requirements.txt`); a one-line description of each script (`survey_gatt_5.py` — GATT enumeration + handle cross-check; `bond_5.py` — confirmed-write bonding trigger; `hr_5.py` — HR notify + battery read); a note that `device_local_5.py` must be created from `device_local_5.example.py` and is gitignored; and links to the runbooks `re/capture/ios-packetlogger.md` (bonding SMP fallback) and `re/capture/wireshark.md` (tshark btsmp analysis). Point readers to `FINDINGS_5.md` as the canonical findings document.</action>
  <verify>
    <automated>test -f re/survey_5/README.md && grep -q 'survey_gatt_5.py' re/survey_5/README.md && grep -q 'bond_5.py' re/survey_5/README.md && grep -q 'hr_5.py' re/survey_5/README.md && grep -q 'ios-packetlogger' re/survey_5/README.md && grep -q 'device_local_5.example.py' re/survey_5/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `re/survey_5/README.md` exists
    - References all three scripts: `survey_gatt_5.py`, `bond_5.py`, `hr_5.py`
    - Links `re/capture/ios-packetlogger.md` and references `device_local_5.example.py`
    - Includes venv setup commands and points to `FINDINGS_5.md`
  </acceptance_criteria>
  <done>`re/survey_5/README.md` lets a developer set up the venv, understand each script, and find the bonding runbooks without reading the planning docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 3 capture artifacts -> committed evidence | device identifiers + SMP key material must not cross into git |
| FINDINGS_5.md -> public repo | findings doc is committed; must contain no raw identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information Disclosure | committed evidence meta.yaml / hex | mitigate | `[REDACTED]` device identity; acceptance criteria grep for raw MAC + LTK/IRK/CSRK and fail if present (D-02, DISCLAIMER §2, Pitfall 5) |
| T-02-11 | Information Disclosure | FINDINGS_5.md committed | mitigate | Header kept `[REDACTED]`; acceptance criterion greps for raw MAC and fails build if present |
| T-02-12 | Information Disclosure | raw .pklg / nRF Connect screenshots | accept | Kept LOCAL only (gitignored re/capture/samples/ + local screenshots); never committed, no in-repo control needed |
</threat_model>

<verification>
- Evidence meta.yaml committed with UUID list, handle map, legacy verdict, bond outcome — no identifiers/keys
- FINDINGS_5.md sections 3/4 completed; all four success criteria mapped to evidence
- re/survey_5/README.md indexes scripts + runbooks
- No raw MAC / key material in any committed Phase 2 file
</verification>

<success_criteria>
- All four ROADMAP Phase 2 success criteria documented and evidenced in FINDINGS_5.md (PROTO-01/02/03)
- Evidence committed under D-02 policy
- survey_5 workspace is navigable via README
</success_criteria>

<output>
Create `.planning/phases/02-gatt-survey-bonding/02-04-SUMMARY.md` when done. Confirm all four success criteria are met (or note any that fell back to the SMP path) so the phase verifier can close Phase 2.
</output>
