---
phase: 04-protocol-decode-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - re/capture/evidence/2026-05-30-biometric-5.meta.yaml
autonomous: false
requirements: [PROTO-16, PROTO-07, PROTO-11, PROTO-12, PROTO-13, PROTO-14]
user_setup:
  - service: "iPhone + PacketLogger + worn WHOOP 5.0 strap"
    why: "macOS Bleak cannot bond to the 5.0 strap (Phase 2 finding). The only path to realtime HR/RR, raw IMU, and possible SpO2/temp/respiration streams is a live PacketLogger capture during a WHOOP-app session on the bonded iPhone."
    dashboard_config:
      - task: "Run a targeted capture session per re/capture/ios-packetlogger.md (D-05): 5-10 min realtime HR/RR streaming with strap worn, then navigate to sleep review (historical backfill), workout history, and the events tab; screen-record the iPhone simultaneously for ground-truth alignment (D-08)"
        location: "iPhone (PacketLogger on tethered Mac) — output .pklg saved to re/capture/samples/ (gitignored)"
must_haves:
  truths:
    - "A new PacketLogger capture exists in re/capture/samples/ containing a realtime-HR session, sleep review, workout history, and events-tab activity (D-05)"
    - "The capture's firmware revision (WG50_r52, Device Info 0x2A26/0x2A27) is recorded in the evidence sidecar (PROTO-16)"
    - "A redacted evidence sidecar documents the capture with device_identity REDACTED and raw pklg listed as local-only"
  artifacts:
    - path: "re/capture/evidence/2026-05-30-biometric-5.meta.yaml"
      provides: "Redacted evidence sidecar for the D-05 targeted biometric capture, with firmware_revision (PROTO-16)"
      contains: "firmware_revision"
  key_links:
    - from: "re/capture/evidence/2026-05-30-biometric-5.meta.yaml"
      to: "re/capture/samples/ (gitignored .pklg)"
      via: "raw_artifacts_local_only listing"
      pattern: "raw_artifacts_local_only"
---

<objective>
Run the D-05 targeted PacketLogger capture — the single gating dependency for all four biometric-stream requirements (PROTO-07 HR/RR, PROTO-11 SpO₂, PROTO-12 skin temp, PROTO-13 respiration, PROTO-14 IMU). The existing two captures are bonding/command sessions and lack realtime sensor streaming; this capture deliberately triggers realtime HR/RR streaming, a sleep-review historical backfill, workout history, and the events tab in one session to avoid 3-4 mid-phase interruptions.

Claude automates everything it can (verifies PacketLogger profile is active, extracts and reports the captured frame inventory, drafts the evidence sidecar with firmware revision). The capture session itself is a human-only physical action (worn strap + on-device app navigation) — this is why the plan is `autonomous: false`.

Purpose: Produce the biometric-stream corpus that Wave 3 (Plan 04) decodes. Without this capture, PROTO-07/11/12/13/14 cannot reach VERIFIED (RESEARCH: NO fallback).
Output: A new `.pklg` in `re/capture/samples/` (gitignored, local-only) + redacted evidence sidecar `re/capture/evidence/2026-05-30-biometric-5.meta.yaml` recording firmware revision (PROTO-16).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-decode-schema/04-CONTEXT.md
@.planning/phases/04-protocol-decode-schema/04-RESEARCH.md
@.planning/phases/04-protocol-decode-schema/04-PATTERNS.md
@re/capture/ios-packetlogger.md
@re/capture/evidence/2026-05-30-framing-5.meta.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-capture readiness check + HR-monitor helper ready</name>
  <files>re/capture/evidence/2026-05-30-biometric-5.meta.yaml</files>
  <read_first>
    - re/capture/ios-packetlogger.md (the PacketLogger runbook — the steps the human will follow for D-05)
    - re/survey_5/hr_5.py (standard HR streaming via Bleak, unbonded — can run alongside the capture to log HR for ground-truth alignment, D-08)
    - re/capture/evidence/2026-05-30-framing-5.meta.yaml (the sidecar template: source/tool/tool_version/captured/device_identity/results/verdict/raw_artifacts_local_only/notes keys, redaction policy)
  </read_first>
  <action>
    Confirm capture readiness before the human session: verify `tshark` is on PATH and runs (`tshark --version`), verify `re/capture/samples/` exists and is gitignored (`git check-ignore re/capture/samples/.gitkeep` exits 0), and confirm `re/capture/ios-packetlogger.md` documents the PacketLogger mobileconfig requirement (TOOL-01). If `re/survey_5/hr_5.py` is runnable, note that it can stream standard 0x2A37 HR unbonded during the session for D-08 ground-truth alignment (do NOT block on it — it is a convenience, not a requirement).

    Draft (but do not yet fill capture results into) the evidence sidecar `re/capture/evidence/2026-05-30-biometric-5.meta.yaml` mirroring the framing-5 sidecar structure: `source`, `tool: "PacketLogger + tshark 4.6.6"`, `tool_version`, `captured: "2026-05-30"`, `device_identity: "[REDACTED]"`, a `firmware_revision: "WG50_r52"` key with `source: "Device Information 0x2A26 (Firmware Revision) / 0x2A27 (Hardware Revision)"` (PROTO-16), an empty `results:` block to be filled after capture (placeholders for frames_by_characteristic, realtime_hr_present, raw_imu_present, sleep_review_present), `raw_artifacts_local_only:` listing the new pklg path, and a `notes:` block restating DISCLAIMER §2 (no BD_ADDR/serial/SMP keys/UUID committed — protocol-structure facts only).
  </action>
  <verify>
    <automated>tshark --version | head -1 && git check-ignore re/capture/samples/.gitkeep && grep -c "firmware_revision" re/capture/evidence/2026-05-30-biometric-5.meta.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `tshark --version` runs and reports a version (readiness confirmed)
    - `git check-ignore re/capture/samples/.gitkeep` exits 0 (samples dir gitignored)
    - `re/capture/evidence/2026-05-30-biometric-5.meta.yaml` contains `firmware_revision` and `device_identity: "[REDACTED]"`
    - The sidecar contains a `raw_artifacts_local_only:` key and a `notes:` block referencing DISCLAIMER
  </acceptance_criteria>
  <done>Capture environment verified ready; redacted biometric-capture sidecar drafted with firmware_revision (PROTO-16) and an empty results block awaiting the human capture session.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Pre-capture readiness verified and the evidence sidecar drafted. The targeted biometric capture itself requires a worn strap + on-device WHOOP-app navigation — macOS cannot bond to the strap, so this is a human-only physical action.</what-built>
  <how-to-verify>
    1. Tether the iPhone to the Mac and launch PacketLogger (confirm `iOSBluetoothLogging.mobileconfig` is installed per re/capture/ios-packetlogger.md).
    2. Wear the WHOOP 5.0 strap. Open the official WHOOP app.
    3. Trigger realtime HR/RR streaming for 5-10 minutes (Live HR view). Simultaneously screen-record the iPhone so decoded values can be aligned to the app display (D-08 ground truth).
    4. Navigate to sleep review (forces a historical backfill), then workout history, then the events tab.
    5. Stop the capture; save the `.pklg` into `re/capture/samples/` (it stays gitignored — never commit the raw file).
    6. Note the Device Info Firmware Revision (0x2A26) / Hardware Revision (0x2A27) shown in nRF Connect or the capture for the sidecar (PROTO-16).
    7. Reply with the saved `.pklg` filename so Claude can extract and report the frame inventory and finalise the sidecar results.
  </how-to-verify>
  <resume-signal>Reply with the saved .pklg filename (in re/capture/samples/) and the firmware/hardware revision, or describe any capture problem.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extract + report captured biometric frame inventory, finalise sidecar</name>
  <files>re/capture/evidence/2026-05-30-biometric-5.meta.yaml</files>
  <read_first>
    - re/survey_5/validate_frames_5.py (extract_frames + build_report — point DEFAULT_CAPTURES or pass the new pklg path to get per-characteristic counts)
    - re/survey_5/decode_5.py (the Plan 01 decoder — run parse_body_5 over the new capture to detect REALTIME_DATA type 40 / REALTIME_RAW_DATA type 43 presence)
    - re/capture/evidence/2026-05-30-biometric-5.meta.yaml (the drafted sidecar from Task 1 — fill the results block)
  </read_first>
  <action>
    Run the extraction pipeline (`validate_frames_5.build_report([<new pklg>])` and `decode_5.parse_body_5` per frame) over the human-supplied capture. Report the per-characteristic frame counts and the distinct PacketTypes observed. Specifically flag whether REALTIME_DATA (type 40), REALTIME_RAW_DATA (type 43), and any sleep-review historical (non-console) packets are present — these are the Wave 3 inputs. If type 40 / type 43 are absent, record that honestly (standard 0x2A37 still satisfies PROTO-07; raw IMU may then be unavailable — flag as a Wave 3 risk).

    Finalise `re/capture/evidence/2026-05-30-biometric-5.meta.yaml`: fill the `results:` block with `frames_by_characteristic` (handle → uuid → count), `realtime_hr_present`, `raw_imu_present`, `sleep_review_present` (true/false from the inventory), and confirm `firmware_revision: "WG50_r52"` (or the actual revision the human reported — PROTO-16). Keep `device_identity: "[REDACTED]"`; list the raw pklg under `raw_artifacts_local_only`. Do NOT commit the raw pklg; do NOT include any BD_ADDR/serial/key bytes.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('re/capture/evidence/2026-05-30-biometric-5.meta.yaml')); assert 'results' in d and 'firmware_revision' in d and d.get('device_identity')=='[REDACTED]'; print('sidecar ok:', list(d['results'].keys()))"</automated>
  </verify>
  <acceptance_criteria>
    - The sidecar's `results:` block contains `frames_by_characteristic`, `realtime_hr_present`, `raw_imu_present`, and `sleep_review_present` keys
    - `firmware_revision` is present and `device_identity` is `[REDACTED]` (the verify one-liner's assertion passes)
    - The new `.pklg` is listed under `raw_artifacts_local_only` and `git check-ignore` on it exits 0 (not committed)
    - A frame inventory was produced reporting which of type 40 / type 43 / sleep-review packets are present (recorded in the sidecar or SUMMARY)
  </acceptance_criteria>
  <done>The D-05 capture is extracted and inventoried; the redacted sidecar records firmware revision and the presence/absence of realtime-HR (type 40), raw-IMU (type 43), and sleep-review packets — the precise inputs (and risks) for Wave 3 biometric decode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live BLE session → pklg file | Decrypted ATT traffic captured to disk; contains identifiable MAC/serial — must stay local |
| pklg → committed evidence sidecar | Risk of leaking device identity into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | raw .pklg contains BD_ADDR / serial / CoreBluetooth UUID | mitigate | Raw .pklg gitignored under re/capture/samples/ (git check-ignore verified); sidecar uses device_identity: "[REDACTED]"; only frame counts / firmware revision committed (DISCLAIMER §2) |
| T-04-03 | Information Disclosure | firmware/serial in sidecar | mitigate | Commit only the firmware revision string (WG50_r52, a protocol fact); no serial, no per-device identifier |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs; tshark already present, hr_5.py uses the existing venv bleak (unchanged) |
</threat_model>

<verification>
- A new `.pklg` exists in re/capture/samples/ and is gitignored (git check-ignore exits 0)
- The evidence sidecar records firmware_revision (PROTO-16) with device_identity REDACTED
- The capture's frame inventory reports realtime-HR / raw-IMU / sleep-review presence for Wave 3
</verification>

<success_criteria>
- D-05 targeted capture completed (the gating dependency for PROTO-07/11/12/13/14 is satisfied)
- Firmware revision recorded in the capture session metadata (PROTO-16)
- No device identity or raw capture committed (evidence policy upheld)
- Wave 3 knows exactly which biometric streams are present in the capture
</success_criteria>

<output>
Create `.planning/phases/04-protocol-decode-schema/04-02-SUMMARY.md` when done
</output>
