---
phase: 04-protocol-decode-schema
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - re/survey_5/command_surface_5.py
  - re/survey_5/decode_streams_5.py
autonomous: true
requirements: [PROTO-06, PROTO-08, PROTO-09, PROTO-10, PROTO-15]
must_haves:
  truths:
    - "All command IDs observed in the existing corpus (body[6] of COMMAND_RESPONSE + cmd-in writes) are enumerated and reconciled against the r52 CommandNumber map (observed vs expected)"
    - "The 14 reused 4.0 command IDs (1,2,3,7,11,14,22,26,35,81,82,106,107,145) are cross-validated as present-or-absent in the 5.0 corpus"
    - "EVENT packets (type 48) decode to r52 EventNumber names, including BATTERY_LEVEL (event 3) and STRAP_CONDITION_REPORT (event 29) with a device-epoch u32 at body[8] (PROTO-09, PROTO-15 device epoch)"
    - "Battery level is decoded (PROTO-08) from the battery event and/or GET_BATTERY_LEVEL response"
    - "The historical-offload protocol is documented from CONSOLE_LOGS + METADATA: SEND_HISTORICAL_DATA (22), HISTORICAL_DATA_RESULT ack (23), HISTORY_START/END, and the trim cursor (PROTO-10, documentation-only per D-09)"
    - "Dual-epoch model: GET_DATA_RANGE payload Unix u32 + EVENT body device-epoch u32, each tagged (PROTO-15)"
  artifacts:
    - path: "re/survey_5/command_surface_5.py"
      provides: "Observed-vs-r52 command surface enumeration + reused-14 cross-validation"
      contains: "def "
      min_lines: 50
    - path: "re/survey_5/decode_streams_5.py"
      provides: "EVENT/battery/metadata/historical-offload/dual-epoch decoders over the existing corpus"
      contains: "def "
      min_lines: 60
  key_links:
    - from: "re/survey_5/command_surface_5.py"
      to: "re/survey_5/decode_5.py"
      via: "imports parse_body_5 + r52 resolver"
      pattern: "from decode_5 import"
    - from: "re/survey_5/decode_streams_5.py"
      to: "re/survey_5/decode_5.py"
      via: "imports parse_body_5"
      pattern: "from decode_5 import"
---

<objective>
Decode everything already present in the existing 5028-frame corpus: the command surface (PROTO-06), events including battery (PROTO-08, PROTO-09), the historical-offload protocol from CONSOLE_LOGS + METADATA (PROTO-10, documentation-only), and the dual-epoch timestamp model (PROTO-15). None of this needs the D-05 capture — it is all in the bonding/command sessions already captured. This wave runs in parallel with the human D-05 capture (Plan 02).

The command surface is enumerated by capture-analysis (D-06): macOS cannot live-probe (no bond), so observed command IDs are reconciled against the r52 CommandNumber map. Unobserved-but-expected commands become HYPOTHESIS with r52 attribution (D-07) — recorded for Plan 05's schema.

Purpose: Convert the corpus's command/event/metadata/console-log content into decoded, documented protocol facts that Plan 05 folds into the schema and FINDINGS_5.md §Phase 4.
Output: `re/survey_5/command_surface_5.py` (observed-vs-r52 reconciliation), `re/survey_5/decode_streams_5.py` (event/battery/metadata/historical/dual-epoch decoders).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-decode-schema/04-CONTEXT.md
@.planning/phases/04-protocol-decode-schema/04-RESEARCH.md
@.planning/phases/04-protocol-decode-schema/04-PATTERNS.md
@.planning/phases/04-protocol-decode-schema/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: command_surface_5.py — observed-vs-r52 enumeration + reused-14 cross-validation</name>
  <files>re/survey_5/command_surface_5.py</files>
  <read_first>
    - re/survey_5/decode_5.py (import parse_body_5 + the r52 resolver; the corpus loader)
    - re/survey_5/validate_frames_5.py (build_report — to enumerate over the FULL 5028-frame corpus, not just the curated golden subset)
    - re/survey_5/frames_5_golden.json (the curated corpus with stream_type; also a quick source)
    - protocol/whoop_protocol.json (enums.CommandNumber lines 32-59 — the full r52 expected command surface)
    - .planning/phases/04-protocol-decode-schema/04-RESEARCH.md (Pattern 2 cmd-in writes role 0; PROTO-06 row; D-06/D-07)
  </read_first>
  <action>
    Create `re/survey_5/command_surface_5.py` (D-02 isolation, `from decode_5 import parse_body_5` and the r52 resolver). Enumerate the command surface over the FULL corpus (call `validate_frames_5.build_report(DEFAULT_CAPTURES)` for all records, not just the curated 46): collect every `cmd` byte (body[6]) from COMMAND_RESPONSE (type 36) and COMMAND (type 35) frames, and every request `cmd` from cmd-in write frames (role 0 on FD4B0002 — Pattern 2). Pair cmd-in requests with cmd-resp by `seq` where possible to confirm the request→response mapping (A7).

    Produce an OBSERVED-vs-EXPECTED reconciliation table: for each r52 CommandNumber, mark OBSERVED (with frame count) or UNOBSERVED. Explicitly cross-validate the 14 reused 4.0 IDs — 1 LINK_VALID, 2 GET_MAX_PROTOCOL_VERSION, 3 TOGGLE_REALTIME_HR, 7 REPORT_VERSION_INFO, 11 GET_CLOCK, 14 TOGGLE_GENERIC_HR_PROFILE, 22 SEND_HISTORICAL_DATA, 26 GET_BATTERY_LEVEL, 35 GET_HELLO_HARVARD, 81 START_RAW_DATA, 82 STOP_RAW_DATA, 106 TOGGLE_IMU_MODE, 107 ENABLE_OPTICAL_DATA, 145 GET_HELLO — reporting each as present/absent in the 5.0 corpus.

    Output a structured result (printed table + a returnable dict) that Plan 05 consumes: observed commands → VERIFIED candidates, unobserved-but-r52-expected → HYPOTHESIS with note "not observed in captures, expected from r52 enum map" (D-07). This is the capture-analysis fulfilment of PROTO-06 (D-06 — no live probe; the live 0-255 probe is deferred to Phase 5).
  </action>
  <verify>
    <automated>cd re/survey_5 && python command_surface_5.py && echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `re/survey_5/command_surface_5.py` contains `from decode_5 import` and does NOT contain `WhoopPacket` (grep -v '^#' returns 0 for WhoopPacket)
    - `cd re/survey_5 && python command_surface_5.py` exits 0
    - Output reports each of the 14 reused IDs (1,2,3,7,11,14,22,26,35,81,82,106,107,145) as OBSERVED or UNOBSERVED (output contains "GET_DATA_RANGE" or the reused command names with an observed/unobserved marker)
    - Output reports at least one OBSERVED command resolved to its r52 name (e.g. GET_DATA_RANGE cmd 34 was confirmed in research)
    - Output marks unobserved-but-expected commands as HYPOTHESIS with the r52 attribution note
  </acceptance_criteria>
  <done>command_surface_5.py enumerates the observed command IDs from the full corpus, reconciles them against the complete r52 CommandNumber map, cross-validates the 14 reused IDs, and tags unobserved commands HYPOTHESIS — fulfilling PROTO-06 via capture analysis (D-06/D-07).</done>
</task>

<task type="auto">
  <name>Task 2: decode_streams_5.py — events/battery, metadata/historical-offload, dual-epoch</name>
  <files>re/survey_5/decode_streams_5.py</files>
  <read_first>
    - re/survey_5/decode_5.py (import parse_body_5 + resolver + the dual-epoch detection from Plan 01)
    - protocol/whoop_protocol.json (enums.EventNumber lines 11-31, enums.MetadataType line 10 — r52 maps)
    - FINDINGS.md (4.0 ground truth for event payload layouts, battery event, and the historical offload store-and-forward protocol — reference for what 5.0 events should look like)
    - .planning/phases/04-protocol-decode-schema/04-RESEARCH.md (PROTO-08/09/10/15 rows; Pattern 3 historical offload; Code Examples for EVENT device-epoch at body[8] and battery event layout A6; the CONSOLE_LOGS offload narration)
    - re/survey_5/frames_5_golden.json (the curated corpus — EVENT, METADATA, CONSOLE_LOGS exemplars)
  </read_first>
  <action>
    Create `re/survey_5/decode_streams_5.py` (D-02 isolation, `from decode_5 import parse_body_5`). Decode the existing-corpus streams:

    EVENTS (PROTO-09): for every EVENT frame (type 48), resolve body[6] to an r52 EventNumber name and read the device-epoch u32 at body[8] (tag epoch=device, PROTO-15). Decode STRAP_CONDITION_REPORT (event 29) and any other observed events. For BATTERY_LEVEL (event 3, PROTO-08): decode the battery payload using the 4.0 precedent (u16 SOC×10 then u16 millivolts — A6) and, where a GET_BATTERY_LEVEL (cmd 26) COMMAND_RESPONSE is present, decode its u16÷10 SOC; cross-check against the standard 0x2A19 read value (23%) recorded in Phase 2 (validate, do not fabricate). Guard every offset (len(body) checks, D-03).

    METADATA + HISTORICAL OFFLOAD (PROTO-10, documentation-only per D-09): decode METADATA frames (type 49) — HISTORY_START (1) / HISTORY_END (2) / HISTORY_COMPLETE (3). Extract the CONSOLE_LOGS (type 50) ASCII narration of the live offload session already in the corpus ("Command Send Historical Data ...", "hist transfer start response ack", "History burst success. Trim: 0x........:........"). From these, identify and document the store-then-ack protocol: SEND_HISTORICAL_DATA (cmd 22) request, the HISTORICAL_DATA_RESULT (cmd 23) ack, the data-range command (GET_DATA_RANGE 34 / SET_READ_POINTER 33), and the trim-cursor format (the `0xXXXXXXXX:XXXXXXXX` pointer = the store-then-ack persistence cursor). Do NOT implement a live kill-process test (Phase 5, D-09) — this is documentation extraction only.

    DUAL-EPOCH (PROTO-15): decode GET_DATA_RANGE (type 36 cmd 34) payload Unix u32-LE timestamps (tag epoch=unix) and the EVENT device-epoch u32 (tag epoch=device); print both with their decoded human-readable form so Plan 05 can record the model.

    Print a structured summary (returnable dict) of all decoded streams for Plan 05 / FINDINGS_5.md §Phase 4.
  </action>
  <verify>
    <automated>cd re/survey_5 && python decode_streams_5.py && echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `re/survey_5/decode_streams_5.py` contains `from decode_5 import` and length guards (grep -c "len(body)" >= 1)
    - `cd re/survey_5 && python decode_streams_5.py` exits 0
    - Output resolves at least one EVENT to an r52 EventNumber name (output contains "STRAP_CONDITION_REPORT" or another EventNumber name) and reports a device-epoch u32 (PROTO-09, PROTO-15 device epoch)
    - Output documents the historical-offload protocol naming SEND_HISTORICAL_DATA, HISTORICAL_DATA_RESULT, and a trim cursor (output contains "Trim" or "trim" and "HISTORICAL")
    - Output decodes at least one Unix timestamp from GET_DATA_RANGE (PROTO-15 unix epoch) and at least one device-epoch value (PROTO-15 device epoch)
    - Battery decode is attempted and reported (PROTO-08) — output references battery SOC or the battery event/command
  </acceptance_criteria>
  <done>decode_streams_5.py decodes the corpus's events (incl. battery), metadata, and the full historical-offload narration into documented protocol facts, and demonstrates the dual-epoch model with both a Unix and a device-epoch timestamp — covering PROTO-08/09/10/15 from existing captures (the live offload kill-test stays Phase 5 per D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus bytes → stream decoders | Possibly-truncated event/metadata bodies indexed at offsets 6/8+; offline only |
| CONSOLE_LOGS ASCII → documentation | Console-log strings may contain device identifiers — must be scrubbed before committing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | decode_streams_5 indexing body[6]/body[8]/battery payload on a short event body | mitigate | len(body) guards before every offset access (D-03 log-and-continue); never crash on truncated frames |
| T-04-04 | Information Disclosure | CONSOLE_LOGS ASCII narration may embed a serial/identifier when documented into FINDINGS | mitigate | When extracting console-log strings for documentation, redact any identifier; commit only protocol-structure narration (cmd names, trim cursor format) per DISCLAIMER §2 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs; stdlib + the existing decode_5/validate_frames_5 only |
</threat_model>

<verification>
- `cd re/survey_5 && python command_surface_5.py` exits 0 and reconciles observed-vs-r52 (incl. the 14 reused IDs)
- `cd re/survey_5 && python decode_streams_5.py` exits 0 and decodes events/battery/metadata/historical/dual-epoch
- Historical-offload protocol documented (SEND_HISTORICAL_DATA / HISTORICAL_DATA_RESULT / trim cursor) — documentation-only per D-09
</verification>

<success_criteria>
- PROTO-06 fulfilled via capture-analysis command-surface reconciliation (observed vs r52, reused-14 cross-validated)
- PROTO-08 battery, PROTO-09 events decoded from existing corpus
- PROTO-10 historical-offload protocol documented (live test deferred to Phase 5 per D-09)
- PROTO-15 dual-epoch model demonstrated with both Unix and device-epoch timestamps
</success_criteria>

<output>
Create `.planning/phases/04-protocol-decode-schema/04-03-SUMMARY.md` when done
</output>
