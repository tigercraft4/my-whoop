---
phase: 04-protocol-decode-schema
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - re/survey_5/decode_biometrics_5.py
autonomous: true
requirements: [PROTO-07, PROTO-11, PROTO-12, PROTO-13, PROTO-14, PROTO-16]
must_haves:
  truths:
    - "Realtime HR/RR is decoded from the D-05 capture: standard 0x2A37 via parse_hr verbatim, plus custom REALTIME_DATA (type 40) if present; HR/RR validated against the HR strap (D-08)"
    - "IMU/gravity (type 43 REALTIME_RAW_DATA) is decoded if present in the capture, with the 4.0 layout adapted and sample rate reported (PROTO-14); HYPOTHESIS if absent"
    - "SpO2 (PROTO-11), skin temp (PROTO-12), respiration (PROTO-13) are decoded ONLY if the bytes are observed in the capture; otherwise tagged HYPOTHESIS with honest 4.0-cloud-computed provenance"
    - "Every biometric stream verdict (VERIFIED vs HYPOTHESIS) is recorded for Plan 05's schema, with the firmware revision noted (PROTO-16)"
  artifacts:
    - path: "re/survey_5/decode_biometrics_5.py"
      provides: "Biometric stream decoders (HR/RR, IMU, SpO2/temp/respiration) over the D-05 capture with VERIFIED/HYPOTHESIS verdicts"
      contains: "def "
      min_lines: 60
  key_links:
    - from: "re/survey_5/decode_biometrics_5.py"
      to: "re/survey_5/decode_5.py"
      via: "imports parse_body_5"
      pattern: "from decode_5 import"
    - from: "re/survey_5/decode_biometrics_5.py"
      to: "re/standard_ble.py"
      via: "reuses parse_hr verbatim for standard 0x2A37 HR/RR"
      pattern: "parse_hr"
---

<objective>
Decode the biometric streams from the D-05 targeted capture (Plan 02): realtime HR/RR (PROTO-07), IMU/gravity (PROTO-14), and — only if their bytes are actually observed — SpO₂ (PROTO-11), skin temperature (PROTO-12), and respiration rate (PROTO-13). These are the four capture-gated requirements (RESEARCH A1-A4): on 4.0, SpO₂/temp/respiration were cloud-computed and NOT on the BLE wire, so they MUST be decoded only if the D-05 capture proves the bytes exist — never fabricated (Pitfall 5).

HR/RR has two paths: the standard 0x2A37 characteristic (reuse `parse_hr` verbatim, already CONFIRMED unbonded in Phase 2) validated against the HR strap (D-08), plus the custom REALTIME_DATA (type 40) stream if the capture contains it. IMU adapts the 4.0 REALTIME_RAW_DATA (type 43) layout if present.

Purpose: Push the biometric requirements toward VERIFIED where the capture supports it, and record honest HYPOTHESIS verdicts where it does not — giving Plan 05 a truthful confidence map for the schema.
Output: `re/survey_5/decode_biometrics_5.py` (per-stream decoders + VERIFIED/HYPOTHESIS verdicts against ground truth D-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-decode-schema/04-CONTEXT.md
@.planning/phases/04-protocol-decode-schema/04-RESEARCH.md
@.planning/phases/04-protocol-decode-schema/04-PATTERNS.md
@.planning/phases/04-protocol-decode-schema/04-01-SUMMARY.md
@.planning/phases/04-protocol-decode-schema/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HR/RR + IMU decode from the D-05 capture (PROTO-07, PROTO-14)</name>
  <files>re/survey_5/decode_biometrics_5.py</files>
  <read_first>
    - re/survey_5/decode_5.py (import parse_body_5 + resolver)
    - re/standard_ble.py (parse_hr lines 11-29 — reuse VERBATIM for standard 0x2A37 HR/RR)
    - re/survey_5/validate_frames_5.py (build_report / extract_frames to read the D-05 capture)
    - re/capture/evidence/2026-05-30-biometric-5.meta.yaml (the Plan 02 sidecar — tells you which streams type 40 / type 43 are present)
    - protocol/whoop_protocol.json (packets.REALTIME_DATA lines 69-76 for the type-40 HR/RR field map; REALTIME_RAW_DATA type 43 for IMU)
    - FINDINGS.md (4.0 §6/§9b REALTIME_RAW_DATA IMU layout: 100 samples/axis int16-LE, accel/gyro offsets — adapt for 5.0 body offset)
    - .planning/phases/04-protocol-decode-schema/04-RESEARCH.md (PROTO-07/14 rows; A4 IMU stride/scale unverified; Pattern 1 body offset reconciliation)
  </read_first>
  <action>
    Create `re/survey_5/decode_biometrics_5.py` (D-02 isolation, `from decode_5 import parse_body_5`, `from standard_ble import parse_hr` or copy parse_hr's logic if the import path requires it). Read the D-05 capture (path from the Plan 02 sidecar / SUMMARY) via `validate_frames_5.extract_frames`.

    HR/RR (PROTO-07): decode the standard 0x2A37 Heart Rate Measurement notifications using `parse_hr` verbatim (flags byte + uint8/uint16 HR + 1/1024s RR intervals → ms). If the capture contains custom REALTIME_DATA (type 40, on the data char), decode its body using the 4.0 field map adapted to the 5.0 body base: the 4.0 schema uses frame-relative offsets 6/10/12/13 (timestamp/subseconds/heart_rate/rr_count); reconcile these to the 5.0 body[7:] payload base (the role+token+type+seq+cmd prefix shifts the base) and decode HR + RR. Compare decoded HR against the HR strap reading captured alongside (D-08): within tolerance → VERIFIED, else flag. Guard all offsets (D-03).

    IMU/gravity (PROTO-14): if the capture contains REALTIME_RAW_DATA (type 43), adapt the 4.0 layout (int16-LE samples, 6-axis X/Y/Z accel + gyro per tazjin "6 integers"; 100 samples/axis per 4.0 §9b) to the 5.0 body offset and decode the sample array; report the inferred sample rate (52 Hz or 26 Hz — A4). Mark stride/scale as HYPOTHESIS where unverified (A4). If type 43 is absent from the capture, record PROTO-14 as HYPOTHESIS "raw IMU not observed; START_RAW_DATA cmd 81 not triggered in capture" — do NOT fabricate offsets.

    Output per-stream verdicts (HR/RR, IMU) with VERIFIED/HYPOTHESIS + provenance for Plan 05.
  </action>
  <verify>
    <automated>cd re/survey_5 && python decode_biometrics_5.py --streams hr,imu && echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `re/survey_5/decode_biometrics_5.py` contains `from decode_5 import` and references `parse_hr` (standard HR reuse)
    - `cd re/survey_5 && python decode_biometrics_5.py --streams hr,imu` exits 0
    - Output reports an HR/RR decode result with a VERIFIED-or-HYPOTHESIS verdict and a ground-truth (HR strap) comparison note (D-08)
    - Output reports an IMU verdict: either decoded sample-rate + values (if type 43 present) OR an explicit HYPOTHESIS "raw IMU not observed" with no fabricated offsets
    - All offset accesses are length-guarded (grep -c "len(" >= 1) — no crash on short/absent streams
  </acceptance_criteria>
  <done>HR/RR is decoded (standard 0x2A37 verbatim + custom type-40 if present) and validated against the HR strap; IMU (type 43) is decoded with sample rate if present, else honestly HYPOTHESIS — never fabricated (PROTO-07, PROTO-14).</done>
</task>

<task type="auto">
  <name>Task 2: SpO2 / skin temp / respiration — decode-if-observed, else HYPOTHESIS (PROTO-11/12/13, PROTO-16)</name>
  <files>re/survey_5/decode_biometrics_5.py</files>
  <read_first>
    - re/survey_5/decode_biometrics_5.py (extend the Task 1 file)
    - re/survey_5/decode_5.py (parse_body_5 + resolver; the dual-epoch helper)
    - re/capture/evidence/2026-05-30-biometric-5.meta.yaml (Plan 02 sidecar — does the capture contain sleep-review / type-53 / event-17 frames?)
    - FINDINGS.md (4.0 §6/§9b: "SpO2/skin-temp computed VALUES: NOT in the BLE stream" — the 4.0 precedent that makes these capture-gated)
    - .planning/phases/04-protocol-decode-schema/04-RESEARCH.md (PROTO-11 type 53 byte 10 per Sivasai2207; PROTO-12 event 17 LE-int/100000; PROTO-13; Pitfall 5; A1-A3 — all HYPOTHESIS until bytes observed)
  </read_first>
  <action>
    Extend `decode_biometrics_5.py` with SpO₂, skin-temp, and respiration decoders that are STRICTLY observation-gated (Pitfall 5):

    SpO₂ (PROTO-11): scan the D-05 capture for type-53 (RELATIVE_PUFFIN_EVENTS per r52) or any frame where byte 10 of the payload yields a plausible SpO₂ value (90-100%). If found, decode it and compare to the WHOOP app display value at the aligned timestamp (D-08, ±2% → VERIFIED). If NOT found, tag PROTO-11 HYPOTHESIS with note "type 53 byte 10 cited by Sivasai2207 but not observed in capture; 4.0 precedent = cloud-computed (off-wire)". Do NOT invent a VERIFIED SpO₂ field without a captured frame.

    Skin temp (PROTO-12): scan for TEMPERATURE_LEVEL events (EventNumber 17). If present, decode the payload as LE-int / 100000 → °C and compare to the app display (D-08, ±0.5°C → VERIFIED). If absent, tag PROTO-12 HYPOTHESIS with note "event 17 TEMPERATURE_LEVEL not observed; 4.0 never captured this event".

    Respiration (PROTO-13): scan for any respiration-rate field in the capture (likely a derived/sleep metric). If observed, decode and validate against the app display. If absent, tag PROTO-13 HYPOTHESIS "respiration likely a derived/sleep metric, may be cloud-only; not observed on wire".

    PROTO-16: confirm the firmware revision from the Plan 02 sidecar is carried into each biometric verdict's provenance (every decoded stream's verdict references the capture's WG50_r52 firmware). Output a final per-stream verdict table (stream → VERIFIED/HYPOTHESIS → provenance → firmware) for Plan 05's schema and FINDINGS_5.md.
  </action>
  <verify>
    <automated>cd re/survey_5 && python decode_biometrics_5.py --streams spo2,temp,resp && echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd re/survey_5 && python decode_biometrics_5.py --streams spo2,temp,resp` exits 0
    - Each of SpO2 / skin-temp / respiration produces a verdict: decoded+VERIFIED (only if bytes observed) OR HYPOTHESIS with an honest provenance note citing the 4.0 cloud-computed precedent
    - The output does NOT emit a VERIFIED SpO2/temp/respiration field unless a corresponding captured frame is referenced (no fabricated offsets — Pitfall 5)
    - Each biometric verdict references the capture's firmware revision (PROTO-16) — output contains "WG50_r52" or the actual revision
    - All payload offset accesses are length-guarded (no crash when a stream is absent)
  </acceptance_criteria>
  <done>SpO2/skin-temp/respiration are decoded only where the D-05 capture proves the bytes, otherwise tagged HYPOTHESIS with honest 4.0-cloud-computed provenance; every verdict carries the firmware revision (PROTO-16). No offsets are fabricated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D-05 pklg → biometric decoders | Untrusted/possibly-short sensor bodies indexed at deep payload offsets; offline only |
| decoded biometric values → app-display comparison | Ground-truth alignment is human-in-the-loop (D-08); no automated network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | decode_biometrics_5 indexing deep payload offsets (type-40 HR, type-43 IMU sample arrays, type-53 byte 10) on truncated bodies | mitigate | len() guards before every slice; absent stream → HYPOTHESIS path, never crash (D-03) |
| T-04-05 | Spoofing/Tampering (data integrity) | fabricating SpO2/temp/respiration offsets to "complete" the schema | mitigate | Strict observation-gating (Pitfall 5): VERIFIED only with a referenced captured frame; else HYPOTHESIS with 4.0-precedent note |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs; reuses existing decode_5 / standard_ble / validate_frames_5 + stdlib |
</threat_model>

<verification>
- `cd re/survey_5 && python decode_biometrics_5.py --streams hr,imu` exits 0 with HR strap (D-08) comparison
- `cd re/survey_5 && python decode_biometrics_5.py --streams spo2,temp,resp` exits 0 with honest VERIFIED/HYPOTHESIS verdicts
- No fabricated offsets; firmware revision carried into every verdict (PROTO-16)
</verification>

<success_criteria>
- PROTO-07 HR/RR decoded and validated against HR strap (D-08)
- PROTO-14 IMU decoded if present, else honest HYPOTHESIS
- PROTO-11/12/13 decoded only where observed; HYPOTHESIS with honest provenance otherwise (no fabrication)
- PROTO-16 firmware revision in every biometric verdict
</success_criteria>

<output>
Create `.planning/phases/04-protocol-decode-schema/04-04-SUMMARY.md` when done
</output>
