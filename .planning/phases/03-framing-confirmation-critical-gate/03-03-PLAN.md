---
phase: 03-framing-confirmation-critical-gate
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - re/capture/evidence/2026-05-30-framing-5.meta.yaml
  - FINDINGS_5.md
autonomous: true
requirements: [PROTO-04, PROTO-05]
must_haves:
  truths:
    - "An evidence sidecar records the 4.0 CRC gate 0% pass rate, the 5028/5028 wrapper-overhead consistency, and frame counts by characteristic"
    - "FINDINGS_5.md has a Phase 3 framing section with the empirical results and the WG50_r52 -> whoop-vault r52 enum-map reuse note"
    - "FINDINGS_5.md records the exact go/no-go verdict: wrapper characterised, decode work cleared with wrapper-strip step"
    - "No raw .pklg paths, BD_ADDR, or SMP keys are committed (evidence redaction policy upheld)"
  artifacts:
    - path: "re/capture/evidence/2026-05-30-framing-5.meta.yaml"
      provides: "Pass-rate + wrapper-overhead evidence sidecar (D-02 policy)"
      contains: "tshark"
    - path: "FINDINGS_5.md"
      provides: "Phase 3 framing section + go/no-go verdict (Phase 4 entry condition)"
      contains: "wrapper characterised, decode work cleared with wrapper-strip step"
  key_links:
    - from: "FINDINGS_5.md"
      to: "protocol/whoop_protocol_5.json"
      via: "Phase 3 section references the committed schema v0"
      pattern: "whoop_protocol_5\\.json"
---

<objective>
Close the critical gate: write the evidence sidecar `re/capture/evidence/2026-05-30-framing-5.meta.yaml` (pass-rate + wrapper-overhead evidence per the D-02 redaction policy) and EXTEND `FINDINGS_5.md` with a Phase 3 framing section that records the empirical results and the exact go/no-go verdict — "wrapper characterised, decode work cleared with wrapper-strip step" (ROADMAP criterion 4). Addresses PROTO-04 (documented CRC-gate result) and PROTO-05 (wrapper characterisation recorded in the canonical findings doc).

Purpose: The go/no-go verdict in `FINDINGS_5.md` is the Phase 4 entry condition (D-03b — Phase 4 does not begin without it). This plan converts the validator outputs into committed, redacted evidence and the canonical decision record.
Output: `re/capture/evidence/2026-05-30-framing-5.meta.yaml`, extended `FINDINGS_5.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-framing-confirmation-critical-gate/03-RESEARCH.md
@.planning/phases/03-framing-confirmation-critical-gate/03-PATTERNS.md
@re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml
@FINDINGS_5.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the framing evidence sidecar (pass-rate + wrapper-overhead, redacted)</name>
  <files>re/capture/evidence/2026-05-30-framing-5.meta.yaml</files>
  <read_first>
    - re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml — sidecar header pattern (source/tool/tool_version/captured/device_identity), raw_artifacts_local_only + notes redaction pattern (lines 1-6, 54-58)
    - re/survey_5/validate_frames_5.py — the validator whose output this sidecar records (produced in plan 03-01)
    - re/survey_5/frames_5_golden.json — the corpus this evidence references (frame counts, characteristics)
    - .planning/phases/03-framing-confirmation-critical-gate/03-RESEARCH.md — Findings 2-4 (frame counts by characteristic, 4.0 CRC 0%, 8-byte overhead 5028/5028), Pitfall 5 (redaction)
    - DISCLAIMER.md — §2 evidence policy (no proprietary material; redact device identifiers)
  </read_first>
  <action>
    Run `cd re/survey_5 && .venv/bin/python validate_frames_5.py` and capture its actual printed numbers (frame counts per characteristic, 4.0 CRC pass rate, wrapper-overhead consistency). Create `re/capture/evidence/2026-05-30-framing-5.meta.yaml` following the gatt-survey sidecar shape:
    - `source:` describing tshark extraction from the two captures (Phase 1 + Phase 2 sessions)
    - `tool: "tshark 4.6.6 + re/survey_5/validate_frames_5.py"`, `tool_version: "tshark 4.6.6 / Python 3.9.6"` (use the actual versions from `tshark --version` / `python3 --version`)
    - `captured: "2026-05-30"`, `device_identity: "[REDACTED]"`
    - A `results:` block recording the ACTUAL run output: `crc8_4_0_pass`, `crc32_4_0_pass`, `crc_4_0_pass_rate: "0%"` (or the actual rate), total frames validated, `wrapper_overhead_consistent: "<N>/<N>"` (the len==length+8 count), and a `frames_by_characteristic:` map (cmd_resp 0x099d, events 0x09a0, data 0x09a3, cmd_in 0x099b counts)
    - `verdict: "wrapper characterised, decode work cleared with wrapper-strip step"`
    - `raw_artifacts_local_only:` listing the two `.pklg` paths (gitignored, local-only)
    - `notes:` affirming no BD_ADDR / SMP keys / device identifiers committed (DISCLAIMER §2, Pitfall 5)
    Do NOT commit any raw `.pklg` path content, BD_ADDR, or SMP key. Confirm `re/capture/samples/` is gitignored (`git check-ignore`).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop && test -f re/capture/evidence/2026-05-30-framing-5.meta.yaml && grep -q 'tshark' re/capture/evidence/2026-05-30-framing-5.meta.yaml && grep -q 'device_identity:.*\[REDACTED\]' re/capture/evidence/2026-05-30-framing-5.meta.yaml && grep -q 'wrapper characterised, decode work cleared with wrapper-strip step' re/capture/evidence/2026-05-30-framing-5.meta.yaml && grep -q 'results:' re/capture/evidence/2026-05-30-framing-5.meta.yaml && git check-ignore "re/capture/samples/2026-05-30-smp-bond-full.pklg" >/dev/null && echo "evidence OK + samples gitignored"</automated>
  </verify>
  <acceptance_criteria>
    - `re/capture/evidence/2026-05-30-framing-5.meta.yaml` exists and is valid YAML (loadable by `re/survey_5/.venv/bin/python -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))"` if PyYAML present; otherwise grep checks below suffice)
    - The verify command prints `evidence OK + samples gitignored` and exits 0
    - `grep -q 'device_identity:.*\[REDACTED\]'` matches and a `results:` block exists with the CRC pass rate and wrapper-overhead consistency
    - The file contains the exact substring `wrapper characterised, decode work cleared with wrapper-strip step`
    - No raw key material committed: `grep -iE '([0-9a-f]{2}:){5}[0-9a-f]{2}' re/capture/evidence/2026-05-30-framing-5.meta.yaml` returns no BD_ADDR-shaped hex (only the redaction-affirming note text is allowed)
    - `git check-ignore re/capture/samples/2026-05-30-smp-bond-full.pklg` exits 0 (raw captures gitignored)
  </acceptance_criteria>
  <done>The evidence sidecar records the actual validator run results (0% 4.0 CRC, wrapper overhead consistent, per-characteristic counts) with the go/no-go verdict and full redaction; raw captures confirmed gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Extend FINDINGS_5.md with the Phase 3 framing section + go/no-go verdict</name>
  <files>FINDINGS_5.md</files>
  <read_first>
    - FINDINGS_5.md — existing section style (`## N. Title` + `### subsection` + evidence-cited prose, §1-§6) and the `## Status at a glance` block (lines 11-26) to update
    - re/capture/evidence/2026-05-30-framing-5.meta.yaml — the evidence numbers produced in Task 1 to cite
    - protocol/whoop_protocol_5.json — the committed schema v0 to reference (produced in plan 03-02)
    - .planning/phases/03-framing-confirmation-critical-gate/03-RESEARCH.md — Findings 3-6, Open Questions (trailer OPEN), Specifics (WG50_r52 -> r52 enum-map reuse note)
    - .planning/phases/03-framing-confirmation-critical-gate/03-CONTEXT.md — Specifics (exact verdict wording), D-04
  </read_first>
  <action>
    Add a new section to `FINDINGS_5.md` titled `## 7. Framing (Phase 3)` (matching the existing `## N. Title` convention) containing, in the file's evidence-cited prose style:
    - The 4.0 CRC gate result: 0% pass on the captured frames (cite the evidence sidecar) — PROTO-04 is a documented NEGATIVE; the 4.0 inner framing is NOT reused verbatim.
    - The Maverick outer wrapper layout: `[0xAA][0x01][len u16 LE][role 1B] ... flat body ... [trailer 4B]`, total_len == length + 8, role 0x00=cmd-in / 0x01=notify (cite Finding 4). Note the body is FLAT — no nested 0xAA frame (Finding 5).
    - The trailer checksum is OPEN/HYPOTHESIS — standard CRC16/CRC32 variants ruled out (Finding 6); this does NOT block phase closure because `strip_maverick()` does not need it.
    - A reference to `protocol/whoop_protocol_5.json` v0 as the committed schema and `re/survey_5/strip_maverick()` as the working stripper.
    - The WG50_r52 -> whoop-vault r52 enum-map note: the r52 command/event ID maps are directly usable in Phase 4 without re-derivation (CONTEXT Specifics).
    - The exact go/no-go verdict line (verbatim, per ROADMAP criterion 4 + CONTEXT Specifics): `wrapper characterised, decode work cleared with wrapper-strip step`.

    Update the `## Status at a glance` table: change the `Command/response protocol` / framing row from `Phase 3 (...)` to reflect the wrapper-characterised outcome, in the existing table style.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop && grep -q "## 7. Framing (Phase 3)" FINDINGS_5.md && grep -q "wrapper characterised, decode work cleared with wrapper-strip step" FINDINGS_5.md && grep -q "whoop_protocol_5.json" FINDINGS_5.md && grep -qi "r52" FINDINGS_5.md && echo "findings OK"</automated>
  </verify>
  <acceptance_criteria>
    - `FINDINGS_5.md contains "## 7. Framing (Phase 3)"`
    - `FINDINGS_5.md contains "wrapper characterised, decode work cleared with wrapper-strip step"` (the exact go/no-go verdict)
    - `FINDINGS_5.md contains "whoop_protocol_5.json"` (references the committed schema)
    - `FINDINGS_5.md` mentions `r52` (the whoop-vault enum-map reuse note)
    - The section documents the 0% 4.0-CRC result AND the Maverick wrapper layout AND the trailer-OPEN status
    - The verify command prints `findings OK` and exits 0
  </acceptance_criteria>
  <done>FINDINGS_5.md §7 records the empirical framing results, references the schema and stripper, includes the r52 enum-map note, and states the exact go/no-go verdict that unblocks Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Local-only, read-only RE/analysis tooling. This plan writes committed documentation and a redacted evidence sidecar. The one security-relevant control is evidence redaction (no device identifiers in committed files).

| Boundary | Description |
|----------|-------------|
| local capture → committed evidence | Redaction boundary: protocol facts are committable; BD_ADDR / SMP keys / device identity are NOT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | committed evidence YAML + FINDINGS_5.md | mitigate | `device_identity: "[REDACTED]"`; raw `.pklg` listed under raw_artifacts_local_only only and confirmed gitignored via git check-ignore; no BD_ADDR / SMP keys / serial committed (DISCLAIMER §2, Pitfall 5). Verified by acceptance criteria grep + git check-ignore. |

Security threat model is otherwise N/A for this phase: no auth, no network, no user-input surface.
</threat_model>

<verification>
- Evidence sidecar records the actual validator run (0% 4.0 CRC, wrapper overhead consistent, per-characteristic counts) with full redaction
- Raw captures confirmed gitignored (git check-ignore)
- FINDINGS_5.md §7 records the empirical results, references the schema + stripper, and states the exact go/no-go verdict
- Phase 4 entry condition (the verdict) is now committed (D-03b)
</verification>

<success_criteria>
- `re/capture/evidence/2026-05-30-framing-5.meta.yaml` committed with results block + verdict, fully redacted
- `FINDINGS_5.md` extended with §7 Framing (Phase 3) + exact go/no-go verdict + r52 enum-map note
- No raw .pklg / BD_ADDR / SMP keys committed
</success_criteria>

<output>
Create `.planning/phases/03-framing-confirmation-critical-gate/03-03-SUMMARY.md` when done
</output>
