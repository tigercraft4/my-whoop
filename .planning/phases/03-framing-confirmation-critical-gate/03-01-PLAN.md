---
phase: 03-framing-confirmation-critical-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - re/survey_5/validate_frames_5.py
  - re/survey_5/frames_5_golden.json
autonomous: true
requirements: [PROTO-04, PROTO-05]
must_haves:
  truths:
    - "Developer runs validate_frames_5.py against the two existing .pklg captures and sees a per-characteristic pass/fail report"
    - "The 4.0 CRC8 + CRC32-LE gate is run and documents a 0.0% pass rate (PROTO-04 negative result)"
    - "strip_maverick() removes the 4-byte header and 4-byte trailer and returns the flat body (PROTO-05)"
    - "frames_5_golden.json contains >=20 wrapper-stripped frames mixing cmd-resp, events, and data across two sessions"
  artifacts:
    - path: "re/survey_5/validate_frames_5.py"
      provides: "4.0 CRC gate + parse_maverick/strip_maverick + golden corpus writer"
      contains: "def strip_maverick"
      min_lines: 80
    - path: "re/survey_5/frames_5_golden.json"
      provides: "Wrapper-stripped frame corpus for Phase 4 (hex-first entries)"
      contains: "\"hex\""
  key_links:
    - from: "re/survey_5/validate_frames_5.py"
      to: "re/capture/samples/whoop- iPhone de Francisco.pklg"
      via: "tshark -r subprocess or piped tshark output"
      pattern: "btatt\\.value"
    - from: "re/survey_5/validate_frames_5.py"
      to: "re/survey_5/frames_5_golden.json"
      via: "json.dump to Path(__file__).parent"
      pattern: "frames_5_golden\\.json"
---

<objective>
Build `re/survey_5/validate_frames_5.py` — the critical-gate validator. It (1) runs the 4.0 CRC8 (poly 0x07) + CRC32-LE gate against captured 5.0 frames and documents the result, (2) implements `parse_maverick()` / `strip_maverick()` that strip the confirmed 4-byte Maverick header + 4-byte trailer to expose the FLAT body, and (3) writes `frames_5_golden.json` (the Phase 4 decode corpus). Addresses PROTO-04 (the CRC gate is run, producing a documented negative) and PROTO-05 (the Maverick wrapper stripper is working code).

Purpose: This is the hinge of the project. RESEARCH already verified the 4.0 CRC gate yields 0.0% on 5028 frames and characterised the Maverick wrapper with HIGH confidence. This plan turns that empirical finding into committed, re-runnable tooling that Phase 4 builds on.
Output: `re/survey_5/validate_frames_5.py`, `re/survey_5/frames_5_golden.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-framing-confirmation-critical-gate/03-RESEARCH.md
@.planning/phases/03-framing-confirmation-critical-gate/03-PATTERNS.md
@re/decode.py
@re/survey_5/survey_gatt_5.py
@re/survey_5/hr_5.py
@Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/frames.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement validate_frames_5.py CRC gate + Maverick wrapper parser</name>
  <files>re/survey_5/validate_frames_5.py</files>
  <read_first>
    - re/decode.py — port reassemble() and parse_frame() logic; DO NOT copy the `sys.path.insert(0, "whoomp/scripts")` + `from packet import WhoopPacket` lines (D-02 isolation anti-pattern, decode.py lines 7-8)
    - re/survey_5/survey_gatt_5.py — module docstring style, `Path(__file__).parent` output convention, run-line convention
    - re/survey_5/hr_5.py — survey_5 script header + per-characteristic report-print convention
    - Packages/WhoopProtocol/Sources/WhoopProtocol/Framing.swift — CRC8 poly 0x07 table to cross-check against
    - .planning/phases/03-framing-confirmation-critical-gate/03-RESEARCH.md — Findings 1-6, Code Examples (verify_4_0, crc8 table), Pattern 2 (parse_maverick/strip_maverick), Pitfalls 1-4
  </read_first>
  <behavior>
    - crc8(b"\x08\x00") returns the same byte the Swift CRC8_TABLE (poly 0x07) produces for the same input (cross-check value asserted in a module self-test or comment)
    - verify_4_0(frame) returns (False, False) for every captured 5.0 frame (4.0 gate fails — this is the expected documented negative)
    - parse_maverick(frame) returns None when len(frame) < 9 or frame[0] != 0xAA or frame[1] != 0x01 or len(frame) != length+8
    - parse_maverick(frame) on a valid wrapper returns dict with keys length (struct.unpack("<H", frame[2:4])), role (frame[4]), body (frame[4:4+length]), trailer (frame[-4:])
    - strip_maverick(frame) is a pure bytes->bytes function returning frame[4:4+length] (the FLAT body) or b"" if parse fails; it does NOT search for or validate a nested 0xAA frame
  </behavior>
  <action>
    Create `re/survey_5/validate_frames_5.py` following the survey_5 module-docstring shape (one-line purpose, "adapted from re/decode.py + Framing.swift", and run line `cd re/survey_5 && .venv/bin/python validate_frames_5.py`). State in the docstring that this script is isolated per D-02 (no `whoomp` sys.path import).

    Implement a bitwise CRC8 table generator with `CRC8_POLY = 0x07` producing the 256-entry table, plus `crc8(data: bytes) -> int`. Implement `verify_4_0(frame: bytes) -> tuple[bool, bool]` exactly per RESEARCH Code Examples: it reads `length = struct.unpack("<H", frame[1:3])[0]` (the WRONG 4.0 offset, intentionally — this documents the gate), checks crc8 over `frame[1:3]` against `frame[3]`, and CRC32 via `zlib.crc32(frame[4:length]) & 0xFFFFFFFF` against `struct.unpack("<L", frame[length:length+4])[0]`. Expected: returns (False, False) on 5.0 frames.

    Implement `parse_maverick(frame: bytes)` and `strip_maverick(frame: bytes) -> bytes` exactly per RESEARCH Pattern 2: header is `frame[0:4]` (SOF=0xAA at offset 0, version=0x01 at offset 1, length u16-LE at offset 2-3, role at offset 4), body = `frame[4:4+length]`, trailer = `frame[-4:]`, total length MUST equal `length + 8`. `strip_maverick()` MUST be a pure function returning the flat body — its docstring MUST document the field offsets (body[0]=role, body[1:4]=session token HYPOTHESIS, body[4..]=seq+payload) AND state there is NO nested 0xAA frame to recover (per RESEARCH Finding 5 / Assumption A2). Do NOT re-run the 4.0 CRC gate on the stripped body.

    Add a `reassemble()`-style pass-through adapted from re/decode.py that filters fragments where `f[0] != 0xAA` (skip strays). Since tshark yields complete ATT values, treat reassembly as a no-op pass-through that just applies the SOF filter; document this in a comment referencing decode.py reassemble().
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop && re/survey_5/.venv/bin/python -c "import sys; sys.path.insert(0,'re/survey_5'); import validate_frames_5 as v; assert v.strip_maverick(bytes.fromhex('aa0108000001e67123942200c0896bce'))==bytes.fromhex('0001e67123942200'), 'strip body wrong'; assert v.verify_4_0(bytes.fromhex('aa0108000001e67123942200c0896bce'))==(False,False), '4.0 gate should fail'; assert v.parse_maverick(b'\\xaa\\x00')is None, 'short frame should be None'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `re/survey_5/validate_frames_5.py contains "def strip_maverick"` and `contains "def parse_maverick"` and `contains "def verify_4_0"`
    - `validate_frames_5.py` does NOT contain the string `sys.path.insert` pointing at `whoomp` (grep `grep -c 'whoomp' re/survey_5/validate_frames_5.py` returns 0)
    - The verify command above prints `OK` and exits 0 (strip returns flat body `0001e67123942200` (frame[4:4+length], length=8); 4.0 gate returns `(False, False)`; short frame returns None)
    - `strip_maverick` docstring contains the substring `NO nested` or `no nested` (documents Finding 5)
  </acceptance_criteria>
  <done>strip_maverick() returns the flat body, parse_maverick() validates the length+8 invariant, verify_4_0() returns (False,False) on 5.0 frames, and no whoomp import exists.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tshark extraction + report + write frames_5_golden.json corpus</name>
  <files>re/survey_5/validate_frames_5.py, re/survey_5/frames_5_golden.json</files>
  <read_first>
    - re/survey_5/validate_frames_5.py — the functions created in Task 1 (extend, do not rewrite)
    - re/decode.py — lines 43-64 `bytype` aggregation + per-type report-print model
    - re/survey_5/survey_gatt_5.py — `OUT_PATH = Path(__file__).parent / ...` + `json.dump(result, f, indent=2)` writer convention
    - Packages/WhoopProtocol/Tests/WhoopProtocolTests/Resources/frames.json — golden fixture format (hex-first minimal entries)
    - .planning/phases/03-framing-confirmation-critical-gate/03-RESEARCH.md — Pattern 1 (tshark command), Pattern 3 (golden format), Finding 2 (handle distribution), Pitfall 3 (filename spaces), Pitfall 4 (handle filter)
  </read_first>
  <action>
    Extend `validate_frames_5.py` to extract ATT values via tshark from BOTH captures (treated as two sessions per D-01): `re/capture/samples/whoop- iPhone de Francisco.pklg` (Phase 1) and `re/capture/samples/2026-05-30-smp-bond-full.pklg` (Phase 2). Use the EXACT filename `whoop- iPhone de Francisco.pklg` (with the space) — quote it; do NOT hardcode `2026-05-30-ios.pklg` (it does not exist, per RESEARCH Finding 1). Run tshark via subprocess: `tshark -r "<file>" -Y "btatt.value" -T fields -e btatt.handle -e btatt.value`. Make the capture paths overridable by a CLI argument or a module constant (Claude's discretion per D-02), defaulting to the two known files.

    Filter extracted rows to keep only values starting with `aa` AND handle in {`0x099b`, `0x099d`, `0x09a0`, `0x09a3`} (the four custom-service handles, per Pitfall 4). For each kept frame: run `verify_4_0()` and `parse_maverick()`.

    Print a per-characteristic breakdown grouped by handle/role following the re/decode.py `bytype` model: for each handle show frame count, 4.0 CRC8 pass count, 4.0 CRC32 pass count, count where the `length+8` wrapper invariant holds, and one example head hex. Print a summary line with the aggregate 4.0 CRC pass rate (expected `0.0%`) and the wrapper-overhead consistency (expected all frames satisfy `len == length+8`). Print an explicit verdict line containing the exact string `Maverick wrapper: CONFIRMED` when all kept frames satisfy the invariant.

    Write `re/survey_5/frames_5_golden.json` via `OUT_PATH = Path(__file__).parent / "frames_5_golden.json"` and `json.dump(..., indent=2)`. Each entry MUST keep `hex` as the FIRST key (Swift loader compatibility, RESEARCH Pattern 3), followed by `handle`, `characteristic`, `role`, `length`, `body_hex`, `trailer_hex`, `crc8_4_0_ok` (false), `crc32_4_0_ok` (false). Map handle->characteristic UUID using: 0x099b->FD4B0002, 0x099d->FD4B0003, 0x09a0->FD4B0004, 0x09a3->FD4B0005. The corpus MUST contain >=20 entries spanning cmd-resp (0x099d), events (0x09a0), and data (0x09a3) across the two captures.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop/re/survey_5 && .venv/bin/python validate_frames_5.py 2>&1 | tee /tmp/vf5.out; grep -q "0.0%" /tmp/vf5.out && grep -q "Maverick wrapper: CONFIRMED" /tmp/vf5.out && .venv/bin/python -c "import json; d=json.load(open('frames_5_golden.json')); assert len(d)>=20, len(d); assert list(d[0].keys())[0]=='hex'; roles=set(e['characteristic'] for e in d); assert {'FD4B0003','FD4B0005'} <= roles, roles; print('golden OK', len(d), 'frames')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd re/survey_5 && .venv/bin/python validate_frames_5.py` exits 0 and prints a line containing `0.0%` (the 4.0 CRC gate pass rate)
    - The script output contains the exact string `Maverick wrapper: CONFIRMED`
    - `re/survey_5/frames_5_golden.json` exists, parses as JSON, has >=20 entries, and `list(entry.keys())[0] == "hex"` for the first entry
    - The golden corpus spans at least the cmd-resp (FD4B0003) and data (FD4B0005) characteristics (assertion in verify command passes)
    - The script references the literal filename `whoop- iPhone de Francisco.pklg` and does NOT reference `2026-05-30-ios.pklg` (grep `grep -c '2026-05-30-ios.pklg' re/survey_5/validate_frames_5.py` returns 0)
  </acceptance_criteria>
  <done>Running validate_frames_5.py extracts frames from both captures, prints the 0.0% 4.0-CRC result and the CONFIRMED wrapper verdict, and writes a >=20-entry frames_5_golden.json with hex-first entries spanning multiple characteristics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is local-only, read-only RE/analysis tooling. No network, no untrusted input source, no authentication surface, no crypto implementation. The only inputs are local `.pklg` capture files the developer recorded from their own device.

| Boundary | Description |
|----------|-------------|
| local .pklg → tshark/python | Developer-owned capture files parsed locally; no remote or third-party data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | committed evidence/hex | mitigate | Raw `.pklg` are gitignored; only redacted output committed (handled in plan 03-03 per D-02 / DISCLAIMER §2). No BD_ADDR or SMP keys in this plan's outputs (frames_5_golden.json holds protocol bytes only). |

Security threat model is otherwise N/A: no auth, no network, no user-input surface.
</threat_model>

<verification>
- `validate_frames_5.py` runs end-to-end against both existing captures with no fresh capture needed (D-01b fallback not triggered — RESEARCH Env Availability confirms 5028 aa-frames available)
- 4.0 CRC gate documents 0.0% pass (PROTO-04 negative result)
- `strip_maverick()` is a working pure function (PROTO-05)
- `frames_5_golden.json` >=20 entries spanning two sessions and multiple characteristics
</verification>

<success_criteria>
- `re/survey_5/validate_frames_5.py` exists with `verify_4_0`, `parse_maverick`, `strip_maverick`, tshark extraction, per-characteristic report, golden writer
- No `whoomp` sys.path import (D-02 isolation)
- `re/survey_5/frames_5_golden.json` generated with >=20 hex-first entries
- Script output documents 0.0% 4.0-CRC pass rate and CONFIRMED Maverick wrapper
</success_criteria>

<output>
Create `.planning/phases/03-framing-confirmation-critical-gate/03-01-SUMMARY.md` when done
</output>
