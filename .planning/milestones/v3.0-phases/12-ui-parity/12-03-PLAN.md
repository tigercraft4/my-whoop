---
phase: 12-ui-parity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/OpenWhoop/Charts/MetricKind.swift
  - ios/OpenWhoop/Tabs/TodayView.swift
  - ios/OpenWhoop/Tabs/DayDetailView.swift
  - ios/OpenWhoopTests/MetricKindTests.swift
autonomous: true
requirements: [IOS-05, UI-03]
must_haves:
  truths:
    - "MetricKind has a .sleepPerformance case (label Sleep Performance, format %.0f%%) sourced from DailyMetric.efficiency"
    - "MetricKind.dailyCases lists .sleepPerformance in place of .sleepDuration as the primary sleep metric"
    - "Trends tab renders a Sleep Performance card (0–100%) instead of Sleep Duration"
    - "DayDetailView skin-temp card reads SKIN TEMP with from-baseline framing"
    - "MetricKind.rhr title reads RHR (compact)"
  artifacts:
    - path: "ios/OpenWhoop/Charts/MetricKind.swift"
      provides: ".sleepPerformance metric case + updated dailyCases"
      contains: "sleepPerformance"
    - path: "ios/OpenWhoopTests/MetricKindTests.swift"
      provides: "Unit tests for sleepPerformance mapping and dailyCases membership"
      min_lines: 20
  key_links:
    - from: "ios/OpenWhoop/Charts/MetricKind.swift"
      to: "DailyMetric.efficiency"
      via: "value(from:) returns metric.efficiency * 100"
      pattern: "metric.efficiency"
    - from: "ios/OpenWhoop/Tabs/TrendsView.swift"
      to: "ios/OpenWhoop/Charts/MetricKind.swift"
      via: "MetricKind.dailyCases loop"
      pattern: "dailyCases"
---

<objective>
Make Sleep Performance the primary sleep metric in Trends and fix the remaining MetricKind-driven and
Today/DayDetail label nits. Add `MetricKind.sleepPerformance` (D-09/D-10) sourced from
`DailyMetric.efficiency` (which on the server represents sleep performance 0–1), swap it into
`dailyCases` in place of `.sleepDuration` (D-11), point the Today→sleep NavigationLink at the new kind,
shorten `MetricKind.rhr` title "Resting HR" → "RHR" plus the Today/DayDetail standalone "Resting HR"
MetricCard titles (D-14), and fix the DayDetailView "Skin Temp Dev" card → "SKIN TEMP" with a
from-baseline unit (D-13, DayDetail side). TrendsView consumes `dailyCases` via a `ForEach`, so the
new Sleep Performance card appears automatically once the enum is updated.

This plan does NOT touch `SleepView.swift` (plan 01 owns it) — so it runs in parallel with plans 01/02.

Purpose: Sleep Performance as the headline Trends sleep metric (IOS-05) + WHOOP label parity (UI-03).
Output: updated MetricKind, MetricKindTests scaffold, TodayView link + RHR, DayDetailView skin-temp + RHR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-ui-parity/12-CONTEXT.md
@ios/OpenWhoop/Charts/MetricKind.swift
@ios/OpenWhoop/Tabs/TrendsView.swift
@ios/OpenWhoop/Tabs/TodayView.swift
@ios/OpenWhoop/Tabs/DayDetailView.swift
@ios/OpenWhoopTests/MetricsRepositoryTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MetricKind.sleepPerformance, update dailyCases, scaffold MetricKindTests</name>
  <files>ios/OpenWhoop/Charts/MetricKind.swift, ios/OpenWhoopTests/MetricKindTests.swift</files>
  <behavior>
    - MetricKind.sleepPerformance.title == "Sleep Performance"; unit == "%"
    - .sleepPerformance.format(87) == "87%"; .formatShort(87) == "87"
    - .sleepPerformance.value(from: metric with efficiency 0.87) == 87 (0–1 → 0–100)
    - .sleepPerformance.value(from: metric with efficiency nil) == nil
    - .sleepPerformance.fixedYDomain == 0...100; markType == .bar
    - MetricKind.dailyCases contains .sleepPerformance and does NOT contain .sleepDuration
    - MetricKind.rhr.title == "RHR"
  </behavior>
  <action>
    Automated test scaffold is MISSING — create `ios/OpenWhoopTests/MetricKindTests.swift` FIRST with
    the behavior cases above (import XCTest + the app module; construct DailyMetric the same way
    MetricsRepositoryTests does), then implement until green.

    In `MetricKind.swift` (D-09/D-10/D-11/D-14): add `case sleepPerformance`. Add it to every switch:
    title "Sleep Performance", unit "%", color `WH.Color.sleepPurple`, markType `.bar`, fixedYDomain
    `0...100`, format `"%.0f%%"`, formatShort `"%.0f"`, and `value(from:)` returning
    `metric.efficiency.map { $0 * 100 }` (efficiency is stored 0–1; on the server it represents sleep
    performance per D-10). Update `dailyCases` to replace `.sleepDuration` with `.sleepPerformance`
    (D-11) in the sleep slot (after `.strain`). Per Claude's Discretion (CONTEXT.md) KEEP the
    `.sleepDuration` case defined in the enum (so TodayView/DayDetail references still compile) but
    remove it from the `dailyCases` array. Change the `.rhr` title from "Resting HR" to "RHR" (D-14).
  </action>
  <verify>
    <automated>cd ios && test -f OpenWhoopTests/MetricKindTests.swift && grep -q "sleepPerformance" OpenWhoop/Charts/MetricKind.swift && grep -n "dailyCases" OpenWhoop/Charts/MetricKind.swift | grep -q "sleepPerformance" && ! grep -E "dailyCases.*sleepDuration" OpenWhoop/Charts/MetricKind.swift && xcodegen generate --project . >/dev/null 2>&1 && xcodebuild test -project OpenWhoop.xcodeproj -scheme OpenWhoop -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:OpenWhoopTests/MetricKindTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5 | grep -q "TEST SUCCEEDED" && echo OK</automated>
  </verify>
  <done>MetricKind.sleepPerformance exists with correct title/unit/format/domain/value mapping; .rhr title is "RHR"; dailyCases includes sleepPerformance and excludes sleepDuration; MetricKindTests created and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Today sleep link + fix DayDetail SKIN TEMP and RHR labels</name>
  <files>ios/OpenWhoop/Tabs/TodayView.swift, ios/OpenWhoop/Tabs/DayDetailView.swift</files>
  <action>
    - TodayView.swift (~line 94): change `MetricDetailView(kind: .sleepDuration)` to
      `MetricDetailView(kind: .sleepPerformance)` so the Today sleep card drills into the primary
      sleep metric (D-09/D-11).
    - TodayView.swift (~line 238): change the standalone `MetricCard(title: "Resting HR", ...)` to
      `title: "RHR"` (D-14).
    - DayDetailView.swift (~line 173): change `MetricCard(title: "Resting HR", ...)` to `title: "RHR"`
      (D-14).
    - DayDetailView.swift (~line 191): change the skin-temp `MetricCard(title: "Skin Temp Dev", ...)`
      to `title: "SKIN TEMP"` and set its `unit` to `"°C from baseline"` when `metric.skinTempDevC != nil`
      (keep nil unit when absent) per D-13. Keep the `%+.1f` deviation value formatting.
    RecoveryCard.swift already uses the compact "RHR" stat label — no change needed there.
  </action>
  <verify>
    <automated>cd ios && grep -q "kind: .sleepPerformance" OpenWhoop/Tabs/TodayView.swift && grep -q "SKIN TEMP" OpenWhoop/Tabs/DayDetailView.swift && ! grep -q "Skin Temp Dev" OpenWhoop/Tabs/DayDetailView.swift && ! grep -q '"Resting HR"' OpenWhoop/Tabs/DayDetailView.swift && xcodegen generate --project . >/dev/null 2>&1 && xcodebuild -project OpenWhoop.xcodeproj -scheme OpenWhoop -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && echo OK</automated>
  </verify>
  <done>Today sleep card links to .sleepPerformance and shows RHR; DayDetail skin-temp card reads "SKIN TEMP" with from-baseline unit and shows RHR; app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | UI metric labels and chart wiring only; DailyMetric.efficiency is already-validated cached server data, no new input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | sleep performance value | accept | Same locally-cached DailyMetric data already displayed elsewhere; no new exposure |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- MetricKind.sleepPerformance present; dailyCases swaps sleepDuration→sleepPerformance; MetricKindTests pass.
- TodayView links to .sleepPerformance; DayDetail SKIN TEMP + RHR labels present, old labels gone.
- `xcodebuild` BUILD SUCCEEDED.
</verification>

<success_criteria>
- Trends tab shows Sleep Performance (0–100%) as the primary sleep card.
- Skin temp card reads "SKIN TEMP" with from-baseline framing; RHR shown compactly (WHOOP parity).
- App builds and unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-ui-parity/12-03-SUMMARY.md` when done.
</output>
