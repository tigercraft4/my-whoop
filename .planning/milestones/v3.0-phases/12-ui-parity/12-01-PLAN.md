---
phase: 12-ui-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/OpenWhoop/Tabs/SleepView.swift
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "Sleep view hero shows the label SLEEP PERFORMANCE (not SLEEP EFFICIENCY)"
    - "Sleep view hero shows the label HOURS OF SLEEP (not TIME ASLEEP)"
    - "Sleep view stats row shows the label SLEEP LATENCY (not LATENCY)"
    - "Sleep view skin-temp card reads SKIN TEMP with a from-baseline unit"
    - "HypnogramView renders an AWAKE lane as the 4th sleep stage in muted grey"
  artifacts:
    - path: "ios/OpenWhoop/Tabs/SleepView.swift"
      provides: "Corrected WHOOP-parity sleep labels"
      contains: "SLEEP PERFORMANCE"
  key_links:
    - from: "ios/OpenWhoop/Tabs/SleepView.swift"
      to: "ios/OpenWhoop/Tabs/HypnogramView.swift"
      via: "stage breakdown render"
      pattern: "HypnogramView"
---

<objective>
Fix every WHOOP-parity label that lives in `SleepView.swift`: "SLEEP EFFICIENCY" → "SLEEP PERFORMANCE"
(D-01), "TIME ASLEEP" → "HOURS OF SLEEP" (D-02), "LATENCY" → "SLEEP LATENCY" (D-12), and the
"Skin Temp Dev" card → "SKIN TEMP" with a from-baseline unit (D-13, SleepView side). Confirm the
AWAKE 4th stage in HypnogramView already meets D-04 (it does — `laneOrder` includes "wake" and the
legend shows AWAKE in `WH.Color.stageWake` grey #6E727E); no code change expected for D-04, only
confirmation.

This plan owns `SleepView.swift` exclusively so it can run in parallel with plans 02 and 03.

Purpose: WHOOP 1:1 label parity on the Sleep tab (UI-04).
Output: Updated `SleepView.swift`; D-04 confirmed already satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-ui-parity/12-CONTEXT.md
@ios/OpenWhoop/Tabs/SleepView.swift
@ios/OpenWhoop/Tabs/HypnogramView.swift
@ios/OpenWhoop/Design/DesignTokens.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct all sleep labels in SleepView.swift</name>
  <files>ios/OpenWhoop/Tabs/SleepView.swift</files>
  <action>
    In `SleepView.swift` make these string-literal edits only — never touch calculation logic
    (D-03 mandates the efficiency/performance VALUE stays exactly as computed):
    - Line ~155: `Text("SLEEP EFFICIENCY")` → `Text("SLEEP PERFORMANCE")` (D-01).
    - Line ~177: `Text("TIME ASLEEP")` → `Text("HOURS OF SLEEP")` (D-02).
    - Line ~251: `smallStatTile(label: "LATENCY", value: latencyMin)` →
      `smallStatTile(label: "SLEEP LATENCY", value: latencyMin)` (D-12).
    - Line ~340: the skin-temp `MetricCard(title: "Skin Temp Dev", ...)` → `title: "SKIN TEMP"` and
      change its `unit` to `"°C from baseline"` when `daily?.skinTempDevC != nil` (keep nil unit when
      absent) per D-13. Keep the `%+.1f` deviation value formatting — absolute skin temp is out of
      scope (needs PROTO-12 VERIFIED).
    Leave `SleepCard.swift` untouched: it already uses the correct labels.
  </action>
  <verify>
    <automated>cd ios && grep -cE "SLEEP PERFORMANCE|HOURS OF SLEEP|SLEEP LATENCY|SKIN TEMP" OpenWhoop/Tabs/SleepView.swift | (read n; [ "$n" -ge 4 ] && ! grep -qE 'SLEEP EFFICIENCY|TIME ASLEEP|"LATENCY"|Skin Temp Dev' OpenWhoop/Tabs/SleepView.swift && echo OK)</automated>
  </verify>
  <done>SleepView.swift contains SLEEP PERFORMANCE, HOURS OF SLEEP, SLEEP LATENCY, SKIN TEMP; no occurrences of SLEEP EFFICIENCY, TIME ASLEEP, the standalone "LATENCY" label, or "Skin Temp Dev" remain.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm AWAKE 4th stage (D-04) and build the app</name>
  <files>ios/OpenWhoop/Tabs/HypnogramView.swift</files>
  <action>
    Confirm D-04 is already satisfied — do not add a stage. Verify in `HypnogramView.swift`:
    `laneOrder = ["wake", "rem", "light", "deep"]` (4 lanes), `laneLabel("wake")` returns "AWAKE",
    `stageColor("wake")` resolves to `WH.Color.stageWake` (grey #6E727E per D-04 / specifics), and the
    legend includes the ("AWAKE", WH.Color.stageWake) entry. If all hold, record D-04 as "already
    satisfied in Phase 9" in the SUMMARY — no edit. Then regenerate the Xcode project and build to
    confirm the Task 1 label edits compile.
  </action>
  <verify>
    <automated>cd ios && grep -c '"wake"' OpenWhoop/Tabs/HypnogramView.swift && xcodegen generate --project . >/dev/null 2>&1 && xcodebuild -project OpenWhoop.xcodeproj -scheme OpenWhoop -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && echo OK</automated>
  </verify>
  <done>HypnogramView confirmed to render the AWAKE lane in grey (D-04 satisfied); app builds (BUILD SUCCEEDED) with the Task 1 label changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Pure UI label changes; no new data flow, no external input, no network, no package installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | SleepView.swift label strings | accept | UI-only string edits, no behavior change; covered by build verification |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `grep` confirms the four new labels present and old labels absent in SleepView.swift.
- `xcodebuild` reports BUILD SUCCEEDED after `xcodegen generate`.
- D-04 confirmed by grep of HypnogramView "wake" lane + AWAKE legend entry.
</verification>

<success_criteria>
- Sleep tab shows SLEEP PERFORMANCE, HOURS OF SLEEP, SLEEP LATENCY, SKIN TEMP (WHOOP parity).
- AWAKE renders as the 4th hypnogram stage in grey (D-04 confirmed).
- App builds cleanly.
</success_criteria>

<output>
Create `.planning/phases/12-ui-parity/12-01-SUMMARY.md` when done.
</output>
