---
phase: 02-gatt-survey-bonding
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - re/survey_5/bond_5.py
  - re/survey_5/hr_5.py
autonomous: false
requirements: [PROTO-02, PROTO-03]
user_setup: []

must_haves:
  truths:
    - "A Bleak script bonds to the WHOOP 5.0 from a fresh state without the official app, using the confirmed-write trick on the cmd-in characteristic"
    - "A Bleak script subscribes to standard HR (0x2A37) and prints live BPM values, and reads battery (0x2A19)"
  artifacts:
    - path: "re/survey_5/bond_5.py"
      provides: "confirmed-write bonding trigger on the 5.0 cmd-in characteristic"
      contains: "response=True"
      min_lines: 25
    - path: "re/survey_5/hr_5.py"
      provides: "HR notify subscription + battery read"
      contains: "parse_hr"
      min_lines: 30
  key_links:
    - from: "re/survey_5/bond_5.py"
      to: "cmd-in characteristic (write_gatt_char response=True)"
      via: "ATT Write Request bonding trigger"
      pattern: "write_gatt_char\\(.*response=True"
    - from: "re/survey_5/hr_5.py"
      to: "0x2A37 / 0x2A19"
      via: "start_notify + read_gatt_char"
      pattern: "2a37"
---

<objective>
Replicate WHOOP 5.0 bonding without the official app and confirm standard HR/battery streaming over Bleak. `bond_5.py` ports `re/bond_attempt.py`: it issues the confirmed-write trick (ATT Write Request, `response=True`) on the 5.0 cmd-in characteristic to trigger CoreBluetooth "just-works" bonding (D-03). `hr_5.py` ports `re/standard_ble.py`: it reads battery (0x2A19) and subscribes to HR (0x2A37), confirming the bond unlocks notifications end-to-end.

Purpose: Bonding without the app (PROTO-02) and live HR streaming (PROTO-03, ROADMAP criteria 3-4) are the two behavioral proofs of this phase. These scripts depend on the confirmed UUIDs from Waves 1-2 — they replace the UUID placeholders with the real constants.

Output: `re/survey_5/bond_5.py` and `re/survey_5/hr_5.py`, both run live against the strap after a fresh-state setup (Forget Device).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gatt-survey-bonding/02-RESEARCH.md
@.planning/phases/02-gatt-survey-bonding/02-PATTERNS.md
@re/bond_attempt.py
@re/standard_ble.py
@re/capture/ios-packetlogger.md
@.planning/phases/02-gatt-survey-bonding/02-01-SUMMARY.md
@.planning/phases/02-gatt-survey-bonding/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write bond_5.py — confirmed-write bonding trigger for WHOOP 5.0</name>
  <files>re/survey_5/bond_5.py</files>
  <read_first>
    - re/bond_attempt.py (exact analog — copy scan/connect/pair-attempt/confirmed-write/notify structure)
    - .planning/phases/02-gatt-survey-bonding/02-PATTERNS.md (§bond_5.py — drop sys.path hack + WhoopPacket import; use raw payload; raw-hex notification callback; §Shared Patterns write_gatt_char response=True)
    - .planning/phases/02-gatt-survey-bonding/02-RESEARCH.md (§Pattern 2 confirmed-write bonding; §Anti-Patterns — no client.pair() reliance, response must be True not False; §Common Pitfalls 2 & 3 — Mac-side bond removal, UUID changes)
    - .planning/phases/02-gatt-survey-bonding/02-01-SUMMARY.md (real cmd-in / cmd-resp / events characteristic UUIDs to substitute for placeholders)
  </read_first>
  <action>Create `re/survey_5/bond_5.py` as a port of `re/bond_attempt.py` (PATTERNS §bond_5.py). Specifics:
  - Import `from device_local_5 import DEVICE_UUID as ADDR`. Do NOT copy the `sys.path.insert(0, "whoomp/scripts")` hack or the `WhoopPacket` import — the 5.0 framing is unconfirmed (Phase 3 territory), so bond_5.py uses raw bytes only (PATTERNS §sys.path Hack — Do NOT Copy).
  - Define UUID constants `CMD_IN_5`, `CMD_RESP_5`, `EVENTS_5` with the REAL UUIDs confirmed in 02-01-SUMMARY (replace the placeholder fragments with the actual full 128-bit UUIDs). If the SUMMARY did not capture a particular UUID, leave the placeholder AND print a warning at startup.
  - In `main()`: scan with `find_device_by_address(ADDR, timeout=15.0)` + None guard; `async with BleakClient(dev) as client:`. First attempt `await client.pair()` wrapped in try/except — on macOS this raises NotImplementedError and that is expected (RESEARCH §Pattern 2, §State of the Art). Subscribe to CMD_RESP_5 and EVENTS_5 first with a raw-hex notification callback (drop the WhoopPacket parse — print the data hex only). Then issue the bonding trigger: a confirmed write on CMD_IN_5 with a single null byte payload and `response=True` — `response=True` is MANDATORY (Write Without Response does NOT trigger encryption negotiation). Wrap in try/except and print the outcome. `await asyncio.sleep(5)` to observe any BLE_BONDED event, then `asyncio.run(main())`.
  - In the module docstring, reference the fallback: if the confirmed-write does not trigger bonding, capture the official app's SMP handshake via PacketLogger per `re/capture/ios-packetlogger.md` and analyze with `tshark -Y btsmp` (D-03b, RESEARCH §SMP Capture Reference).
  Before the live run, perform fresh-state setup: Forget Device on iPhone AND remove the strap from macOS System Settings -> Bluetooth (Option+click -> Remove) per Pitfall 2, and force-quit the official WHOOP app per Pitfall 1.</action>
  <verify>
    <automated>re/survey_5/.venv/bin/python -c "import ast; ast.parse(open('re/survey_5/bond_5.py').read())" && grep -q 'from device_local_5 import DEVICE_UUID' re/survey_5/bond_5.py && grep -Eq 'write_gatt_char\(.*response=True' re/survey_5/bond_5.py && ! grep -q 'WhoopPacket' re/survey_5/bond_5.py && ! grep -q 'sys.path.insert' re/survey_5/bond_5.py</automated>
  </verify>
  <acceptance_criteria>
    - `re/survey_5/bond_5.py` parses as valid Python
    - Contains `write_gatt_char(` with `response=True` (the bonding trigger; response=True present, not response=False)
    - Imports `device_local_5` and does NOT import `WhoopPacket` or use `sys.path.insert`
    - Defines `CMD_IN_5` (and CMD_RESP_5, EVENTS_5) — real UUIDs from 02-01-SUMMARY where available
    - Wraps `client.pair()` in try/except (NotImplementedError tolerated on macOS)
    - Behavior (live, fresh state): script connects, sends the confirmed write, and the outcome (bonding dialog / BLE_BONDED event / silent success / fallback-to-SMP needed) is recorded in the SUMMARY. SMP packets visible in PacketLogger is the ROADMAP criterion 3 proof.
  </acceptance_criteria>
  <done>`bond_5.py` triggers fresh-state bonding to the WHOOP 5.0 via the confirmed-write trick without the official app, with the SMP/bond outcome recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Write hr_5.py — standard HR notify + battery read</name>
  <files>re/survey_5/hr_5.py</files>
  <read_first>
    - re/standard_ble.py (exact analog — copy parse_hr verbatim, copy battery read + HR notify main())
    - .planning/phases/02-gatt-survey-bonding/02-PATTERNS.md (§hr_5.py — copy parse_hr and main() verbatim, only change device_config -> device_local_5)
    - .planning/phases/02-gatt-survey-bonding/02-RESEARCH.md (§Pattern 3 HR notification; §Standard GATT Profiles 0x2A37 / 0x2A19; §Security Domain V5 — validate len(data) before indexing)
  </read_first>
  <action>Create `re/survey_5/hr_5.py` as a near-verbatim port of `re/standard_ble.py` (PATTERNS §hr_5.py). Specifics:
  - Change only the device import to `from device_local_5 import DEVICE_UUID as ADDR` (the standard GATT UUIDs are identical across 4.0/5.0).
  - Keep the standard UUID constants verbatim: BATTERY = `00002a19-0000-1000-8000-00805f9b34fb`, MANUF = `00002a29-0000-1000-8000-00805f9b34fb`, HR_MEAS = `00002a37-0000-1000-8000-00805f9b34fb`.
  - Copy `parse_hr(data)` verbatim — it already guards `len(data)` before indexing RR intervals (RESEARCH §Security Domain V5, satisfies input-validation on untrusted BLE notification data). Do not hand-roll a new parser (RESEARCH §Don't Hand-Roll).
  - Copy `main()`: read battery via `read_gatt_char(BATTERY)` and print the integer percentage; attempt manufacturer read in try/except; subscribe to HR_MEAS with `start_notify`, print HR bpm + RR list per notification for ~12s, then `stop_notify`. Use the bare `asyncio.run(main())` entry (no `if __name__` guard — project convention).
  Run live AFTER bond_5.py in the same fresh-state session (standard characteristics work unbonded per RESEARCH, but running after bond confirms the full chain). HR reads 0 when off-wrist/charging — wear the strap during the run to confirm live BPM.</action>
  <verify>
    <automated>re/survey_5/.venv/bin/python -c "import ast; ast.parse(open('re/survey_5/hr_5.py').read())" && grep -q 'from device_local_5 import DEVICE_UUID' re/survey_5/hr_5.py && grep -qi '00002a37-0000-1000-8000-00805f9b34fb' re/survey_5/hr_5.py && grep -qi '00002a19-0000-1000-8000-00805f9b34fb' re/survey_5/hr_5.py && grep -q 'def parse_hr' re/survey_5/hr_5.py && grep -q 'start_notify' re/survey_5/hr_5.py</automated>
  </verify>
  <acceptance_criteria>
    - `re/survey_5/hr_5.py` parses as valid Python
    - Imports `device_local_5` (not device_config)
    - Contains the standard HR UUID `00002a37-0000-1000-8000-00805f9b34fb` and battery UUID `00002a19-0000-1000-8000-00805f9b34fb`
    - Contains `def parse_hr` and `start_notify`
    - Behavior (live, strap worn, after bond): script prints a battery percentage and at least one `HR=<nonzero bpm>` notification line — recorded in SUMMARY (ROADMAP criterion 4 proof)
  </acceptance_criteria>
  <done>`hr_5.py` reads battery and streams live HR BPM over a Bleak subscription, confirming bond + notifications work end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE strap -> bond_5.py / hr_5.py | untrusted BLE notification data crosses into our process |
| SMP handshake -> PacketLogger evidence (fallback) | bonding key material (LTK/IRK/CSRK) + BD_ADDR present in capture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | parse_hr indexing untrusted HR notification bytes | mitigate | parse_hr guards `len(data)` before each index (RESEARCH §Security Domain V5); copied verbatim from validated 4.0 code, not hand-rolled |
| T-02-08 | Information Disclosure | SMP fallback capture (LTK/IRK/CSRK, BD_ADDR) | mitigate | If fallback used, raw .pklg stays in gitignored re/capture/samples/; only scrubbed hex committed; key material removed per D-02 + DISCLAIMER §2 (enforced in plan 02-04 evidence task) |
| T-02-09 | Spoofing | confirmed-write bonding to wrong/rogue peripheral | accept | ADDR is the user's own CoreBluetooth UUID from device_local_5; single-user local RE context, no untrusted peers |
</threat_model>

<verification>
- `bond_5.py` ast-parses, contains the `response=True` confirmed write, no WhoopPacket/sys.path
- `hr_5.py` ast-parses, contains standard HR/battery UUIDs + parse_hr + start_notify
- Live: bond outcome recorded (dialog/event/silent/fallback), battery % printed, live HR BPM observed while worn
</verification>

<success_criteria>
- Fresh-state bonding without the official app via confirmed-write trick (PROTO-02, ROADMAP criterion 3)
- Standard HR streams live BPM via Bleak subscription; battery readable (PROTO-03, ROADMAP criterion 4)
</success_criteria>

<output>
Create `.planning/phases/02-gatt-survey-bonding/02-03-SUMMARY.md` when done. Record: bond outcome (which path worked, whether SMP fallback was needed), sample battery %, sample live HR BPM, and whether the custom service became visible to survey_gatt_5.py only after bonding.
</output>
