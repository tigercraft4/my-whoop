---
phase: 02-gatt-survey-bonding
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - re/survey_5/__init__.py
  - re/survey_5/device_local_5.example.py
  - re/survey_5/requirements.txt
  - re/survey_5/survey_gatt_5.py
autonomous: false
requirements: [PROTO-01, PROTO-03]
user_setup: []

must_haves:
  truths:
    - "A Python 3.11+ venv with bleak 3.0.2 exists at re/survey_5/.venv and can import bleak"
    - "device_local_5.example.py committed as a placeholder template; device_local_5.py (real) stays gitignored"
    - "survey_gatt_5.py programmatically enumerates the WHOOP 5.0 GATT table and flags the Phase 1 handles 0x099b/0x099d/0x09a3"
  artifacts:
    - path: "re/survey_5/survey_gatt_5.py"
      provides: "Programmatic GATT enumeration + handle->UUID cross-check + JSON dump"
      contains: "client.services"
      min_lines: 30
    - path: "re/survey_5/device_local_5.example.py"
      provides: "committed placeholder template for the 5.0 device identity"
      contains: "DEVICE_UUID"
    - path: "re/survey_5/requirements.txt"
      provides: "pinned bleak version"
      contains: "bleak==3.0.2"
    - path: "re/survey_5/__init__.py"
      provides: "package marker"
  key_links:
    - from: "re/survey_5/survey_gatt_5.py"
      to: "device_local_5.DEVICE_UUID"
      via: "import"
      pattern: "from device_local_5 import DEVICE_UUID"
    - from: "re/survey_5/survey_gatt_5.py"
      to: "Phase 1 handles"
      via: "handle membership check"
      pattern: "0x099b"
---

<objective>
Build the `re/survey_5/` scaffolding and the programmatic GATT survey script. This wave establishes the isolated 5.0 RE workspace (D-04): a Python 3.11 venv with bleak 3.0.2 (the system Python 3.9.6 is too old per Pitfall 6), the committed device-identity template, and `survey_gatt_5.py` — a port of `re/gatt_dump.py` that enumerates `client.services` and cross-checks the Phase 1 handles, producing a machine-readable `gatt_dump_5.json`.

Purpose: nRF Connect (Wave 1) gives the visual ground truth; this script gives the programmatic cross-check that the same UUIDs/handles are reachable from Bleak — the toolchain all of Wave 3 (bonding, HR) depends on. Confirms PROTO-01/PROTO-03 from the macOS/Bleak side.

Output: `re/survey_5/` directory with `__init__.py`, `device_local_5.example.py`, `requirements.txt`, `survey_gatt_5.py`, and a working `.venv` (gitignored). A live run produces `gatt_dump_5.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gatt-survey-bonding/02-RESEARCH.md
@.planning/phases/02-gatt-survey-bonding/02-PATTERNS.md
@re/gatt_dump.py
@re/device_local.example.py
@.planning/phases/02-gatt-survey-bonding/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create re/survey_5/ scaffolding and Python 3.11 venv with bleak 3.0.2</name>
  <files>re/survey_5/__init__.py, re/survey_5/device_local_5.example.py, re/survey_5/requirements.txt</files>
  <read_first>
    - re/device_local.example.py (analog — copy exact structure, adapt comments for 5.0 + re/survey_5/ path)
    - .planning/phases/02-gatt-survey-bonding/02-PATTERNS.md (§device_local_5.example.py; §requirements.txt + venv setup; §__init__.py)
    - .planning/phases/02-gatt-survey-bonding/02-RESEARCH.md (§Standard Stack install block; §Common Pitfalls Pitfall 6 — Python version)
    - .gitignore (confirm re/survey_5/device_local_5.py is already ignored from plan 02-01)
  </read_first>
  <action>Create the `re/survey_5/` workspace (D-04):
  1. `re/survey_5/__init__.py` — empty file (package marker).
  2. `re/survey_5/device_local_5.example.py` — copy `re/device_local.example.py` and adapt the two comment lines to reference `re/survey_5/device_local_5.py` and "WHOOP 5.0 strap"; keep the three placeholder assignments `DEVICE_UUID`/`DEVICE_MAC`/`DEVICE_SERIAL` with all-zero placeholder values (per D-04b and PATTERNS §device_local_5.example.py). Do NOT create the real `device_local_5.py` here — it is gitignored and holds real identifiers the developer fills locally.
  3. `re/survey_5/requirements.txt` — single pinned line `bleak==3.0.2`.
  4. Create the venv. The system Python is 3.9.6 which cannot run bleak 3.x (requires >=3.10). Check for Homebrew Python 3.11 with `command -v python3.11`; if absent, run `brew install python@3.11` (this is a CLI install Claude performs, not a human-only step). Then create and populate the venv: `python3.11 -m venv re/survey_5/.venv` and `re/survey_5/.venv/bin/pip install -r re/survey_5/requirements.txt`. The `.venv/` is already gitignored via the `**/.venv/` glob — do not add a gitignore entry.
  Note: bleak was slopcheck-flagged `[SUS]` as a typosquat of "black" but RESEARCH §Package Legitimacy Audit confirms it is a FALSE POSITIVE (8 years on PyPI, 376K weekly downloads, github.com/hbldh/bleak) — disposition Approved, no install checkpoint required.</action>
  <verify>
    <automated>test -f re/survey_5/__init__.py && grep -q 'DEVICE_UUID' re/survey_5/device_local_5.example.py && grep -qx 'bleak==3.0.2' re/survey_5/requirements.txt && re/survey_5/.venv/bin/python -c "import sys,bleak; assert sys.version_info[:2] >= (3,10); assert bleak.__version__ == '3.0.2'; print('ok', bleak.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `re/survey_5/__init__.py` exists (may be empty)
    - `re/survey_5/device_local_5.example.py` contains `DEVICE_UUID`, `DEVICE_MAC`, `DEVICE_SERIAL` with all-zero placeholder values and a comment referencing `re/survey_5/device_local_5.py`
    - `re/survey_5/requirements.txt` contains exactly `bleak==3.0.2`
    - `re/survey_5/.venv/bin/python -c "import bleak; print(bleak.__version__)"` prints `3.0.2`
    - venv Python is >= 3.10: `re/survey_5/.venv/bin/python -c "import sys; print(sys.version_info >= (3,10))"` prints `True`
    - The real `re/survey_5/device_local_5.py` was NOT created/committed (`git status --porcelain re/survey_5/device_local_5.py` is empty)
  </acceptance_criteria>
  <done>The 5.0 RE workspace exists with a working Python 3.11 venv (bleak 3.0.2 importable), the committed device-identity template, and pinned requirements — with the real identity file still absent and gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Write survey_gatt_5.py — programmatic GATT enumeration with Phase 1 handle cross-check</name>
  <files>re/survey_5/survey_gatt_5.py</files>
  <read_first>
    - re/gatt_dump.py (exact analog — copy the scan/connect/iterate structure)
    - .planning/phases/02-gatt-survey-bonding/02-PATTERNS.md (§survey_gatt_5.py — additions required: handle field, Phase 1 match flag, JSON output; Shared Patterns: device discovery, BleakClient context manager, client.services property, asyncio entry point)
    - .planning/phases/02-gatt-survey-bonding/02-RESEARCH.md (§Code Examples Full GATT Dump skeleton; §Pattern 1; §Pattern 4 handle resolution; §Anti-Patterns — no get_services(), UUID-not-MAC on macOS, no passive scan)
    - .planning/phases/02-gatt-survey-bonding/02-01-SUMMARY.md (confirmed service/characteristic UUIDs from Wave 1 nRF Connect data)
  </read_first>
  <action>Create `re/survey_5/survey_gatt_5.py` as a port of `re/gatt_dump.py` adapted for 5.0 (PATTERNS §survey_gatt_5.py). Specifics:
  - Import `from device_local_5 import DEVICE_UUID as ADDR` (direct import per D-04b — do NOT use the 4.0 `device_config` env-var fallback).
  - Use `BleakScanner.find_device_by_address(ADDR, timeout=15.0)` with the `if dev is None: print(...); return` guard (Shared Patterns: BLE Device Discovery). On macOS `ADDR` is a CoreBluetooth UUID string, never a MAC (Anti-Patterns).
  - Define `PHASE1_HANDLES = {0x099b, 0x099d, 0x09a3}` and, while iterating `client.services` (a property in bleak 3.x — never `await client.get_services()`), print each service and characteristic INCLUDING the integer handle as `0x{handle:04x}`, and append a visible flag (e.g. `<<< PHASE1 MATCH`) when `char.handle in PHASE1_HANDLES`. This closes the Phase 1 handle->UUID loop (D-02).
  - Build a result dict (device name/address + nested services->characteristics->descriptors with uuid/handle/properties) and write it to `re/survey_5/gatt_dump_5.json` via `json.dump(..., indent=2)`.
  - Use `async with BleakClient(dev) as client:` and the bare `asyncio.run(main())` entry (no `if __name__ == "__main__"` guard — project convention).
  After writing, run the script live against the strap (official WHOOP app force-quit per Pitfall 1; if the custom service is not visible unbonded per Pitfall 4 and Wave 1 noted that, defer the live run until after bond_5.py in Wave 3 and record this in the SUMMARY). Do not commit `gatt_dump_5.json` if it embeds the real device name/address — treat it as a local artifact unless scrubbed.</action>
  <verify>
    <automated>re/survey_5/.venv/bin/python -c "import ast; ast.parse(open('re/survey_5/survey_gatt_5.py').read())" && grep -q 'from device_local_5 import DEVICE_UUID' re/survey_5/survey_gatt_5.py && grep -q 'client.services' re/survey_5/survey_gatt_5.py && grep -q '0x099b' re/survey_5/survey_gatt_5.py && ! grep -q 'get_services' re/survey_5/survey_gatt_5.py</automated>
  </verify>
  <acceptance_criteria>
    - `re/survey_5/survey_gatt_5.py` parses as valid Python (ast.parse exit 0)
    - Contains `from device_local_5 import DEVICE_UUID as ADDR` (direct import, not device_config)
    - Iterates `client.services` (property) and does NOT call `get_services()`
    - Contains `0x099b`, `0x099d`, `0x09a3` in a PHASE1_HANDLES set and a match flag in the print path
    - Writes `re/survey_5/gatt_dump_5.json` (json.dump present)
    - Behavior (when run live with strap awake + WHOOP app closed): script exits 0, prints services/characteristics with `0x` handles, and flags at least one Phase 1 handle match (recorded in SUMMARY)
  </acceptance_criteria>
  <done>`survey_gatt_5.py` enumerates the 5.0 GATT table programmatically, prints handles, flags the Phase 1 handles, and dumps the table to JSON — cross-checking the nRF Connect data from the Bleak side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install -> venv | third-party package (bleak) installed from PyPI |
| BLE strap -> survey_gatt_5.py | untrusted GATT data read into our process |
| real device identity -> git | DEVICE_UUID/MAC/SERIAL must stay in gitignored device_local_5.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | pip install bleak==3.0.2 | mitigate | RESEARCH §Package Legitimacy Audit: bleak [SUS] is a confirmed false positive (8yr PyPI, 376K/wk dl, github.com/hbldh/bleak); pinned to exact 3.0.2; no other packages installed |
| T-02-04 | Information Disclosure | re/survey_5/device_local_5.py | mitigate | gitignored (verified in plan 02-01); only the all-zero example.py is committed; acceptance criterion asserts the real file is not staged |
| T-02-05 | Information Disclosure | gatt_dump_5.json (device name/address) | mitigate | Treated as a local artifact; not committed unless scrubbed; .venv and dump remain outside committed evidence |
| T-02-06 | Tampering | BLE GATT data read by survey script | accept | Enumeration only reads metadata (UUIDs/handles/properties), no payload parsing/indexing in this script — low risk |
</threat_model>

<verification>
- `re/survey_5/.venv/bin/python -c "import bleak"` succeeds (3.0.2)
- `survey_gatt_5.py` ast-parses and imports device_local_5
- Live run (strap on, WHOOP app closed) prints the GATT table with handles and flags the Phase 1 handles
- Real device identity file not committed
</verification>

<success_criteria>
- Isolated 5.0 RE workspace exists (D-04) with working bleak 3.0.2 venv
- Programmatic GATT enumeration cross-checks nRF Connect UUIDs (ROADMAP criterion 1, Bleak half; PROTO-01/PROTO-03)
- Phase 1 handle->UUID loop closed programmatically (D-02)
</success_criteria>

<output>
Create `.planning/phases/02-gatt-survey-bonding/02-02-SUMMARY.md` when done. Record the live-run outcome (handles flagged, whether custom service required bonding first) so Wave 3 knows whether to bond before re-running the survey.
</output>
