---
phase: 02-gatt-survey-bonding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - FINDINGS_5.md
autonomous: false
requirements: [PROTO-01, PROTO-02, PROTO-03]
user_setup: []

must_haves:
  truths:
    - "WHOOP 5.0 custom service UUID and all visible characteristic UUIDs are recorded for the user's specific device"
    - "Presence or absence of the legacy 61080001-... service is recorded as an explicit verdict"
    - "Phase 1 handles 0x099b/0x099d/0x09a3 are mapped to their characteristic UUIDs from the nRF Connect browser"
  artifacts:
    - path: "FINDINGS_5.md"
      provides: "Phase 2 bootstrap: GATT map, legacy UUID verdict, handle->UUID map, status table"
      contains: "## 1. GATT Map"
      min_lines: 40
    - path: ".gitignore"
      provides: "gitignore entry for the real device identity file"
      contains: "re/survey_5/device_local_5.py"
  key_links:
    - from: "FINDINGS_5.md ## 5. Handle"
      to: "re/capture/evidence/2026-05-30-ios.meta.yaml handles"
      via: "handle->UUID transcription from nRF Connect"
      pattern: "0x099b"
---

<objective>
Bootstrap the Phase 2 evidence trail BEFORE any Python code is written. This is the manual/runbook wave: the developer uses nRF Connect on the iPhone to visually enumerate the WHOOP 5.0 GATT surface, records the confirmed UUIDs and the legacy-UUID verdict into a new `FINDINGS_5.md`, closes the Phase 1 handle->UUID loop, and adds the gitignore entry for the upcoming real device-identity file.

Purpose: nRF Connect is the ground-truth UUID source (D-01). Phase 1 captured handles (0x099b/0x099d/0x09a3) but not UUIDs — this wave resolves them so the Wave 2/3 Bleak scripts can be written against confirmed UUID constants instead of placeholders.

Output: `FINDINGS_5.md` (new, committed) with confirmed GATT map + legacy UUID verdict + handle map; `.gitignore` updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gatt-survey-bonding/02-CONTEXT.md
@.planning/phases/02-gatt-survey-bonding/02-RESEARCH.md
@FINDINGS.md
@re/capture/evidence/2026-05-30-ios.meta.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: nRF Connect GATT enumeration of the physical WHOOP 5.0 device</name>
  <files>(none — manual device interaction; output is developer notes consumed by Tasks 2-3)</files>
  <what-built>
  This is a manual device-interaction task — no code. The developer drives nRF Connect on the iPhone to enumerate the live WHOOP 5.0 GATT table and capture the UUID/handle data that Tasks 2-3 will transcribe into FINDINGS_5.md.
  </what-built>
  <read_first>
    - .planning/phases/02-gatt-survey-bonding/02-RESEARCH.md (§Common Pitfalls Pitfall 1 — close official app first; §Handle -> UUID Resolution Strategy; §Pattern 4)
    - .planning/phases/02-gatt-survey-bonding/02-CONTEXT.md (D-01, D-01b, D-01c, D-02)
    - re/capture/evidence/2026-05-30-ios.meta.yaml (Phase 1 handles 0x099b/0x099d/0x09a3 to resolve)
  </read_first>
  <action>Perform the manual nRF Connect GATT enumeration described in the verification steps below. This task produces NO committed files — its output is the developer's transcribable notes (service UUID(s), characteristic UUIDs/handles/properties, legacy-UUID verdict, and the handle->UUID map for 0x099b/0x099d/0x09a3) which Tasks 2-3 transcribe into FINDINGS_5.md. Follow D-01/D-01b/D-01c/D-02 and RESEARCH Pitfall 1 (force-quit the official WHOOP app before connecting, since only one BLE central can hold the strap at a time).</action>
  <how-to-verify>
    1. On the iPhone, force-quit the official WHOOP app (App Switcher -> swipe away the WHOOP card). Per Pitfall 1, only one BLE central can connect at a time — leaving the WHOOP app open will make nRF Connect see the device advertising but fail to connect.
    2. Open nRF Connect (install free from App Store if absent; LightBlue Explorer is the documented fallback per RESEARCH §Environment Availability).
    3. Scan and connect to the WHOOP 5.0 strap. If "Connect" spinner never resolves, re-confirm the official app is fully closed.
    4. Expand all services. For EACH service record: service UUID (full 128-bit) + handle. For EACH characteristic record: UUID, handle, properties (read/write/notify/indicate).
    5. Explicitly check for the legacy `61080001-...` service: record "present" or "absent" (D-01c, RESEARCH assumption A2). This one observation has Phase 5 implications.
    6. Locate the three Phase 1 handles 0x099b, 0x099d, 0x09a3 in the table and note their UUIDs (closes the Phase 1 loop per D-02).
    7. Note presence/absence of all seven expected characteristics: cmd-in (...0002), cmd-resp (...0003), events (...0004), data (...0005), diagnostics (...0007), standard HR (0x2A37), standard battery (0x2A19). Also note Device Information service 0x180A manufacturer (0x2A29) if visible (RESEARCH §Standard GATT Profiles).
    8. Capture this as raw notes (screenshots are LOCAL ONLY per evidence policy — never committed). The transcription into committed files happens in Tasks 2-3.
  </how-to-verify>
  <verify>
    <human-check>Developer has nRF Connect notes covering: full service UUID(s), all characteristic UUIDs + handles + properties, legacy 61080001 verdict, and the UUIDs for handles 0x099b/0x099d/0x09a3.</human-check>
  </verify>
  <acceptance_criteria>
    - Developer has recorded the full custom service UUID(s) for the WHOOP 5.0
    - All characteristic UUIDs + handles + properties are captured
    - Legacy `61080001-...` verdict recorded as explicit "present" or "absent"
    - Handles 0x099b/0x099d/0x09a3 each mapped to a characteristic UUID
  </acceptance_criteria>
  <resume-signal>Type "approved" with the confirmed UUIDs, or describe what nRF Connect showed (e.g., "could not connect", "custom service not visible until bonded")</resume-signal>
  <done>Developer holds a complete, transcribable nRF Connect GATT enumeration: service UUID(s), all characteristic UUIDs + handles + properties, legacy-UUID verdict, and handle->UUID mapping for 0x099b/0x099d/0x09a3.</done>
</task>

<task type="auto">
  <name>Task 2: Add device_local_5.py gitignore entry</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current state — the `re/device_local.py` entry under "Personal device identity for RE scripts" is the analog block to extend)
    - .planning/phases/02-gatt-survey-bonding/02-PATTERNS.md (§gitignore Update Required)
  </read_first>
  <action>Add the line `re/survey_5/device_local_5.py` to `.gitignore`, immediately after the existing `re/device_local.py` line inside the "Personal device identity for RE scripts" comment block. This pre-empts the real 5.0 device-identity file (created in Wave 2) from ever being committed, mirroring the established 4.0 pattern (D-04b). Do NOT add a `.venv` entry — `**/.venv/` already covers `re/survey_5/.venv`.</action>
  <verify>
    <automated>grep -qx 're/survey_5/device_local_5.py' .gitignore && git check-ignore re/survey_5/device_local_5.py</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains an exact line `re/survey_5/device_local_5.py`
    - `git check-ignore re/survey_5/device_local_5.py` prints that path (exit 0), proving the rule is active
    - `.gitignore` still contains `re/device_local.py` (the 4.0 entry was not removed)
  </acceptance_criteria>
  <done>`re/survey_5/device_local_5.py` is gitignored and verified via `git check-ignore`.</done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap FINDINGS_5.md with confirmed UUIDs, legacy verdict, and handle map</name>
  <files>FINDINGS_5.md</files>
  <read_first>
    - FINDINGS.md (4.0 analog — mirror header, "Status at a glance" table, and section heading style; do NOT copy 4.0 UUIDs)
    - .planning/phases/02-gatt-survey-bonding/02-RESEARCH.md (§FINDINGS_5.md Structure — the authoritative section outline)
    - .planning/phases/02-gatt-survey-bonding/02-PATTERNS.md (§FINDINGS_5.md — document header + status table + section structure patterns)
    - re/capture/evidence/2026-05-30-ios.meta.yaml (Phase 1 handles to cross-reference in section 5)
  </read_first>
  <action>Create `FINDINGS_5.md` at the repo root, mirroring `FINDINGS.md` structure but for WHOOP 5.0. Use the section outline from RESEARCH §FINDINGS_5.md Structure. Required sections with these exact headings: `## Status at a glance` (capability->status table), `## 1. GATT Map` (service UUID + characteristic UUID/handle/properties table — fill from the Task 1 nRF Connect data), `## 2. Legacy UUID Verdict` (one-line "present" or "absent" verdict for `61080001-...` with observation date, per D-01c), `## 3. Bonding` (mark "pending Wave 3" for now), `## 4. Standard Characteristics` (mark HR/battery "pending Wave 3"), `## 5. Handle -> UUID Map` (table mapping 0x099b/0x099d/0x09a3 to their characteristic UUIDs from Task 1, closing the Phase 1 loop per D-02), `## 6. Open Questions / Phase 3 Inputs`. Header line must use `[REDACTED]` for serial and macOS BLE UUID — never commit real device identifiers (evidence policy, DISCLAIMER §2). If Task 1 found the custom service is NOT visible until bonded (Pitfall 4), record that explicitly in section 1 and section 6 so Wave 3 runs bond_5.py before survey_gatt_5.py.</action>
  <verify>
    <automated>test -f FINDINGS_5.md && grep -q '^## 1. GATT Map' FINDINGS_5.md && grep -q '^## 2. Legacy UUID Verdict' FINDINGS_5.md && grep -q '^## 5. Handle' FINDINGS_5.md && grep -q '0x099b' FINDINGS_5.md && grep -Eq 'present|absent' FINDINGS_5.md</automated>
  </verify>
  <acceptance_criteria>
    - `FINDINGS_5.md` exists at repo root with all six numbered section headings plus `## Status at a glance`
    - Section 2 contains a literal `present` or `absent` verdict string for the legacy `61080001-...` service
    - Section 5 references handle `0x099b` (and 0x099d, 0x09a3) mapped to characteristic UUIDs
    - File contains NO real device serial or macOS BLE UUID: `grep -iE '[0-9A-F]{2}(:[0-9A-F]{2}){5}' FINDINGS_5.md` returns no real MAC (placeholder/REDACTED only)
    - `FINDINGS_5.md` is at least 40 lines
  </acceptance_criteria>
  <done>`FINDINGS_5.md` exists with the confirmed GATT map, an explicit legacy-UUID verdict, and the closed Phase 1 handle->UUID map, with no raw device identifiers committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| physical strap -> nRF Connect (iPhone) | BLE GATT data crosses from the device; observed, not parsed by our code |
| developer notes -> committed FINDINGS_5.md | Device-identifying material (serial, BLE UUID, MAC) must not cross into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | FINDINGS_5.md committed to git | mitigate | Header uses `[REDACTED]` for serial + BLE UUID; acceptance criterion greps for raw MAC pattern and fails build if present (evidence policy, DISCLAIMER §2) |
| T-02-02 | Information Disclosure | future re/survey_5/device_local_5.py | mitigate | Task 2 adds gitignore entry and verifies with `git check-ignore` before the real file is ever created in Wave 2 |
| T-02-03 | Information Disclosure | nRF Connect screenshots | accept | Screenshots kept LOCAL only per evidence policy; never enter the repo, so no in-repo control needed |
</threat_model>

<verification>
- `FINDINGS_5.md` exists with sections 1, 2, 5 populated from real device data
- Legacy UUID verdict is explicit (present/absent)
- `git check-ignore re/survey_5/device_local_5.py` succeeds
- No raw device identifiers in any committed file
</verification>

<success_criteria>
- nRF Connect enumeration complete on the user's physical WHOOP 5.0 (ROADMAP criterion 1, partial — visual half)
- Legacy `61080001-...` presence/absence recorded (ROADMAP criterion 2; PROTO-01)
- Phase 1 handles mapped to UUIDs (D-02)
- Gitignore protects the upcoming real device-identity file
</success_criteria>

<output>
Create `.planning/phases/02-gatt-survey-bonding/02-01-SUMMARY.md` when done. Record the confirmed service UUID, full characteristic UUID list, and legacy-UUID verdict in the summary so Wave 2/3 planners have the real UUID constants.
</output>
