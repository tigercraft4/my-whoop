---
phase: 04-protocol-decode-schema
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - protocol/whoop_protocol_5.json
  - scripts/sync-schema-5.sh
  - re/survey_5/frames_5_golden.json
  - FINDINGS_5.md
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05]
must_haves:
  truths:
    - "whoop_protocol_5.json enums are populated with the four r52 maps (PacketType, MetadataType, EventNumber, CommandNumber) copied verbatim"
    - "whoop_protocol_5.json packets section has body field maps for every decoded stream, every field tagged with epoch + provenance note + confidence (VERIFIED/HYPOTHESIS)"
    - "scripts/sync-schema-5.sh validates and copies the schema to the Swift bundle Resources dir (creating it), exits 0"
    - "FINDINGS_5.md has a §Phase 4 section documenting command surface, decoded streams, dual-epoch, and historical offload"
    - "Golden fixtures span every decoded packet type and round-trip through decode_5.parse_body_5"
  artifacts:
    - path: "protocol/whoop_protocol_5.json"
      provides: "Complete canonical 5.0 schema: enums + packets, every field epoch/provenance/confidence tagged"
      contains: "CommandNumber"
    - path: "scripts/sync-schema-5.sh"
      provides: "Schema → Swift bundle sync with JSON validation"
      contains: "whoop_protocol_5.json"
    - path: "FINDINGS_5.md"
      provides: "§Phase 4 canonical protocol reference (command surface, streams, timestamps, offload)"
      contains: "Phase 4"
  key_links:
    - from: "scripts/sync-schema-5.sh"
      to: "Packages/WhoopProtocol/Sources/WhoopProtocol/Resources/whoop_protocol_5.json"
      via: "cp after JSON validation + mkdir -p"
      pattern: "Resources/whoop_protocol_5.json"
    - from: "protocol/whoop_protocol_5.json"
      to: "the Plan 03/04 decode verdicts"
      via: "confidence + epoch + provenance per field"
      pattern: "\"confidence\""
---

<objective>
Complete the canonical schema and documentation deliverables that fold together everything Plans 01-04 decoded: populate `protocol/whoop_protocol_5.json` (enums + packets, every field epoch/provenance/confidence tagged), author `scripts/sync-schema-5.sh` (the Swift-bundle sync), ensure golden fixtures cover every decoded packet type, and extend `FINDINGS_5.md` with §Phase 4. This is the wiring/integration wave — it consumes the command-surface and event/metadata/historical/dual-epoch decode (Plan 03) and the biometric verdicts (Plan 04).

The confidence policy is the load-bearing discipline: VERIFIED only for ground-truth-matched streams (HR strap / app display, D-08); HYPOTHESIS for unobserved-but-r52-expected commands (D-07) and capture-absent biometrics (SpO₂/temp/respiration, A1-A3) with honest provenance.

Purpose: Produce the single source of truth (`whoop_protocol_5.json`) that Phase 5's Swift package consumes, plus the canonical FINDINGS_5.md §Phase 4 reference the Phase 5 planner reads.
Output: complete `protocol/whoop_protocol_5.json`, `scripts/sync-schema-5.sh`, expanded golden fixtures, `FINDINGS_5.md §Phase 4`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-decode-schema/04-CONTEXT.md
@.planning/phases/04-protocol-decode-schema/04-RESEARCH.md
@.planning/phases/04-protocol-decode-schema/04-PATTERNS.md
@.planning/phases/04-protocol-decode-schema/04-03-SUMMARY.md
@.planning/phases/04-protocol-decode-schema/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate whoop_protocol_5.json enums + packets (SCHEMA-01, SCHEMA-02)</name>
  <files>protocol/whoop_protocol_5.json</files>
  <read_first>
    - protocol/whoop_protocol_5.json (the v0 schema — empty enums {} and packets {}; preserve version/schema_note/framing_notes/envelope/gatt/firmware_revision)
    - protocol/whoop_protocol.json (4.0 enums lines 3-60 to copy verbatim; packets.REALTIME_DATA field-map shape lines 69-76)
    - .planning/phases/04-protocol-decode-schema/04-03-SUMMARY.md (command-surface observed-vs-r52, event/metadata/historical/dual-epoch decode results)
    - .planning/phases/04-protocol-decode-schema/04-04-SUMMARY.md (biometric stream VERIFIED/HYPOTHESIS verdicts)
    - Packages/WhoopProtocol/Sources/WhoopProtocol/Schema.swift (the loader — keep field key names off/len/dtype/name/cat/enum compatible)
    - .planning/phases/04-protocol-decode-schema/04-PATTERNS.md (schema section: enum verbatim copy, field-map shape, epoch/note/confidence on EVERY field, offset-base reconciliation note)
  </read_first>
  <action>
    Populate the empty `"enums": {}` in `protocol/whoop_protocol_5.json` by copying the FOUR r52 enum maps VERBATIM from `protocol/whoop_protocol.json` enums: PacketType, MetadataType, EventNumber, CommandNumber (WG50_r52 confirmed identical — do NOT re-derive).

    Populate `"packets": {}` with body field maps for every decoded stream from Plans 03-04: COMMAND_RESPONSE (incl. GET_DATA_RANGE dual-epoch), EVENT (incl. BATTERY_LEVEL, STRAP_CONDITION_REPORT, device-epoch u32 at body[8]), METADATA (HISTORY_START/END), CONSOLE_LOGS, and biometric packets REALTIME_DATA (type 40 HR/RR) + REALTIME_RAW_DATA (type 43 IMU) + any SpO2/temp/respiration packet ONLY where Plan 04 marked it VERIFIED. Use the 4.0 field-map shape (off/len/dtype/name/cat/enum) and add to EVERY field the three SCHEMA-02 tags: `"epoch"` ("unix" vs "device"), `"note"` (provenance — which capture/frame proved it), and `"confidence"` ("VERIFIED" for ground-truth-matched, "HYPOTHESIS" otherwise).

    Reconcile the offset base explicitly: the 4.0 field offsets are frame-relative (e.g. REALTIME_DATA timestamp at frame offset 6); the 5.0 body adds role+token+type+seq+cmd before the payload, so express each field `off` against the 5.0 body and add a `schema_note` (or per-packet note) recording the offset base. Add unobserved-but-r52-expected commands as HYPOTHESIS entries with note "not observed in captures, expected from r52 enum map" (D-07). Keep SpO2/temp/respiration HYPOTHESIS unless Plan 04 proved the bytes (Pitfall 5). Validate the result is valid JSON.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('protocol/whoop_protocol_5.json')); assert set(['PacketType','MetadataType','EventNumber','CommandNumber']).issubset(d['enums']); assert len(d['packets'])>=4; print('enums', list(d['enums']), 'packets', list(d['packets']))"</automated>
  </verify>
  <acceptance_criteria>
    - `protocol/whoop_protocol_5.json` is valid JSON and `enums` contains all four r52 maps (the verify assertion passes)
    - `packets` has >= 4 decoded packet types with body field maps
    - Every field in `packets` carries `confidence`, `epoch`, and `note` keys (spot-check: grep -c '"confidence"' protocol/whoop_protocol_5.json >= field count; grep -c '"epoch"' >= 1)
    - A unix-epoch field is tagged `"epoch": "unix"` and an EVENT device-epoch field tagged `"epoch": "device"` (PROTO-15 dual-epoch represented)
    - SpO2/temp/respiration fields, if present, are `"confidence": "HYPOTHESIS"` unless Plan 04 SUMMARY recorded a VERIFIED capture (no fabricated VERIFIED biometrics)
    - The envelope/gatt/firmware_revision sections from v0 are preserved unchanged
  </acceptance_criteria>
  <done>whoop_protocol_5.json is complete: four r52 enum maps copied verbatim, packet body field maps for every decoded stream, every field epoch/provenance/confidence tagged, dual-epoch represented, biometrics honestly confidence-tagged (SCHEMA-01, SCHEMA-02).</done>
</task>

<task type="auto">
  <name>Task 2: scripts/sync-schema-5.sh + golden fixtures coverage (SCHEMA-05, SCHEMA-04)</name>
  <files>scripts/sync-schema-5.sh, re/survey_5/frames_5_golden.json</files>
  <read_first>
    - scripts/sync-schema.sh (the 4.0 template — mirror it, retarget to _5 paths; decide on the home-server branch)
    - protocol/whoop_protocol_5.json (the schema this script syncs)
    - re/survey_5/frames_5_golden.json (the curated corpus — confirm it spans every decoded packet type from Plans 03-04)
    - re/survey_5/decode_5.py (parse_body_5 — fixtures must round-trip through it)
    - .planning/phases/04-protocol-decode-schema/04-PATTERNS.md (sync-schema-5.sh section: add JSON validation before cp, mkdir -p the Resources dir which does not yet exist; golden fixture sidecar pattern)
    - .planning/phases/04-protocol-decode-schema/04-RESEARCH.md (Code Examples sync-schema-5.sh; Runtime State Inventory — Resources/whoop_protocol_5.json does not yet exist)
  </read_first>
  <action>
    Create `scripts/sync-schema-5.sh` mirroring `scripts/sync-schema.sh`, retargeted: `CANON="$ROOT/protocol/whoop_protocol_5.json"`, `PKG="$ROOT/Packages/WhoopProtocol/Sources/WhoopProtocol/Resources/whoop_protocol_5.json"`. Include `set -euo pipefail`, `mkdir -p "$(dirname "$PKG")"` (load-bearing — the 5.0 Resources file does not yet exist), a JSON-validation step before copy (`python3 -c "import json; json.load(open('$CANON'))"` — fail with a clear message and non-zero exit if invalid), then `cp` and an echo confirmation. For the home-server branch: keep it conditional/guarded like the 4.0 script (only sync if the target exists) OR omit it if the 5.0 home-server consumer does not exist yet — document the choice in a comment. Make the script executable (`chmod +x scripts/sync-schema-5.sh`).

    Golden fixtures (SCHEMA-04): confirm `re/survey_5/frames_5_golden.json` (expanded in Plan 01, biometrics added by Plan 04) spans EVERY decoded packet type — COMMAND_RESPONSE, EVENT (incl. battery), METADATA, CONSOLE_LOGS, and any VERIFIED biometric (REALTIME_DATA / REALTIME_RAW_DATA / SpO2/temp). If any decoded type lacks a fixture exemplar, add one curated exemplar (wrapper-stripped, redacted — no key material, evidence policy). The fixtures must round-trip: re-decode each fixture body through `decode_5.parse_body_5` and confirm its resolved type matches its stored `stream_type`. iOS pklg is the primary source; Android btsnoop cross-source is deferred (D).
  </action>
  <verify>
    <automated>bash scripts/sync-schema-5.sh && test -f Packages/WhoopProtocol/Sources/WhoopProtocol/Resources/whoop_protocol_5.json && cd re/survey_5 && python -c "import json; from decode_5 import parse_body_5; d=json.load(open('frames_5_golden.json')); print('types:', sorted({r['stream_type'] for r in d}))"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/sync-schema-5.sh` exists, is executable (test -x scripts/sync-schema-5.sh), and contains `whoop_protocol_5.json` + `mkdir -p` + a JSON validation line
    - `bash scripts/sync-schema-5.sh` exits 0 and creates `Packages/WhoopProtocol/Sources/WhoopProtocol/Resources/whoop_protocol_5.json`
    - The synced bundle file is byte-identical to `protocol/whoop_protocol_5.json` (diff exits 0)
    - `re/survey_5/frames_5_golden.json` spans at least COMMAND_RESPONSE, EVENT, METADATA, CONSOLE_LOGS in its stream_type set (verify output lists them)
    - Every fixture body of length >= 7 round-trips through parse_body_5 without error
  </acceptance_criteria>
  <done>sync-schema-5.sh validates and syncs the canonical schema to the (newly created) Swift bundle Resources dir; the golden fixtures span every decoded packet type and round-trip through the decoder (SCHEMA-04, SCHEMA-05).</done>
</task>

<task type="auto">
  <name>Task 3: Extend FINDINGS_5.md with §Phase 4 (SCHEMA-03)</name>
  <files>FINDINGS_5.md</files>
  <read_first>
    - FINDINGS_5.md (the §7 Phase 3 section lines 166-211 — the section shape to mirror: subsections + Committed artifacts + verdict)
    - FINDINGS.md (4.0 §5 per-stream subsection template — Decoded data streams)
    - .planning/phases/04-protocol-decode-schema/04-03-SUMMARY.md (command surface, events/battery, metadata/historical, dual-epoch results)
    - .planning/phases/04-protocol-decode-schema/04-04-SUMMARY.md (biometric stream verdicts)
    - .planning/phases/04-protocol-decode-schema/04-PATTERNS.md (FINDINGS_5.md section: subsections per stream, Committed artifacts list, confidence-per-stream table)
  </read_first>
  <action>
    Append a new `## Phase 4` section (or `## 8. Decode & Schema (Phase 4)`) to `FINDINGS_5.md`, mirroring the §7 / 4.0 §5 structure. Include subsections:
    - Body layout confirmed (offset-4, role/token/type/seq/cmd/payload, NO inner CRC) — the corrected D-01.
    - Command surface (PROTO-06): observed-vs-r52 reconciliation summary, the 14 reused IDs status, capture-analysis method (D-06) + HYPOTHESIS for unobserved (D-07).
    - Events incl. battery (PROTO-08/09): decoded EventNumbers, battery layout, device-epoch.
    - Dual-epoch timestamps (PROTO-15): Unix (GET_DATA_RANGE) vs device-epoch (event body[8]).
    - Historical offload protocol (PROTO-10): SEND_HISTORICAL_DATA (22), HISTORICAL_DATA_RESULT ack (23), HISTORY_START/END, trim cursor — documentation-only, live test deferred to Phase 5 (D-09).
    - Biometric streams (PROTO-07/11/12/13/14): per-stream verdict — HR/RR (VERIFIED vs strap), IMU, SpO2/temp/respiration with honest HYPOTHESIS + 4.0-cloud-computed provenance where absent.

    End with a "Committed artifacts" list (decode_5.py, command_surface_5.py, decode_streams_5.py, decode_biometrics_5.py, expanded frames_5_golden.json, whoop_protocol_5.json, sync-schema-5.sh, the biometric evidence sidecar) and a confidence-per-stream table. Apply DISCLAIMER §2: no BD_ADDR/serial/SMP keys/device UUID; protocol-structure facts only.
  </action>
  <verify>
    <automated>grep -c "Phase 4" FINDINGS_5.md && grep -Eic "command surface|historical offload|dual-epoch" FINDINGS_5.md</automated>
  </verify>
  <acceptance_criteria>
    - `FINDINGS_5.md` contains a Phase 4 section heading (grep "Phase 4" >= 1)
    - The section documents command surface, dual-epoch, and historical offload (grep -Eic "command surface|historical offload|dual-epoch" >= 3)
    - The section has a Committed artifacts list naming whoop_protocol_5.json and the decode scripts
    - The section has a confidence-per-stream table marking biometrics VERIFIED/HYPOTHESIS consistent with Plan 04's verdicts
    - No device identity committed (grep -ci "REDACTED\|protocol-structure" FINDINGS_5.md confirms the DISCLAIMER discipline is restated, or no raw identifier appears)
  </acceptance_criteria>
  <done>FINDINGS_5.md §Phase 4 is the canonical protocol reference: body layout, command surface, events/battery, dual-epoch, historical offload, and per-stream biometric verdicts, with a committed-artifacts list and confidence table — the document the Phase 5 planner reads (SCHEMA-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decode verdicts → committed schema/docs | Risk of leaking device identity or fabricating VERIFIED confidence into the canonical source of truth |
| canonical schema → Swift bundle | sync script writes into the package Resources dir; must validate JSON first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering (data integrity) | a field tagged VERIFIED without ground-truth support, or a fabricated biometric offset entering the canonical schema | mitigate | Confidence policy enforced: VERIFIED only where Plan 03/04 SUMMARY recorded ground-truth match; SpO2/temp/respiration HYPOTHESIS unless captured (Pitfall 5) |
| T-04-02 | Information Disclosure | committing BD_ADDR/serial/SMP keys into schema, fixtures, or FINDINGS_5.md | mitigate | DISCLAIMER §2: only protocol-structure facts; fixtures wrapper-stripped/redacted; no key material in any committed artifact |
| T-04-06 | Tampering | sync-schema-5.sh copying invalid JSON into the Swift bundle | mitigate | JSON validation step before cp; script exits non-zero on invalid JSON |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs; stdlib (json) + bash + existing decoders only |
</threat_model>

<verification>
- `python3 -c "import json; json.load(open('protocol/whoop_protocol_5.json'))"` succeeds; enums + packets populated, every field epoch/provenance/confidence tagged
- `bash scripts/sync-schema-5.sh` exits 0 and the synced bundle is byte-identical to the canonical schema
- Golden fixtures span every decoded packet type and round-trip through parse_body_5
- FINDINGS_5.md §Phase 4 documents command surface, streams, dual-epoch, and historical offload
</verification>

<success_criteria>
- SCHEMA-01/02 complete schema with full enums + packets, every field tagged
- SCHEMA-05 sync-schema-5.sh syncs to the Swift bundle with JSON validation
- SCHEMA-04 golden fixtures cover every decoded packet type, round-trip verified
- SCHEMA-03 FINDINGS_5.md §Phase 4 is the canonical protocol reference for Phase 5
</success_criteria>

<output>
Create `.planning/phases/04-protocol-decode-schema/04-05-SUMMARY.md` when done
</output>
