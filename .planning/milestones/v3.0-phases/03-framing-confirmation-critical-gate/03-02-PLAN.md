---
phase: 03-framing-confirmation-critical-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - protocol/whoop_protocol_5.json
autonomous: true
requirements: [PROTO-05]
must_haves:
  truths:
    - "protocol/whoop_protocol_5.json exists with a framing/envelope section describing the Maverick wrapper, not the 4.0 inner frame"
    - "The JSON carries the GATT constants (custom service UUID + 5 characteristic UUIDs + legacy-ABSENT verdict) as single source of truth for Phase 5"
    - "Every framing field is confidence-tagged VERIFIED or HYPOTHESIS; the trailer checksum is tagged HYPOTHESIS/OPEN"
    - "firmware_revision WG50_r52 is recorded and tagged VERIFIED"
  artifacts:
    - path: "protocol/whoop_protocol_5.json"
      provides: "v0 canonical 5.0 schema: framing + GATT + firmware, confidence-tagged"
      contains: "FD4B0001-CCE1-4033-93CE-002D5875F58A"
  key_links:
    - from: "protocol/whoop_protocol_5.json"
      to: "Phase 5 Swift/Python schema loaders"
      via: "top-level keys compatible with 4.0 whoop_protocol.json layout"
      pattern: "\"version\""
---

<objective>
Author `protocol/whoop_protocol_5.json` v0 — the canonical 5.0 schema envelope. It mirrors the 4.0 `whoop_protocol.json` top-level layout (so the Phase 5 Swift/Python loaders stay compatible) but describes the Maverick OUTER WRAPPER (not the 4.0 inner frame), carries the GATT constants as single source of truth (D-04b), records `firmware_revision: WG50_r52`, and confidence-tags every item (VERIFIED for confirmed, HYPOTHESIS/OPEN for the trailer checksum). Addresses PROTO-05 (wrapper structure documented in `whoop_protocol_5.json`).

Purpose: Phase 5 Swift (`WhoopProtocol`) and Python (`whoop_protocol`) packages import this JSON — not a markdown file (D-04b). It must be the single committed source of truth for the 5.0 GATT UUIDs and the wrapper layout.
Output: `protocol/whoop_protocol_5.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-framing-confirmation-critical-gate/03-RESEARCH.md
@.planning/phases/03-framing-confirmation-critical-gate/03-PATTERNS.md
@protocol/whoop_protocol.json
@re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author whoop_protocol_5.json v0 (framing + GATT + firmware, confidence-tagged)</name>
  <files>protocol/whoop_protocol_5.json</files>
  <read_first>
    - protocol/whoop_protocol.json — 4.0 top-level layout to mirror (`version`, `enums`, `envelope`, `packets`) and the envelope-entry shape (`{off, len, name, cat}`, lines 61-67)
    - re/capture/evidence/2026-05-30-gatt-survey-5.meta.yaml — verbatim GATT UUID values (custom_service_uuid + characteristic_uuids block, lines 9-19) to copy as single source of truth
    - .planning/phases/03-framing-confirmation-critical-gate/03-RESEARCH.md — Finding 4 (wrapper offset layout), Finding 6 (trailer OPEN), D-04 schema scope
    - .planning/phases/03-framing-confirmation-critical-gate/03-PATTERNS.md — the protocol/whoop_protocol_5.json pattern section (envelope-as-wrapper guidance, confidence tagging)
    - FINDINGS_5.md — §1 GATT map + §2 legacy-ABSENT verdict for the legacy_61080001 field value
  </read_first>
  <action>
    Create `protocol/whoop_protocol_5.json` reusing the 4.0 top-level skeleton (`version`, `enums`, `envelope`, `packets`) so the existing `Schema.swift` loader stays compatible. Set `version: 0` (v0 envelope). Keep `enums` and `packets` present but minimal/empty objects in v0 (Phase 4 populates them) — do NOT invent packet field maps in this phase.

    Build the `envelope` array to describe the Maverick OUTER WRAPPER per RESEARCH Finding 4 (NOT the 4.0 inner frame): entry `{off:0, len:1, name:"SOF", cat:"frame"}` value 0xAA; `{off:1, len:1, name:"version", cat:"frame"}` value 0x01; `{off:2, len:2, name:"length", cat:"frame"}` u16-LE = body length; `{off:4, len:1, name:"role", cat:"frame"}` 0x00=cmd-in write / 0x01=notify; then the flat body region; and a trailing 4-byte trailer entry named `trailer`. Add a `confidence` key to each envelope entry: `VERIFIED` for SOF/version/length/role (per D-04 — these passed structural validation on 5028 frames), and `HYPOTHESIS` (with a `note` like "checksum algorithm OPEN; standard CRC16/CRC32 variants ruled out") for the trailer entry per RESEARCH Finding 6. Add a top-level `framing_notes` field stating that `total_len == length + 8` (4-byte header + body + 4-byte trailer) and that the body is FLAT with no nested 0xAA frame (Finding 5).

    Add a `gatt` object copying the GATT constants VERBATIM from `2026-05-30-gatt-survey-5.meta.yaml`: `service: "FD4B0001-CCE1-4033-93CE-002D5875F58A"`, a `characteristics` map (cmd_in FD4B0002, cmd_resp FD4B0003, events FD4B0004, data FD4B0005, diagnostics FD4B0007, heart_rate 00002A37-..., battery_level 00002A19-...), and `legacy_61080001: "ABSENT"`. Tag the gatt block `confidence: "VERIFIED"`.

    Add a `firmware_revision` object: `value: "WG50_r52"`, `source: "Device Information 0x2A27"`, `confidence: "VERIFIED"`.
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop && python3 -c "import json; d=json.load(open('protocol/whoop_protocol_5.json')); assert d['version']==0; assert d['gatt']['service']=='FD4B0001-CCE1-4033-93CE-002D5875F58A'; assert d['gatt']['legacy_61080001']=='ABSENT'; assert d['firmware_revision']['value']=='WG50_r52'; trailer=[e for e in d['envelope'] if 'trailer' in e['name'].lower()]; assert trailer and trailer[0]['confidence'].upper() in ('HYPOTHESIS','OPEN'), trailer; sof=[e for e in d['envelope'] if e['name']=='SOF']; assert sof[0]['confidence'].upper()=='VERIFIED'; print('schema OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `protocol/whoop_protocol_5.json` parses as valid JSON and `version == 0`
    - The verify command above prints `schema OK` and exits 0
    - `gatt.service == "FD4B0001-CCE1-4033-93CE-002D5875F58A"` and `gatt.legacy_61080001 == "ABSENT"`
    - The `envelope` entry named SOF has `confidence == "VERIFIED"`; the `trailer` entry has `confidence` of `HYPOTHESIS` or `OPEN`
    - `firmware_revision.value == "WG50_r52"` with `confidence == "VERIFIED"`
    - `grep -c '"version"' protocol/whoop_protocol_5.json` returns >= 1 (top-level key present for Schema.swift loader compatibility)
    - The envelope length entry is at `off: 2` (NOT `off: 1`) — confirms wrapper layout, not 4.0 inner frame (assert: `[e for e in d['envelope'] if e['name']=='length'][0]['off']==2`)
  </acceptance_criteria>
  <done>whoop_protocol_5.json v0 exists, parses, describes the Maverick wrapper (length at off 2), carries GATT constants verbatim + WG50_r52 firmware, and confidence-tags the trailer as HYPOTHESIS/OPEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Local-only, read-only RE/analysis tooling. This plan authors a static JSON file of uncopyrightable protocol facts and GATT UUIDs. No network, no untrusted input, no authentication surface, no crypto implementation.

| Boundary | Description |
|----------|-------------|
| (none) | Static schema authoring; no runtime input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | committed JSON | accept | The JSON contains only protocol structure + GATT UUIDs (uncopyrightable facts), no BD_ADDR / SMP keys / device identifiers. Device identity stays in gitignored device_local_5.py (DISCLAIMER §2). |

Security threat model is otherwise N/A for this phase: no auth, no network, no user-input surface.
</threat_model>

<verification>
- `protocol/whoop_protocol_5.json` is valid JSON, mirrors the 4.0 top-level skeleton, and is loadable by a 4.0-style schema loader
- Envelope describes the Maverick wrapper (length at off 2, role at off 4), not the 4.0 inner frame
- GATT constants match the verified evidence YAML verbatim
- Trailer tagged HYPOTHESIS/OPEN (does not assert a CRC algorithm — RESEARCH Finding 6)
</verification>

<success_criteria>
- `protocol/whoop_protocol_5.json` v0 committed with framing (wrapper) + GATT + firmware_revision
- Confidence tags present: VERIFIED for confirmed structure/GATT/firmware, HYPOTHESIS/OPEN for trailer checksum
- Top-level keys compatible with the 4.0 schema loader
</success_criteria>

<output>
Create `.planning/phases/03-framing-confirmation-critical-gate/03-02-SUMMARY.md` when done
</output>
