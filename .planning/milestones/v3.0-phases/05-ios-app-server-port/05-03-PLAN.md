---
phase: 05-ios-app-server-port
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/WhoopStore/Sources/WhoopStore/Database.swift
autonomous: true
requirements: [IOS-09]
must_haves:
  truths:
    - "WhoopStore migra para v8 adicionando colunas gx/gy/gz nullable a gravitySample"
    - "DBs existentes no iPhone migram sem perda de dados no proximo launch"
    - "As colunas gyro sao nullable (null ate frame tipo 43 ser capturada)"
    - "spo2Sample/skinTempSample mantem o formato ADC raw existente (D-08)"
  artifacts:
    - path: "Packages/WhoopStore/Sources/WhoopStore/Database.swift"
      provides: "migration v8 registada"
      contains: "registerMigration(\"v8\")"
  key_links:
    - from: "Database.swift makeMigrator()"
      to: "tabela gravitySample"
      via: "db.alter(table:) com t.add(column:)"
      pattern: "registerMigration\\(\"v8\"\\)"
---

<objective>
Adicionar a migration v8 ao WhoopStore que estende a tabela `gravitySample` com colunas giroscopio nullable `gx`, `gy`, `gz` (D-06, IOS-09). As colunas ficam null ate uma frame tipo 43 (REALTIME_RAW_DATA via TOGGLE_IMU_MODE) ser capturada — preparam o terreno para PROTO-14 sem exigir uma migration v9 futura. As tabelas spo2Sample/skinTempSample NAO sao alteradas (D-08: formato ADC raw mantido).

Purpose: O schema da app tem de aceitar os campos gyro que o GravitySample (estendido em 05-01) pode produzir. Sem v8, os inserts de GravitySample com gx/gy/gz falham no iPhone.
Output: makeMigrator() regista v8; abrir um WhoopStore aplica a migration idempotentemente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ios-app-server-port/05-CONTEXT.md
@.planning/phases/05-ios-app-server-port/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: registar migration v8 com colunas gyro nullable (D-06, IOS-09)</name>
  <files>Packages/WhoopStore/Sources/WhoopStore/Database.swift</files>
  <read_first>
    - Packages/WhoopStore/Sources/WhoopStore/Database.swift (padrao v6 ADD COLUMN nullable linhas 137-143; padrao v7 multiplas colunas nullable linhas 144-153; posicao do `return migrator` linha 155; makeMigrator())
  </read_first>
  <action>
    Em Database.swift, dentro de makeMigrator(), inserir `migrator.registerMigration("v8")` ENTRE a migration "v7" (linha 153) e o `return migrator` (linha 155). O corpo usa `try db.alter(table: "gravitySample")` e adiciona tres colunas via `t.add(column: "gx", .double)`, `t.add(column: "gy", .double)`, `t.add(column: "gz", .double)` — SEM `.notNull()` para serem nullable, seguindo exactamente o padrao de v7 (spo2Pct/skinTempDevC/respRateBpm). Adicionar comentario a explicar que as colunas ficam null ate REALTIME_RAW_DATA tipo 43 (TOGGLE_IMU_MODE) ser capturada (PROTO-14 HYPOTHESIS). NAO reordenar nem renumerar v1..v7. NAO adicionar coluna device_generation (D-07: fork 5.0-only, redundante na app). NAO alterar as tabelas/structs spo2Sample(red, ir) nem skinTempSample(raw) — D-08: SpO2 e skinTemp mantem o formato ADC raw existente (a conversao para SpO2%/°C e feita no servidor por units.py, nao na app); a v8 so toca em gravitySample.
  </action>
  <verify>
    <automated>cd Packages/WhoopStore && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'registerMigration("v8")' Packages/WhoopStore/Sources/WhoopStore/Database.swift` retorna 1
    - Database.swift contem `t.add(column: "gx", .double)` e idem gy/gz, todos sem `.notNull()`
    - A v8 aparece DEPOIS de v7 e ANTES de `return migrator` (ordem v1..v8 preservada)
    - `grep -q 'device_generation' Packages/WhoopStore/Sources/WhoopStore/Database.swift` falha (D-07)
    - A v8 nao toca em spo2Sample nem skinTempSample (D-08: formato raw inalterado) — apenas gravitySample
    - `cd Packages/WhoopStore && swift build` sai com codigo 0
    - `cd Packages/WhoopStore && swift test` sai com codigo 0 (migration aplica numa DB de teste)
  </acceptance_criteria>
  <done>v8 registada com gx/gy/gz nullable em gravitySample; spo2/skinTemp inalterados (D-08); build e testes passam; ordem de migrations intacta</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration GRDB → SQLite local | a migration corre sobre a DB do utilizador no App Sandbox iOS; nao ha input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | migration v8 sobre DB existente | mitigate | ADD COLUMN nullable e nao-destrutivo; GRDB corre migrations em transacao — falha faz rollback sem corromper dados existentes |
| T-05-03-02 | Information Disclosure | dados biometricos em SQLite | accept | WhoopStore reside no App Sandbox iOS (protecao do SO); dados sao do proprio utilizador; sem novo vector introduzido por colunas nullable |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero packages novos; GRDB ja presente (RESEARCH confirma) |
</threat_model>

<verification>
- `cd Packages/WhoopStore && swift build` sai com codigo 0
- `cd Packages/WhoopStore && swift test` aplica v8 numa DB limpa sem erro
- Colunas gx/gy/gz sao nullable; spo2/skinTemp inalterados (D-08)
</verification>

<success_criteria>
- IOS-09: WhoopStore tem migration v8 que adiciona os tipos de dados 5.0 (gyro columns) — schema migrado
</success_criteria>

<output>
Create `.planning/phases/05-ios-app-server-port/05-03-SUMMARY.md` when done
</output>
