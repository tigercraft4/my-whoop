---
phase: 05-ios-app-server-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/WhoopProtocol/Sources/WhoopProtocol/Schema.swift
  - Packages/WhoopProtocol/Sources/WhoopProtocol/WhoopProtocol.swift
  - Packages/WhoopProtocol/Sources/WhoopProtocol/Framing.swift
  - Packages/WhoopProtocol/Sources/WhoopProtocol/Interpreter.swift
  - Packages/WhoopProtocol/Sources/WhoopProtocol/Streams.swift
  - server/packages/whoop-protocol/whoop_protocol/schema.py
  - server/packages/whoop-protocol/whoop_protocol/schema/whoop_protocol_5.json
autonomous: true
requirements: [SWIFT-01, SWIFT-02, SWIFT-03, SWIFT-04, SWIFT-06]
must_haves:
  truths:
    - "loadSchema() carrega whoop_protocol_5.json (não o 4.0)"
    - "parseFrame() faz strip do Maverick wrapper internamente sem mudar a assinatura"
    - "GravitySample tem campos gx/gy/gz opcionais"
    - "O package Python whoop_protocol pode carregar o schema 5.0 via load_schema_5()"
  artifacts:
    - path: "Packages/WhoopProtocol/Sources/WhoopProtocol/Framing.swift"
      provides: "stripMaverick() pure function"
      contains: "func stripMaverick"
    - path: "server/packages/whoop-protocol/whoop_protocol/schema/whoop_protocol_5.json"
      provides: "schema 5.0 disponível ao package Python"
  key_links:
    - from: "Schema.swift loadSchema()"
      to: "Resources/whoop_protocol_5.json"
      via: "Bundle.module.url(forResource:)"
      pattern: "whoop_protocol_5"
    - from: "Interpreter.swift parseFrame()"
      to: "Framing.swift stripMaverick()"
      via: "chamada interna antes do schema decode"
      pattern: "stripMaverick"
---

<objective>
Portar o núcleo do decoder Swift (`Packages/WhoopProtocol/`) e o package Python espelho para WHOOP 5.0: `loadSchema()` aponta para `whoop_protocol_5.json` (D-01), `parseFrame()` faz strip do wrapper Maverick internamente (D-02), `GravitySample` ganha campos giroscópio opcionais (D-06 lado Swift), e o package Python `whoop_protocol` ganha um loader do schema 5.0 (SWIFT-06).

Purpose: O decoder é a fundação de toda a Fase 5 — a app iOS, os testes de paridade e o servidor dependem de bytes decodificados correctamente. Sem strip Maverick + offsets body-absolute, todos os streams são lidos em posições erradas.
Output: Decoder Swift 5.0 funcional (`swift build` passa) e package Python com `load_schema_5()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ios-app-server-port/05-CONTEXT.md
@.planning/phases/05-ios-app-server-port/05-RESEARCH.md
@.planning/phases/05-ios-app-server-port/05-PATTERNS.md
@FINDINGS_5.md
@re/survey_5/validate_frames_5.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stripMaverick() + parseFrame() Maverick path (D-02)</name>
  <files>Packages/WhoopProtocol/Sources/WhoopProtocol/Framing.swift, Packages/WhoopProtocol/Sources/WhoopProtocol/Interpreter.swift</files>
  <read_first>
    - Packages/WhoopProtocol/Sources/WhoopProtocol/Framing.swift (padrão de função pura: crc8/verifyFrame; helper privado u16le linhas 66-68)
    - Packages/WhoopProtocol/Sources/WhoopProtocol/Interpreter.swift (parseFrame linha 88; SOF check `frame.count < 8 || frame[0] != 0xAA` linha 90; type em frame[4] linha 101; seq em frame[5] linha 103)
    - re/survey_5/validate_frames_5.py (strip_maverick() reference — output byte-a-byte alvo)
    - FINDINGS_5.md (§7 Framing — layout Maverick `[0xAA][0x01][len u16-LE][body][trailer 4B]`, total_len == length + 8)
  </read_first>
  <behavior>
    - stripMaverick devolve nil quando frame.count < 9
    - stripMaverick devolve nil quando frame[0] != 0xAA
    - stripMaverick devolve nil quando frame[1] != 0x01
    - stripMaverick devolve nil quando frame.count != length + 8 (length = u16-LE em offset 2)
    - stripMaverick devolve frame[4..<4+length] quando o wrapper é válido
    - parseFrame sobre uma frame Maverick-wrapped lê ptype do body[4] e seq do body[5] (mesmos offsets numéricos do path 4.0)
    - parseFrame sobre uma frame não-wrapped (frame[1] != 0x01) processa pelo path existente sem strip
  </behavior>
  <action>
    Adicionar a função pública `func stripMaverick(_ frame: [UInt8]) -> [UInt8]?` a Framing.swift, APÓS verifyFrame(), seguindo o padrão de função pura existente (guard early-return, sem imports adicionais). Lógica: guard `frame.count >= 9 && frame[0] == 0xAA && frame[1] == 0x01`; `let length = Int(frame[2]) | (Int(frame[3]) << 8)`; guard `frame.count == length + 8`; return `Array(frame[4..<4+length])`. Deve produzir output byte-a-byte idêntico ao `strip_maverick()` de validate_frames_5.py.

    Em Interpreter.swift parseFrame() (linha 88): ANTES do SOF check existente (`frame.count < 8 || frame[0] != 0xAA`, linha 90), detectar Maverick com a condição `frame.count >= 9 && frame[0] == 0xAA && frame[1] == 0x01 && frame.count == (Int(frame[2]) | (Int(frame[3]) << 8)) + 8`. Se detectado, substituir a variável de trabalho pelo body stripped e processar pelo path schema-driven SEM chamar verifyFrame() (o body Maverick é plano, não tem inner CRC32). Manter os offsets `frame[4]=type` e `frame[5]=seq` — coincidem numericamente com body[4]/body[5]. NÃO re-executar o CRC8/CRC32 gate sobre o body stripped. Frames não-Maverick (frame[1] != 0x01) continuam pelo path existente inalterado.

    NÃO inlinar a implementação completa do parseFrame neste plano — replicar o padrão de detecção descrito e reutilizar a lógica schema-driven existente.
  </action>
  <verify>
    <automated>cd Packages/WhoopProtocol && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func stripMaverick' Packages/WhoopProtocol/Sources/WhoopProtocol/Framing.swift` retorna 1
    - `grep -q 'stripMaverick' Packages/WhoopProtocol/Sources/WhoopProtocol/Interpreter.swift` (parseFrame chama o strip)
    - Framing.swift contém `frame[4..<4 + length]` ou equivalente que devolve o body em offset 4
    - `cd Packages/WhoopProtocol && swift build` sai com código 0
    - Interpreter.swift NÃO chama verifyFrame() sobre o body stripped (o path Maverick salta o CRC gate)
  </acceptance_criteria>
  <done>stripMaverick() existe e é pura; parseFrame() faz strip interno de frames Maverick e processa o body plano sem mudar a assinatura pública; swift build passa</done>
</task>

<task type="auto">
  <name>Task 2: loadSchema() 5.0 + GravitySample gyro (D-01, D-06 Swift)</name>
  <files>Packages/WhoopProtocol/Sources/WhoopProtocol/Schema.swift, Packages/WhoopProtocol/Sources/WhoopProtocol/WhoopProtocol.swift, Packages/WhoopProtocol/Sources/WhoopProtocol/Streams.swift</files>
  <read_first>
    - Packages/WhoopProtocol/Sources/WhoopProtocol/Schema.swift (loadSchema() linha 176; forResource "whoop_protocol" linha 178; mensagens de erro linhas 179/185/191; cache singleton _cachedSchema)
    - Packages/WhoopProtocol/Sources/WhoopProtocol/WhoopProtocol.swift (schemaResourceURL() linha 8, forResource "whoop_protocol")
    - Packages/WhoopProtocol/Sources/WhoopProtocol/Streams.swift (GravitySample existente; BatterySample como padrão de campo Double? opcional; Streams.init(from:) com decodeIfPresent)
    - Packages/WhoopProtocol/Sources/WhoopProtocol/Resources/whoop_protocol_5.json (confirmar que o resource já está presente no target)
  </read_first>
  <action>
    Em Schema.swift linha 178: alterar `forResource: "whoop_protocol"` para `forResource: "whoop_protocol_5"` (D-01). Actualizar as três mensagens de erro (linhas 179, 185, 191) de `whoop_protocol.json` para `whoop_protocol_5.json`. NÃO tocar no padrão de cache singleton `_cachedSchema` nem no JSONDecoder — só o nome do resource muda.

    Em WhoopProtocol.swift linha 8: alterar `forResource: "whoop_protocol"` para `forResource: "whoop_protocol_5"` (corolário de D-01 — usado por SchemaSyncTests).

    Em Streams.swift: adicionar a `GravitySample` três campos opcionais `public let gx: Double?`, `public let gy: Double?`, `public let gz: Double?` (giroscópio; nil até frame tipo 43 REALTIME_RAW_DATA ser capturada — D-06/PROTO-14 HYPOTHESIS). Seguir o padrão de campo opcional de `BatterySample` (`soc: Double?`). Estender o `init` para aceitar `gx: Double? = nil, gy: Double? = nil, gz: Double? = nil` com defaults para que call sites existentes não quebrem. Garantir conformidade Codable preservada (campos opcionais usam decodeIfPresent automaticamente para structs sintetizadas).
  </action>
  <verify>
    <automated>cd Packages/WhoopProtocol && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'whoop_protocol_5' Packages/WhoopProtocol/Sources/WhoopProtocol/Schema.swift` retorna >= 1 (resource name + mensagens de erro)
    - `grep -q 'whoop_protocol"' Packages/WhoopProtocol/Sources/WhoopProtocol/Schema.swift` falha (nenhuma referência 4.0 restante no forResource)
    - `grep -q 'whoop_protocol_5' Packages/WhoopProtocol/Sources/WhoopProtocol/WhoopProtocol.swift`
    - `grep -q 'gx: Double?' Packages/WhoopProtocol/Sources/WhoopProtocol/Streams.swift` e idem gy/gz
    - GravitySample init aceita `gx: Double? = nil` (default — call sites antigos compilam)
    - `cd Packages/WhoopProtocol && swift build` sai com código 0
  </acceptance_criteria>
  <done>loadSchema() e schemaResourceURL() apontam para whoop_protocol_5; GravitySample tem gx/gy/gz opcionais com defaults; swift build passa</done>
</task>

<task type="auto">
  <name>Task 3: Python whoop_protocol package suporta schema 5.0 (SWIFT-06)</name>
  <files>server/packages/whoop-protocol/whoop_protocol/schema.py, server/packages/whoop-protocol/whoop_protocol/schema/whoop_protocol_5.json</files>
  <read_first>
    - server/packages/whoop-protocol/whoop_protocol/schema.py (padrão _SCHEMA_PATH + @lru_cache(maxsize=1) load_schema())
    - protocol/whoop_protocol_5.json (fonte canónica a copiar)
    - scripts/sync-schema-5.sh (confirma o pipeline de sincronização canónica)
  </read_first>
  <action>
    Copiar `protocol/whoop_protocol_5.json` para `server/packages/whoop-protocol/whoop_protocol/schema/whoop_protocol_5.json` (cópia byte-a-byte da fonte canónica).

    Em schema.py: adicionar `_SCHEMA_PATH_5 = os.path.join(os.path.dirname(__file__), "schema", "whoop_protocol_5.json")` e uma função `load_schema_5()` decorada com `@lru_cache(maxsize=1)` que abre `_SCHEMA_PATH_5` e retorna `Schema(json.load(fh))`, seguindo exactamente o padrão de `load_schema()` existente. NÃO remover `load_schema()` (4.0) — o package mantém ambos. NÃO alterar a assinatura de `load_schema()`.
  </action>
  <verify>
    <automated>python3 -c "import sys, json; d=json.load(open('server/packages/whoop-protocol/whoop_protocol/schema/whoop_protocol_5.json')); print('keys', len(d))"</automated>
  </verify>
  <acceptance_criteria>
    - Ficheiro `server/packages/whoop-protocol/whoop_protocol/schema/whoop_protocol_5.json` existe e é JSON válido
    - O ficheiro copiado é idêntico à fonte: `diff protocol/whoop_protocol_5.json server/packages/whoop-protocol/whoop_protocol/schema/whoop_protocol_5.json` não imprime diferenças
    - `grep -q 'def load_schema_5' server/packages/whoop-protocol/whoop_protocol/schema.py`
    - `grep -q '_SCHEMA_PATH_5' server/packages/whoop-protocol/whoop_protocol/schema.py`
    - `grep -q 'lru_cache' server/packages/whoop-protocol/whoop_protocol/schema.py` (decorator preservado no novo loader)
  </acceptance_criteria>
  <done>O package Python tem whoop_protocol_5.json e load_schema_5() funcional; load_schema() 4.0 intacto</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strap BLE → parseFrame() | bytes ATT não confiáveis entram no decoder; um wrapper malformado não pode causar out-of-bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | stripMaverick() / parseFrame() | mitigate | Todos os acessos a índices são protegidos por `guard frame.count >= N` antes da leitura; um wrapper com length inconsistente devolve nil em vez de crashar (guard `frame.count == length + 8`) |
| T-05-02 | Tampering | body stripped sem inner CRC | accept | O body Maverick não tem CRC32 interno (VERIFIED 5028/5028 frames); a integridade é garantida pela camada de link BLE encriptada/bonded; risco aceite — sem PII em trânsito além de biometria do próprio utilizador |
| T-05-03 | Information Disclosure | golden fixtures / schema | accept | Schema e fixtures sintéticos são públicos por design (repo público, provably synthetic); sem segredos |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum package novo instalado nesta fase (RESEARCH §Standard Stack confirma zero novas dependências); nada a verificar |
</threat_model>

<verification>
- `cd Packages/WhoopProtocol && swift build` sai com código 0
- stripMaverick é pura e protegida por guards de bounds
- parseFrame não chama verifyFrame() sobre body stripped
- Python load_schema_5() carrega o schema 5.0 copiado
</verification>

<success_criteria>
- SWIFT-01: loadSchema() carrega whoop_protocol_5.json (verificado por grep + build)
- SWIFT-02: parseFrame() faz strip do Maverick wrapper antes do processamento
- SWIFT-03/04: extractStreams()/extractHistoricalStreams() operam sobre o body plano com offsets body-absolute (build passa; paridade validada em 05-02)
- SWIFT-06: package Python tem load_schema_5() e o schema 5.0 copiado
</success_criteria>

<output>
Create `.planning/phases/05-ios-app-server-port/05-01-SUMMARY.md` when done
</output>
