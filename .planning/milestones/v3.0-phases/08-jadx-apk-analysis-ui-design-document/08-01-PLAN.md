---
phase: "08"
plan: "08-01"
title: "APK Acquisition + JADX Extraction"
wave: 1
depends_on: []
files_modified:
  - "re/capture/samples/apk/base.apk"
  - "re/capture/samples/apk/notes-draft.md"
autonomous: true
requirements:
  - UI-01
---

<objective>
Download the latest WHOOP Android APK from APKMirror (no Android device available — APKMirror fallback per D-01), open it in JADX-GUI, and extract the three raw data sources needed to build `docs/whoop-ui-reference.md`: (1) all UI-label strings from `res/values/strings.xml`, (2) the Activity/Fragment hierarchy from `res/layout/*.xml`, and (3) ViewModel property names (names only, no logic) for the field→model mapping. All findings recorded in a gitignored `notes-draft.md` under `re/capture/samples/apk/`.
</objective>

<tasks>

<task id="08-01-T1" type="execute">
  <title>Verify JADX, Java, and output directory prerequisites</title>
  <read_first>
    - `re/capture/jadx.md` — TOOL-03 runbook: prerequisites section and JRE troubleshooting
    - `scripts/check-tools.sh` — confirm which tool checks are in scope
    - `.gitignore` — confirm `re/capture/samples/` is ignored
  </read_first>
  <action>
    Run `jadx --version` and `java -version` to confirm JADX >= 1.5.1 and JRE 11+ are present.
    If `jadx` is not found: run `brew install jadx` (Brewfile includes it).
    If Java is missing or the system wrapper cannot find it: run the symlink command from the runbook — `sudo ln -sfn /opt/homebrew/opt/openjdk/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk.jdk` — then verify with `java -version`.
    Confirm output directory exists: `mkdir -p re/capture/samples/apk/`.
    Confirm gitignore coverage: `git status re/capture/samples/apk/` must show the directory as ignored (no untracked files listed).
  </action>
  <acceptance_criteria>
    - `jadx --version` prints a version string >= 1.5.1 (e.g. `1.5.5`)
    - `java -version` exits 0 and prints a JDK version >= 11
    - `ls re/capture/samples/apk/` exits 0 (directory exists)
    - `git status re/capture/samples/apk/` prints nothing or "nothing to commit" (gitignored)
  </acceptance_criteria>
</task>

<task id="08-01-T2" type="execute">
  <title>Download latest WHOOP APK from APKMirror</title>
  <read_first>
    - `re/capture/jadx.md` — APKMirror Fallback section (D-04 rule: APKMirror use when adb unavailable)
    - `DISCLAIMER.md §2` — legal boundary: APK gitignored, never committed
  </read_first>
  <action>
    Navigate to https://www.apkmirror.com and search for "WHOOP" to find the latest stable release of the WHOOP Android app (`com.whoop.android`).
    Download the `base.apk` variant for the most recent version (choose arm64-v8a if split APKs are offered; otherwise download the universal APK).
    Save the file to `re/capture/samples/apk/base.apk`.
    Record the exact version string (e.g. `WHOOP 9.32.0.0`) — this will be included in the header of `docs/whoop-ui-reference.md` for traceability.
    Verify: `git status re/capture/samples/apk/base.apk` must show the file as ignored.
  </action>
  <acceptance_criteria>
    - `ls -lh re/capture/samples/apk/base.apk` shows the file exists and is > 10 MB (a plausible APK size)
    - `git status re/capture/samples/apk/` shows no untracked or staged files (APK is gitignored)
    - The exact version string (e.g. `WHOOP 9.x.y.z`) is noted for use in the reference document header
  </acceptance_criteria>
</task>

<task id="08-01-T3" type="execute">
  <title>Extract strings.xml labels via grep (primary source for UI field names)</title>
  <read_first>
    - `re/capture/jadx.md` — TOOL-03 Steps 4–6: recording rule, what is/isn't permitted
    - `DISCLAIMER.md §2` — only string values (UI labels) permitted; no logic
    - `.planning/phases/08-jadx-apk-analysis-ui-design-document/08-CONTEXT.md` — D-06 specifies grep terms
  </read_first>
  <action>
    Unzip the APK to a temporary directory (APKs are ZIP archives):
    `unzip re/capture/samples/apk/base.apk -d re/capture/samples/apk/whoop_extracted/`
    (The extracted directory is gitignored under `re/capture/samples/`.)

    Run the following grep commands to extract UI label strings. Save full output to `re/capture/samples/apk/notes-draft.md` (gitignored):

    ```
    grep -i "recovery\|hrv\|heart_rate\|resting\|strain\|sleep\|spo2\|sp_o2\|oxygen\|temperature\|respiration\|respiratory\|breath\|score\|performance\|efficiency\|cycle\|disturbance\|rem\|deep\|light\|awake\|stage\|workout\|calories\|duration\|zone\|today\|overview\|trends\|device\|coach\|plus" \
      re/capture/samples/apk/whoop_extracted/res/values/strings.xml \
      > re/capture/samples/apk/notes-strings.txt 2>/dev/null || true
    ```

    Also extract ALL string entries (not just keyword-matching) for the 5 tab labels specifically:
    `grep -i "tab\|nav\|bottom\|navigation\|overview\|today\|sleep\|strain\|workout\|trend\|device\|coach\|member\|plus" re/capture/samples/apk/whoop_extracted/res/values/strings.xml >> re/capture/samples/apk/notes-strings.txt`

    Append the contents to `notes-draft.md` under a `## strings.xml labels` section heading.
  </action>
  <acceptance_criteria>
    - `re/capture/samples/apk/whoop_extracted/res/values/strings.xml` exists (unzip succeeded)
    - `re/capture/samples/apk/notes-strings.txt` exists and contains at least 20 matching lines
    - `notes-draft.md` under `re/capture/samples/apk/` contains a `## strings.xml labels` section with grep output
    - `git status re/capture/samples/apk/` shows all new files as ignored (not staged)
  </acceptance_criteria>
</task>

<task id="08-01-T4" type="execute">
  <title>Map Activity/Fragment layout hierarchy from res/layout/*.xml</title>
  <read_first>
    - `re/capture/jadx.md` — Step 4: JADX-GUI Resources panel navigation
    - `DISCLAIMER.md §2` — widget IDs and Activity names are factual; no method bodies
    - `.planning/phases/08-jadx-apk-analysis-ui-design-document/08-CONTEXT.md` — D-06 priority order
  </read_first>
  <action>
    Launch JADX-GUI on the extracted APK:
    `jadx-gui re/capture/samples/apk/base.apk`

    In the JADX-GUI Resources panel, navigate to `res/layout/` and identify layout XML files associated with the 5 main tabs. Look for file names containing:
    - `overview`, `dashboard`, `today` → Overview/Today tab
    - `sleep` → Sleep tab
    - `strain`, `workout`, `activity` → Strain/Workouts tab
    - `trend`, `chart`, `history` → Trends tab
    - `device`, `strap`, `member`, `coach`, `profile` → 5th tab (Device/Coach/Plus)

    For each tab, open the root Activity/Fragment layout and record in `notes-draft.md`:
    1. The layout file name (e.g. `activity_overview.xml`)
    2. The top-level container type (RecyclerView, ScrollView, ConstraintLayout, etc.)
    3. Child widget IDs referencing UI label strings (e.g. `android:text="@string/recovery_score_label"`)
    4. Any nested Fragment layout references

    Use JADX-GUI Text Search (Ctrl+F) to search for `layout` filenames containing the tab keywords above.
    Record all findings under `## Layout hierarchy` in `notes-draft.md`.
  </action>
  <acceptance_criteria>
    - `notes-draft.md` contains a `## Layout hierarchy` section
    - At least 3 of the 5 tabs have a named layout file recorded (e.g. `activity_overview.xml`)
    - Each tab entry lists at least 2 child widget IDs or string resource references (e.g. `@string/hrv_label`)
    - No method bodies, constructor code, or algorithm logic is recorded — only file names, widget IDs, string refs
  </acceptance_criteria>
</task>

<task id="08-01-T5" type="execute">
  <title>Extract ViewModel property names for field→model mapping (names only)</title>
  <read_first>
    - `re/capture/jadx.md` — Step 6 recording rule: property names permitted, method bodies forbidden
    - `DISCLAIMER.md §2` — field names are factual; no logic
    - `ios/OpenWhoop/Metrics/MetricsRepository.swift` — target model properties: `DailyMetric.recovery`, `DailyMetric.strain`, `DailyMetric.hrv`, `CachedSleepSession.stagesJSON`, `CachedSleepSession.efficiency` etc. — cross-reference against what JADX reveals
    - `.planning/phases/08-jadx-apk-analysis-ui-design-document/08-CONTEXT.md` — D-07 legal rule
  </read_first>
  <action>
    In JADX-GUI, use Class Search (Ctrl+N) to locate ViewModel classes for each tab. Search for class names containing:
    `RecoveryViewModel`, `SleepViewModel`, `StrainViewModel`, `TrendsViewModel`, `OverviewViewModel`, `DashboardViewModel`, `WorkoutViewModel`

    For each ViewModel found, record ONLY:
    1. The class name
    2. Property/field names (as declared, e.g. `val recoveryScore: LiveData<Int>`, record as `recoveryScore: LiveData<Int>`)
    3. Any data class or data model names referenced in property types

    DO NOT record: method bodies, constructor code, lambda expressions, business logic, string literals from logic, algorithm implementations.

    Append findings under `## ViewModel property names` in `notes-draft.md`.
    Cross-reference against `ios/OpenWhoop/Metrics/MetricsRepository.swift` published properties (`today: DailyMetric`, `lastNight: CachedSleepSession`) to identify which Android ViewModel fields correspond to which iOS model properties.
  </action>
  <acceptance_criteria>
    - `notes-draft.md` contains a `## ViewModel property names` section
    - At least one ViewModel class name is recorded (e.g. `RecoveryViewModel`)
    - Property names are recorded in the form `propertyName: Type` — no method bodies present
    - A cross-reference note maps at least 3 Android ViewModel properties to iOS `DailyMetric` or `CachedSleepSession` property names
    - `git status` confirms no new tracked files were created (all notes remain under gitignored `samples/apk/`)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `ls re/capture/samples/apk/base.apk` exits 0
2. `ls re/capture/samples/apk/whoop_extracted/res/values/strings.xml` exits 0
3. `ls re/capture/samples/apk/notes-draft.md` exits 0 and file size > 2 KB
4. `git status re/capture/samples/apk/` prints nothing (all new files gitignored)
5. `notes-draft.md` contains three sections: `## strings.xml labels`, `## Layout hierarchy`, `## ViewModel property names`
</verification>

<must_haves>
  <truths>
    - APK file exists at `re/capture/samples/apk/base.apk` and is gitignored
    - `notes-draft.md` captures findings from all three JADX sources (strings.xml, layout XMLs, ViewModel names)
    - Zero committed files contain decompiled source code, method bodies, or proprietary material
    - The exact APK version string is recorded for traceability in `docs/whoop-ui-reference.md`
  </truths>
</must_haves>

<success_criteria>
- [ ] JADX prerequisites confirmed (JADX >= 1.5.1, Java >= 11)
- [ ] Latest WHOOP APK downloaded to `re/capture/samples/apk/base.apk` (gitignored)
- [ ] `strings.xml` grep output captured for all 5 tabs' field label keywords
- [ ] Layout hierarchy documented for at least 3 of 5 tabs with widget IDs
- [ ] ViewModel property names extracted (names only) and cross-referenced with iOS model
- [ ] All findings in gitignored `notes-draft.md` — zero committed proprietary material
</success_criteria>
