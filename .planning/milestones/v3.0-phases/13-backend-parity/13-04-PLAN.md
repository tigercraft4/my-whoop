---
phase: 13-backend-parity
plan: 04
type: execute
wave: 4
depends_on:
  - 13-02
  - 13-03
files_modified:
  - server/ingest/app/analysis/calories.py
  - server/ingest/app/analysis/daily.py
  - Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift
  - Packages/WhoopStore/Sources/WhoopStore/Database.swift
  - ios/OpenWhoop/Upload/ServerSync.swift
  - ios/OpenWhoop/Tabs/TodayView.swift
  - ios/OpenWhoop/Design/Components/StrainCard.swift
  - ios/OpenWhoop/Charts/MetricKind.swift
autonomous: true
requirements:
  - ALG-13

must_haves:
  truths:
    - "calories.py tem rmr_kcal_per_day() que calcula RMR via Mifflin–St Jeor"
    - "daily.py persiste total_calories_kcal = RMR + exercise_kcal em metrics"
    - "DailyMetric Swift struct tem 4 novos campos opcionais: sleepPerformance, trainingState, sleepNeededMin, totalCaloriesKcal"
    - "Database.swift tem migração v9 com 4 × t.add(column:)"
    - "ServerSync.swift faz parse dos 4 novos campos JSON de /v1/daily e /v1/today"
    - "TodayView mostra CALORIES MetricCard quando totalCaloriesKcal é não nulo"
    - "StrainCard usa DailyMetric.trainingState do servidor quando não nulo, fallback ao cálculo client-side"
    - "MetricKind.sleepPerformance.value(from:) lê DailyMetric.sleepPerformance (campo do servidor) com fallback a efficiency * 100"
  artifacts:
    - path: "server/ingest/app/analysis/calories.py"
      provides: "rmr_kcal_per_day() Mifflin–St Jeor"
      exports: ["rmr_kcal_per_day"]
    - path: "Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift"
      provides: "DailyMetric com 4 novos campos + upsertDailyMetrics + dailyMetrics actualizados"
      contains: "sleepPerformance"
    - path: "Packages/WhoopStore/Sources/WhoopStore/Database.swift"
      provides: "migração v9 com 4 colunas nullable para dailyMetric"
      contains: "v9"
    - path: "ios/OpenWhoop/Upload/ServerSync.swift"
      provides: "getDaily() e getTodayMetric() com parse de 4 novos campos"
      contains: "sleep_performance"
    - path: "ios/OpenWhoop/Tabs/TodayView.swift"
      provides: "CALORIES MetricCard condicional"
      contains: "totalCaloriesKcal"
    - path: "ios/OpenWhoop/Design/Components/StrainCard.swift"
      provides: "training state preferência servidor > client-side"
      contains: "trainingState"
    - path: "ios/OpenWhoop/Charts/MetricKind.swift"
      provides: "sleepPerformance.value(from:) lê campo real do servidor com fallback"
      contains: "sleepPerformance"
  key_links:
    - from: "server/ingest/app/analysis/calories.py"
      to: "server/ingest/app/analysis/daily.py"
      via: "import + chamada rmr_kcal_per_day(device_profile)"
      pattern: "rmr_kcal_per_day"
    - from: "ios/OpenWhoop/Upload/ServerSync.swift"
      to: "Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift"
      via: "DailyMetric(..., sleepPerformance:, trainingState:, sleepNeededMin:, totalCaloriesKcal:)"
      pattern: "totalCaloriesKcal"
    - from: "ios/OpenWhoop/Design/Components/StrainCard.swift"
      to: "ios/OpenWhoop/BLE/TrainingState.swift"
      via: "fallback: daily?.trainingState ?? TrainingState.trainingState(recovery:strain:)"
      pattern: "trainingState.*TrainingState.trainingState"
    - from: "ios/OpenWhoop/Charts/MetricKind.swift"
      to: "Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift"
      via: "metric.sleepPerformance ?? metric.efficiency.map { $0 * 100 }"
      pattern: "metric\\.sleepPerformance"
---

<objective>
Implementar ALG-13 (calorias totais Mifflin–St Jeor + exercício) no servidor, e propagar os 4 novos campos da Fase 13 por toda a stack iOS: DailyMetric struct → GRDB migração v9 → ServerSync parse → TodayView display + StrainCard fallback. Inclui correcção de MetricKind.sleepPerformance para usar o campo real do servidor (ALG-10 success criteria 1).

Purpose: Este é o plano de integração final. O servidor já calcula sleep_performance, training_state e sleep_needed_min (planos 13-02 e 13-03). Este plano adiciona calorias e faz o iOS consumir todos os 4 valores novos. Corrige também o MetricKind que ainda lia efficiency * 100 em vez do campo sleepPerformance persistido pelo servidor.

Output: rmr_kcal_per_day() em calories.py, integração em daily.py, DailyMetric Swift com 4 campos, migração v9, ServerSync parse, CALORIES card no TodayView, Training State server-first no StrainCard, MetricKind.sleepPerformance server-first com fallback.
</objective>

<execution_context>
@/Users/francisco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/francisco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-backend-parity/13-RESEARCH.md
@.planning/phases/13-backend-parity/13-02-SUMMARY.md
@.planning/phases/13-backend-parity/13-03-SUMMARY.md
@server/ingest/app/analysis/calories.py
@server/ingest/app/analysis/daily.py
@Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift
@Packages/WhoopStore/Sources/WhoopStore/Database.swift
@ios/OpenWhoop/Upload/ServerSync.swift
@ios/OpenWhoop/Tabs/TodayView.swift
@ios/OpenWhoop/Design/Components/StrainCard.swift
@ios/OpenWhoop/Charts/MetricKind.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar rmr_kcal_per_day() a calories.py e integrar total_calories em daily.py (ALG-13)</name>
  <files>
    server/ingest/app/analysis/calories.py
    server/ingest/app/analysis/daily.py
  </files>
  <read_first>
    - server/ingest/app/analysis/calories.py — ler na íntegra (é pequeno) para perceber os _COEFFS existentes (Harris-Benedict), a estrutura das funções actuais, e onde adicionar a nova função rmr_kcal_per_day sem quebrar o que existe
    - server/ingest/app/analysis/daily.py — ler linhas 450–500 (exercise calc + metrics dict) para perceber onde device_profile é lido, onde os exercise_sessions são calculados, e onde adicionar total_calories_kcal ao dict metrics
  </read_first>
  <behavior>
    - rmr_kcal_per_day({"sex": "male", "weight_kg": 70, "height_cm": 175, "age": 30}) deve retornar aprox. 1648.75 (Mifflin: 10×70 + 6.25×175 - 5×30 + 5)
    - rmr_kcal_per_day({"sex": "female", "weight_kg": 60, "height_cm": 165, "age": 25}) deve retornar aprox. 1345.25 (Mifflin: 10×60 + 6.25×165 - 5×25 - 161)
    - rmr_kcal_per_day({}) deve retornar valor positivo com defaults (weight=70, height=170, age=30, nonbinary intercept)
    - rmr_kcal_per_day(None) deve retornar None
    - total_calories_kcal = None quando device_profile é None
  </behavior>
  <action>
Em calories.py, adicionar a função `rmr_kcal_per_day` logo após os `_COEFFS` existentes (antes de qualquer função existente). A função usa coeficientes Mifflin–St Jeor (ALG-13 spec), NÃO Harris-Benedict (que já existe nos _COEFFS actuais para Keytel):

Definir `_MIFFLIN_COEFFS` com chaves "male" (intercept +5.0), "female" (intercept -161.0), "nonbinary" (intercept -78.0) — todos com weight=10.0, height=6.25, age=5.0. Definir `rmr_kcal_per_day(profile: dict | None) -> float | None` que: retorna None se profile é None; lê weight_kg (default 70), height_cm (default 170), age (default 30), sex (lowercase, default "nonbinary"); selecciona coeficientes; calcula rmr = weight*c["weight"] + height*c["height"] - age*c["age"] + c["intercept"]; retorna max(0.0, rmr).

Em daily.py, após o bloco de cálculo de exercícios (onde `ex_dicts` é construído, aprox. linha 465), calcular:
- _rmr = _calories.rmr_kcal_per_day(device_profile) se device_profile não é None
- _exercise_kcal = sum(e.get("calories_kcal") or 0.0 for e in ex_dicts)
- _total_calories = round(_rmr + _exercise_kcal, 1) se _rmr não é None, else None

Verificar os imports actuais de daily.py — se `calories` já é importado como `_calories` ou tem outro alias. Se não estiver importado, adicionar `from . import calories as _calories` no bloco de imports do topo.

Adicionar a chave ao dict metrics: `"total_calories_kcal": _total_calories,`
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/my-whoop && python3 -c "
from server.ingest.app.analysis.calories import rmr_kcal_per_day
# Male 70kg, 175cm, 30y: 10×70 + 6.25×175 - 5×30 + 5 = 700 + 1093.75 - 150 + 5 = 1648.75
r_male = rmr_kcal_per_day({'sex': 'male', 'weight_kg': 70, 'height_cm': 175, 'age': 30})
assert abs(r_male - 1648.75) < 1.0, f'Expected ~1648.75, got {r_male}'
# Female 60kg, 165cm, 25y: 10×60 + 6.25×165 - 5×25 - 161 = 600 + 1031.25 - 125 - 161 = 1345.25
r_female = rmr_kcal_per_day({'sex': 'female', 'weight_kg': 60, 'height_cm': 165, 'age': 25})
assert abs(r_female - 1345.25) < 1.0, f'Expected ~1345.25, got {r_female}'
# None profile → None
assert rmr_kcal_per_day(None) is None
# Empty profile → defaults → positive
assert rmr_kcal_per_day({}) > 0
print('ALL OK')
"</automated>
  </verify>
  <done>rmr_kcal_per_day() existe em calories.py com coeficientes Mifflin–St Jeor correctos; daily.py calcula total_calories_kcal = RMR + exercise e inclui no dict metrics; sintaxe Python válida</done>
</task>

<task type="auto">
  <name>Task 2: Propagar 4 campos novos no stack iOS e corrigir MetricKind.sleepPerformance</name>
  <files>
    Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift
    Packages/WhoopStore/Sources/WhoopStore/Database.swift
    ios/OpenWhoop/Upload/ServerSync.swift
    ios/OpenWhoop/Tabs/TodayView.swift
    ios/OpenWhoop/Design/Components/StrainCard.swift
    ios/OpenWhoop/Charts/MetricKind.swift
  </files>
  <read_first>
    - Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift — ler na íntegra para perceber a struct DailyMetric (campos, init, tipos), upsertDailyMetrics (SQL INSERT + DO UPDATE), e dailyMetrics (SELECT + map)
    - Packages/WhoopStore/Sources/WhoopStore/Database.swift — ler as linhas 144–170 (migração v7 e v8) para perceber o padrão exacto de db.alter(table: "dailyMetric") { t in t.add(column:) }
    - ios/OpenWhoop/Upload/ServerSync.swift — ler linhas 351–412 (getDaily + getTodayMetric) para perceber o padrão de DailyMetric(...) construído a partir do dict JSON r
    - ios/OpenWhoop/Tabs/TodayView.swift — ler o ficheiro completo (é pequeno) para perceber onde inserir o CALORIES MetricCard
    - ios/OpenWhoop/Design/Components/StrainCard.swift — ler na íntegra para perceber trainingStateLabel e onde adicionar fallback logic
    - ios/OpenWhoop/Charts/MetricKind.swift — ler na íntegra para perceber o padrão actual de cada case em value(from:), especificamente o case .sleepPerformance (linha 167: `metric.efficiency.map { $0 * 100 }`)
  </read_first>
  <action>
Fazer as seguintes alterações em ordem:

--- MetricsCache.swift ---

1. Adicionar 4 campos ao struct DailyMetric (após `respRateBpm`):
   `public let sleepPerformance: Double?` (ALG-10: score 0–100),
   `public let trainingState: String?` (ALG-11: "RESTORATIVE"/"OPTIMAL"/"OVERREACHING"),
   `public let sleepNeededMin: Double?` (ALG-12: minutos necessários),
   `public let totalCaloriesKcal: Double?` (ALG-13: kcal totais do dia).

2. Actualizar o `public init(...)` de DailyMetric para incluir os 4 novos parâmetros com defaults `= nil`.

3. Actualizar `upsertDailyMetrics` SQL: adicionar as 4 colunas ao INSERT, 4 marcadores `?` ao VALUES, 4 entradas ao DO UPDATE SET (sleepPerformance = excluded.sleepPerformance, etc.), e os 4 valores ao array `arguments:`.

4. Actualizar `dailyMetrics` SELECT e map: adicionar os 4 campos ao SELECT e ao DailyMetric(...) no map.

--- Database.swift ---

5. Adicionar migração `v9` logo após o bloco `v8` (antes do `return migrator`):
   ```
   migrator.registerMigration("v9") { db in
       try db.alter(table: "dailyMetric") { t in
           t.add(column: "sleepPerformance", .double)
           t.add(column: "trainingState", .text)
           t.add(column: "sleepNeededMin", .double)
           t.add(column: "totalCaloriesKcal", .double)
       }
   }
   ```
   NUNCA usar `.notNull()` sem `.defaults(to:)` em colunas novas de migração.

--- ServerSync.swift ---

6. Actualizar `getDaily()` — no `DailyMetric(...)` construído a partir do dict JSON `r`, adicionar os 4 campos após `respRateBpm:`:
   `sleepPerformance: dbl(r, "sleep_performance") ?? dbl(r, "sleepPerformance")`,
   `trainingState: r["training_state"] as? String ?? r["trainingState"] as? String`,
   `sleepNeededMin: dbl(r, "sleep_needed_min") ?? dbl(r, "sleepNeededMin")`,
   `totalCaloriesKcal: dbl(r, "total_calories_kcal") ?? dbl(r, "totalCaloriesKcal")`.

7. Actualizar `getTodayMetric()` — mesmo padrão no DailyMetric(...) construído a partir do dict JSON `r`.

--- TodayView.swift ---

8. Adicionar um CALORIES MetricCard após a `hrvAndRhrRow` existente. Seguir o padrão de `strainCard` (computed var que lê metrics.today):
   `@ViewBuilder private var caloriesCard: some View` que mostra `MetricCard(title: "CALORIES", value: String(format: "%.0f", kcal), unit: "kcal", accentColor: WH.Color.strainAccent)` apenas se `metrics.today?.totalCaloriesKcal` não é nil.
   No body (scrollContent), adicionar `caloriesCard` logo após `hrvAndRhrRow`.

--- StrainCard.swift ---

9. Actualizar `trainingStateLabel` para preferir o valor do servidor quando disponível:
   Se `daily?.trainingState` não é nil, retornar esse valor directamente.
   Fallback: usar `TrainingState.trainingState(recovery: recoveryFraction * 100, strain: strain)` como antes.

--- MetricKind.swift (per ALG-10 success criteria 1) ---

10. Actualizar o case `.sleepPerformance` em `value(from:)` (actualmente na linha 167: `return metric.efficiency.map { $0 * 100 }`).
    Ler o ficheiro para confirmar o padrão exacto antes de alterar.
    Substituir por: `return metric.sleepPerformance ?? metric.efficiency.map { $0 * 100 }`
    Isto garante que quando o servidor persiste o score real de ALG-10 em DailyMetric.sleepPerformance, o iOS usa-o directamente; se o campo for nil (dados anteriores à Fase 13), usa o fallback de efficiency para retrocompatibilidade.
  </action>
  <verify>
    <automated>grep -l "sleepPerformance\|trainingState\|sleepNeededMin\|totalCaloriesKcal" \
      /Users/francisco/Documents/my-whoop/Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift \
      /Users/francisco/Documents/my-whoop/Packages/WhoopStore/Sources/WhoopStore/Database.swift \
      /Users/francisco/Documents/my-whoop/ios/OpenWhoop/Upload/ServerSync.swift \
      /Users/francisco/Documents/my-whoop/ios/OpenWhoop/Tabs/TodayView.swift \
      /Users/francisco/Documents/my-whoop/ios/OpenWhoop/Design/Components/StrainCard.swift | wc -l && grep -v '^#\|^//' /Users/francisco/Documents/my-whoop/ios/OpenWhoop/Charts/MetricKind.swift | grep -c "metric\.sleepPerformance"</automated>
  </verify>
  <done>Todos os 5 ficheiros Swift de propagação contêm referências aos 4 novos campos; v9 migration existe em Database.swift; TodayView mostra CALORIES card condicionalmente; StrainCard usa server trainingState com fallback client-side; MetricKind.sleepPerformance.value(from:) lê metric.sleepPerformance com fallback a efficiency * 100</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| /v1/today (servidor) → ServerSync iOS | JSON sobre HTTPS com Bearer token; iOS faz parse dos campos novos |
| ServerSync → GRDB (MetricsCache.swift) | Dados locais; sem superfície externa directa |
| DailyMetric.trainingState (string) → StrainCard UI | String do servidor renderizada como label; sem risco de XSS em SwiftUI |
| DailyMetric.sleepPerformance (Double?) → MetricKind.value(from:) | Campo nullable; fallback a efficiency garantido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Tampering | trainingState String do servidor → StrainCard | accept | StrainCard só mostra o label textual; o switch de cor usa comparação de string — um valor inesperado do servidor cai no case `default: WH.Color.textSecondary` sem crash |
| T-13-04-02 | Information Disclosure | totalCaloriesKcal em /v1/today | accept | Derivado de perfil (peso/altura/idade/sexo) que já está no servidor; sem PII adicional exposto |
| T-13-04-03 | Denial of Service | GRDB v9 migration falha | mitigate | NUNCA usar .notNull() sem .defaults(to:) em colunas novas; 4 colunas todas nullable — migração segura |
| T-13-04-04 | Tampering | ServerSync parse de campos novos com NSNumber | mitigate | Usar `ServerSync.dbl(r, "sleep_performance")` (já trata NSNumber vs Double) — nunca cast directo `as? Double` para campos numéricos |
| T-13-04-05 | Repudiation | calories calculadas sem perfil → None | accept | Se device_profile é None no servidor, total_calories_kcal é None; iOS não mostra o MetricCard — comportamento correcto e documentado |
| T-13-04-06 | Tampering | metric.sleepPerformance com valor fora de 0–100 | accept | MetricKind.sleepPerformance.fixedYDomain é 0...100; Charts.swift faz clip automático; sem risco de crash |
| T-13-04-SC | Tampering | npm/pip/cargo installs | accept | Este plano não instala dependências externas |
</threat_model>

<verification>
1. `grep "v9" /Users/francisco/Documents/my-whoop/Packages/WhoopStore/Sources/WhoopStore/Database.swift` deve mostrar a migração v9
2. `grep "totalCaloriesKcal" /Users/francisco/Documents/my-whoop/Packages/WhoopStore/Sources/WhoopStore/MetricsCache.swift` deve mostrar o campo no struct DailyMetric, no upsert e no select
3. `grep "sleep_performance" /Users/francisco/Documents/my-whoop/ios/OpenWhoop/Upload/ServerSync.swift` deve mostrar o parse em getDaily e getTodayMetric
4. `grep "CALORIES\|totalCaloriesKcal" /Users/francisco/Documents/my-whoop/ios/OpenWhoop/Tabs/TodayView.swift` deve mostrar o MetricCard
5. `grep "trainingState" /Users/francisco/Documents/my-whoop/ios/OpenWhoop/Design/Components/StrainCard.swift` deve mostrar a nova lógica de fallback
6. `grep -v '^#\|^//' /Users/francisco/Documents/my-whoop/ios/OpenWhoop/Charts/MetricKind.swift | grep "metric\.sleepPerformance"` deve mostrar o fallback `metric.sleepPerformance ?? metric.efficiency.map { $0 * 100 }`
7. `python3 -c "from server.ingest.app.analysis.calories import rmr_kcal_per_day; print(rmr_kcal_per_day({'sex':'male','weight_kg':70,'height_cm':175,'age':30}))"` deve imprimir ~1648.75
</verification>

<success_criteria>
- rmr_kcal_per_day() usa Mifflin–St Jeor (não Harris-Benedict); retorna None para profile=None
- daily.py calcula total_calories_kcal = rmr + sum(exercise_kcal); None sem perfil
- DailyMetric struct tem 4 campos opcionais: sleepPerformance, trainingState, sleepNeededMin, totalCaloriesKcal
- Database v9 migration adiciona 4 colunas nullable a dailyMetric (NUNCA notNull sem default)
- ServerSync.getDaily() e getTodayMetric() fazem parse dos 4 campos usando ServerSync.dbl() e string cast
- TodayView mostra MetricCard "CALORIES" só quando totalCaloriesKcal é não nulo
- StrainCard.trainingStateLabel usa DailyMetric.trainingState (servidor) quando não nulo; fallback ao TrainingState.trainingState() client-side
- MetricKind.sleepPerformance.value(from:) retorna `metric.sleepPerformance ?? metric.efficiency.map { $0 * 100 }` (campo real do servidor, retrocompat com dados antigos)
</success_criteria>

<output>
Criar `.planning/phases/13-backend-parity/13-04-SUMMARY.md` quando concluído.
</output>
