# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
**/.venv/
venv/

# Transient RE artifacts / logs (not source)
*.out
*.bin
# ...but the synthetic e2e replay fixture IS a committed test resource
!server/ingest/tests/fixtures/hist_biometric.bin
re_log.jsonl
control.txt
phase.txt

# Personal biometric captures — kept LOCAL, never published.
# (The small derived parity fixtures in Packages/.../Tests/.../Resources ARE committed.)
capture.jsonl
motion_capture.jsonl
optical_capture.jsonl
fixtures/*.jsonl

# Decompiled proprietary app — DO NOT publish
apk/

# Third-party clones (their own repos; documented in re/README.md)
whoomp/
whoop-reader/
research/
# ...but GSD planning research docs ARE committed
!.planning/research/

# Swift / Xcode (generated by XcodeGen / SwiftPM)
.build/
DerivedData/
ios/OpenWhoop.xcodeproj/
ios/OpenWhoop/Info.plist
.swiftpm/
**/xcuserdata/

# Git worktrees (isolated workspaces for feature branches)
.worktrees/

# Local agent/tooling config — never publish
.claude/

# macOS
.DS_Store

# Secrets (real values — never commit; Secrets.example.xcconfig IS committed as a template)
ios/OpenWhoop/Config/Secrets.xcconfig

# Personal device identity for RE scripts (real BLE UUID/MAC/serial — never commit;
# device_local.example.py IS committed as a template)
re/device_local.py
re/survey_5/device_local_5.py

# Programmatic GATT dump — embeds the real CoreBluetooth device name + address (local only)
re/survey_5/gatt_dump_5.json

# RE config/probe capture logs (key names, not source)
re/config_dump.jsonl
re/*.out

# Read-only command-probe dumps (command responses; local only)
command_probe.jsonl
re/command_probe.jsonl

# Xcode device-build derived data (CLI builds)
ios/build/

# Mac-side device-soak captures (gitignored; not the committed hist_biometric.bin fixture)
fixtures/soak_*.bin

# Raw BLE/HCI captures — kept LOCAL (may contain device IDs / SMP bonding material).
# Redacted hex excerpts + SHA256 checksums + metadata sidecars under re/capture/evidence/ ARE committed.
re/capture/samples/*
!re/capture/samples/.gitkeep
!re/capture/samples/README.md
re/capture/*.pklg

# Decompiled WHOOP APK + JADX project output — DO NOT publish (DISCLAIMER §2; D-04).
# Source APK via `adb pull` from your own device; record only enum names + numeric values in jadx.md.
re/capture/samples/apk/

# Test virtualenv (local only)
server/ingest/.venv-test/
